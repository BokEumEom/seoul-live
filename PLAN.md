# PLAN.md

서울 라이브(Seoul Live) 제품 계획.

진행 상황은 [STATE.md](./STATE.md), 기술 결정 근거는 `docs/superpowers/specs/`를 본다.

## 무엇을 만드는가

나가기 전에 목적지가 붐비는지 확인할 수 있는 토스 미니앱.

주말에 나가려는데 거기 붐비는지 도착해서야 아는 게 싫었다. 지도를 열면 명소별 혼잡도가 여유/보통/붐빔으로 뜨고, 내 위치 근처 명소가 거리순으로 나오면 된다.

비결은 허무하다. 서울시가 KT 기지국 신호 기반으로 5분마다 갱신하는 인구 데이터를 이미 공개하고 있다. 공공데이터는 이미 차려진 밥상이다.

## 설계 원칙

**직관적인 상태 시스템** — 혼잡도를 색으로 0.1초 만에 파악할 수 있게 한다. 서울 API는 4단계(여유·보통·약간 붐빔·붐빔)를 주는데, 시안은 3색이었지만 정보 손실을 피해 4단계를 그대로 산다.

**목적 중심의 UI** — 데이터를 나열하는 게 아니라 "아이와 나들이", "데이트" 같은 상황별 프리셋으로 의사결정을 돕는다.

**미래 예측** — 현재만이 아니라 "지금은 붐빔, 21시엔 여유 예상"까지 알려준다. 서울 API의 `FCST_PPLTN`이 시간별 예측을 주므로 1차부터 가능하다.

## 차수별 계획

### 1차 — 완료 (2026-08-04, 목업 기준)

화면 2개. 서울 API `citydata_ppltn` 하나로 둘 다 커버된다.

두 화면 모두 동작하고 시안 디자인이 반영돼 있다. 실데이터 전환은 인증키 발급을 기다린다 — 현재 상태는 [STATE.md](./STATE.md).

- **내 주변** — GPS 기반 명소 목록, 혼잡도 배지, 거리순 정렬, 카테고리 필터
- **혼잡예보** — 명소 상세, 현재 혼잡 상태와 안내 문구, 시간별 예측 차트, 한산해지는 시각, 길찾기·공유

하단 탭은 시안대로 4개를 노출하되 지도·더보기는 비활성으로 시작했다. 둘 다 이후에 활성화됐고, 2026-08-07 개편으로 셋이 됐다가, **2026-08-10 개편으로 탭이 아예 없어졌다.** 지금은 **단일 화면**이다 — 지도가 뷰포트를 채우고 그 위를 3단 바텀시트가 덮는다. 「내 주변」은 지도 위 FAB, 「혼잡예보」는 명소 상세, 「더보기」의 오늘의 서울은 시트 안 뷰가 됐다. 설계: `docs/superpowers/specs/2026-08-07-gmaps-style-shell-design.md` §2.2

**명소는 30곳으로 시작한다.** 목표는 121곳이지만 인증키의 하루 1,000회 제한 때문에 갤러리 등록 전까지는 30곳이 상한이다. 자세한 계산은 [AGENTS.md](./AGENTS.md)에 있다.

### 2차 — 지도와 프리셋

- ~~**지도 화면** — 명소별 혼잡도 마커. 카카오맵 키 발급과 도메인 등록이 선행돼야 한다.~~
  **완료 (2026-08-05).** 카카오맵이 아니라 **Google Maps JavaScript API**로 붙였다 — 앱인토스가 iframe을 금지해 Embed API가 빠졌고, 시안(`stitch_ui/_1`)도 Google 지도로 그려져 있었다. 길찾기는 계속 카카오맵·네이버지도 딥링크로 나간다(한국에서 Google 지도는 경로 안내가 제한된다). 설계: `docs/superpowers/specs/2026-08-04-map-tab-design.md`
- ~~**목적 프리셋** — 아이와 나들이 / 데이트 / 지금 핫플, 탭 하나로 필터~~
  **완료 (2026-08-06).** 지도 화면에 칩 3개. 카테고리와 실시간 혼잡도를 함께 쓰므로 시간대마다 결과가 바뀐다. 칩에 해당 개수를 표시하고 0이면 비활성이라 빈 화면을 만나지 않는다. 설계: `docs/superpowers/specs/2026-08-06-purpose-presets-design.md`
- ~~**즐겨찾기**~~
  **완료 (2026-08-07).** 저장소는 **기기 로컬** — 토스 `Storage` 브리지를 먼저 쓰고 없으면 `localStorage`로 떨어진다. 서버 저장은 익명 기기 ID 발급·보관을 부르는데 "무료고 로그인도 없다"는 방향과 `api/`가 상태를 갖지 않는 순수 중계기라는 구조에 어긋난다. 기기를 바꾸면 즐겨찾기가 사라지는 것은 이 선택의 알려진 대가다.

  **2026-08-10 개편으로 독립 탭이 아니라 「★ 내 장소」 필터 칩이 됐다.** 프리셋 칩 셋과 한 줄에서 배타적으로 동작한다 — 교집합(내 장소 ∩ 아이와 나들이)은 30곳 규모에서 결과가 0이 되기 쉬워 지원하지 않는다.

  **빈 상태 안내는 시트 안 목록으로 옮겨갔다.** 「내 장소」 칩은 담은 곳이 0이어도 눌린다(다른 칩은 0이면 비활성이다) — 이 칩의 0은 「지금 그런 곳이 없다」는 데이터 사정이 아니라 **아직 안 써 본 기능의 초기 상태**라서다. 누르면 빈 목록 문구가 「아직 담은 곳이 없어요. 명소를 열고 「저장」을 누르면 여기에 모여요.」로 답하고, 시트가 살짝 열림이었으면 그 답이 보이는 절반까지 올라온다. 사라진 즐겨찾기 화면의 빈 상태 안내가 이 자리를 물려받았다 — 막으면 신규 사용자에게 「어떻게 담는가」를 말하는 곳이 앱에 하나도 남지 않는다.
- **명소 121곳으로 확장** — 활용갤러리 등록 완료가 조건

### 3차 — 도시정보 대시보드 (일부 완료)

시안 4번 화면. ~~서울 API 5종이 추가로 필요하다.~~

**정정 (2026-08-04):** 공식 명세를 확인하니 아래가 전부 `citydata` **한 서비스의 필드**다. `citydata_ppltn` 대신 `citydata`를 부르면 같은 1회 호출로 다 온다.

**완료 (2026-08-07)** — 처음엔 「더보기」 탭이었으나 같은 날 개편으로 **명소 상세 안의 접이식 섹션**이 됐다. `citydata`가 장소 단위 API라 명소 하나를 골라야 성립하는데, 상세는 이미 명소가 정해진 자리라 고르는 단계(`AreaPicker`)가 통째로 없어졌다.

**접힌 채로 시작하고 펼칠 때 조회한다.** 상세를 열 때마다 부르면 하루 1,000회를 혼잡도와 나눠 쓰는 구조에서 압력이 그대로 는다.

비워진 「더보기」 자리는 **「오늘의 서울」**이 됐다 — 혼잡도 분포, 붐빔·여유 TOP, 카테고리별 평균, 재난문자 모음, 추천. 2026-08-10 개편으로 탭이 사라져 지금은 **시트 안 뷰**이고, 시트 맨 위 요약 스트립을 눌러 연다. **추가 API 호출이 0이다**: 이미 받아둔 30곳 스냅샷을 다르게 집계할 뿐이고, 재난문자도 상세에서 펼쳐 캐시에 들어온 것만 모은다.

설계 문서는 없다 — 속도를 위해 문서 단계를 건너뛰기로 정했고, 결정 근거는 코드 주석과 [STATE.md](./STATE.md)에 있다.

| | 항목 | 상태 |
| --- | --- | --- |
| 🅿️ | **주차 여유** — 잔여 면수, 여유순 5곳. "만차"와 "실시간 미제공"을 구분한다 | 완료 |
| 🚲 | **따릉이** — 대여소별 남은 대수, 많은 순 5곳 | 완료 |
| 🎁 | **문화행사** — 기간·장소·무료 여부·링크 | 완료 |
| 📢 | **재난문자** — 오면 화면 맨 위에 배너(`role="alert"`) | 완료 |
| 🌤️ | **날씨·대기** — 기온, 최고·최저, 강수 안내, 미세먼지·초미세먼지, 통합대기등급 | 완료 (원래 목록에 없었는데 같은 응답에 있어서 함께 붙였다) |
| 🚗 | **도로소통** — 지표 한 줄 + 평균속도 + 안내 문구 | 완료 (2026-08-11) |
| 🚨 | **사고·통제** — 통제 내용, 유형, 종료 예정 시각 | 완료 (2026-08-11) |

**도로소통과 사고통제는 한 섹션에 함께 넣었다.** 같은 주제이고 시트가 좁아 제목을 두 번 쓸 자리가 아니다. 사고가 없으면 목록만 빠진다.

**계획과 달라진 것 둘.** (1) 「정체·서행」이라고 적었지만 `ROAD_TRAFFIC_IDX`의 **값 목록이 공식 명세에 없다** — 「원활」·「서행」·「정체」가 명세 전문에서 0회다. 그래서 혼잡도처럼 네 톤에 겹치지 않고 문자열 그대로 보여준다. 짐작으로 매핑하면 처음 보는 값에서 틀린 색이 붙는다. (2) 사고통제는 「빨간 배지」가 아니라 도로소통 섹션 안의 목록이다 — 빨강(`crowded`)은 재난문자 배너가 쓰는 색이고, 차로 하나 통제를 같은 무게로 그리면 진짜 경보가 묻힌다.

**구간 목록은 일부러 안 읽는다.** 같은 섹션에 도로 구간마다 `LINK_ID`·`ROAD_NM`·`SPD`·`XYLIST`가 딸려 오는데 `XYLIST`는 보간점 좌표 덩어리다. 시트에서 필요한 것은 「지금 이 근처가 막히는가」 한 줄이고, 구간별로 보여줄 일이 생기면 그건 카드가 아니라 지도 위에 그릴 일이다.

지하철 도착·버스정류소·전기차충전소·상권·연합뉴스도 마찬가지로 같은 응답에 있다. **채택하지 않았다** — 설계(§2.6)가 상세에 넣을 것으로 고른 목록에 없다. 붙이려면 시트의 세로 예산을 먼저 봐야 한다.

**인구 구성 — 완료 (2026-08-10).** 명소 상세에 「지금 누가 있나」가 붙었다: 성별 비율, 연령대 10년 단위 8칸, 상주·비상주 비율. **여기는 진짜로 추가 호출이 0이다** — `citydata`가 아니라 이미 부르고 있는 **`citydata_ppltn`의 같은 응답**에 있던 필드다(`MALE_PPLTN_RATE`·`FEMALE_PPLTN_RATE`·`NON_RESNT_PPLTN_RATE`·`PPLTN_RATE_0`~`PPLTN_RATE_70`). 위 표의 접이식 도시 정보와 달리 쿼터를 한 푼도 더 쓰지 않는다.

파싱은 `data/compositionSchema.ts`에 따로 뒀다. 혼잡도를 파싱하는 `schema.ts`의 엄격한 스키마에 얹으면 비율 한 칸이 이상할 때 혼잡도까지 통째로 날아간다 — 부가 정보 때문에 본체를 잃지 않으려는 분리다. 관련 키가 하나도 없으면 `null`을 돌려 상세가 섹션을 통째로 숨긴다.

**"호출 횟수가 늘지 않는다"는 절반만 맞았다.** 한 화면 안에서는 맞지만, 실제 구현은 `citydata_ppltn`을 **교체하지 않고** `/api/cityinfo`를 추가로 얹었다 — 응답이 큰 `citydata`를 30곳 일괄 조회에 얹으면 인구만 필요한 지도·목록까지 매번 큰 응답을 받기 때문이다. 그래서 하루 1,000회 한도를 두 서비스가 나눠 쓴다. 계산과 대응(`CITYINFO_CACHE_TTL_SECONDS`)은 [STATE.md](./STATE.md)의 "도시 정보는 호출량을 늘린다"에 있다.

### 4차 — 과거 데이터가 필요한 것

서울 API는 **현재와 예측만 준다.** 요청 인자가 `KEY`·`TYPE`·`SERVICE`·`START_INDEX`·`END_INDEX`·`AREA_NM` 여섯뿐이고 날짜 인자가 없다 — 명세 전문에서 「이력」·「과거」·「조회일」이 0회다. 아래는 데이터를 직접 쌓아야 한다.

- 🗓️ **요일×시간 패턴 히트맵** — **부분 완료 (2026-08-12).** 상세에 7요일 × 3시간 8칸 표가 붙었다. 다만 데이터는 **이 기기에서 상세를 열 때마다 한 칸씩 쌓은 것**이다(토스 Storage). 추가 API 호출이 0이고 인프라도 없는 대신, 칸이 천천히 차고 기기마다 따로다. 화면이 그 사실을 숨기지 않는다 — 관측이 없는 칸은 「여유」가 아니라 **「관측 없음」**이고 지금까지 몇 번 봤는지를 카드가 적는다.
  - **남은 것은 서버 수집이다.** Vercel Cron이 매시간 30곳을 모아 저장소에 쌓으면 모든 사용자가 같은 패턴을 본다. 저장소 선택(Vercel Marketplace)과 하루 720회 호출이 필요해 **활용갤러리 등록이 선행**된다. 화면과 도메인은 출처를 모르게 만들어 뒀으니 `useWeekPattern`의 읽는 쪽만 갈아끼우면 된다.
- **어제 대비 비교** — 예측 차트에 어제 곡선 겹치기. 같은 이유로 아직이다.

### 검토 필요

- **CCTV 실시간 영상** — 별도 API이고 웹뷰 재생 제약을 확인해야 한다. 앱인토스는 iframe을 금지한다(YouTube만 예외).

## 현실 점검

초안에서 낙관적으로 잡았던 것들을 실제 확인 결과에 맞춰 정정한다.

| 초안 | 실제 |
| --- | --- |
| "API 키도 서버비도 0원" | 키는 무료지만 **서버는 필요하다.** 서울 API가 HTTPS를 지원하지 않아 중계 서버 없이는 토스 웹뷰에서 호출 자체가 막힌다. Vercel 무료 티어로 충분하다. |
| "서울 121곳" | 1차는 30곳. 하루 1,000회 제한 때문. 활용갤러리 등록하면 해제된다. |
| "화면만 만들었습니다" | 화면 외에 HTTPS 중계와 캐시 계층이 필요하다. 파일 2개 수준이지만 없으면 동작하지 않는다. |
| "홈 화면에 추가하면 앱처럼 (PWA)" | 앱인토스 미니앱이라 해당 없다. 토스 앱 안에서 실행된다. |
| "12시간 뒤 예측" | 가능. `FCST_PPLTN`이 시간별 예측을 준다. |
| "어제와 비교" | 1차 불가. 서울 API에 과거 데이터가 없다. 4차로 미룬다. |

## 출시 전 필수

- [ ] 서울 열린데이터광장 일반 인증키 발급
- [ ] **활용갤러리에 앱 등록** — 하루 1,000회 제한 해제. 이게 없으면 121곳 서비스가 성립하지 않는다.
- [ ] `src/data/areas.ts`의 명소 이름 30개를 공식 목록과 대조 검증
- [ ] Vercel 프록시 배포 + 환경변수 설정 (`SEOUL_API_KEY`, `CACHE_TTL_SECONDS`, `CITYINFO_CACHE_TTL_SECONDS`)
- [ ] `citydata` 실제 응답 한 번 확인 — 더보기 화면이 아직 본 적 없는 응답 형태에 기대고 있다 (STATE.md "더보기의 확인 안 된 가정")
- [ ] 토스 실기기 테스트
- [ ] Google Cloud — Maps JavaScript API 활성화, HTTP 리퍼러 제한, API 제한, 결제 예산 알림
- [ ] `DEMO_MAP_ID` → 자체 Map ID로 교체 (없으면 마커가 렌더되지 않는다)

무료고 로그인도 없다. 주말 나들이 전에 한번 열어보면 되는 앱을 목표로 한다.
