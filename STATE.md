# STATE.md

현재 진행 상황. 세션이 바뀌어도 여기만 읽으면 이어서 작업할 수 있게 유지한다.

**최종 갱신:** 2026-08-25 (영어 화면에 남아 있던 한국어 셋을 옮겼다 — 재난 갈래·사고 유형·지하철 방향. 아래 절)
**작업 브랜치:** `master`. 개편 둘과 상세 기능 셋이 전부 들어와 있고, 2026-08-20에 명소가 **121곳**이 됐다.
**원격:** <https://github.com/BokEumEom/seoul-live.git> — GitHub 기본 브랜치는 `master`다.

**로컬 `master`와 `origin/master`가 같다**(2026-08-25 확인, `07e934e`). 정확한 수는 늘 `git rev-list --count origin/master..master`로 다시 세라 — 이 줄은 적히는 순간부터 낡는다.

**브랜치는 `master` 하나뿐이다**(2026-08-20). `feat/nearby-forecast`·`feat/road-traffic-section`·`feat/weekly-pattern` 셋을 지웠다 — 셋 다 `master`에 없는 커밋이 **0개**이고 `git merge-base --is-ancestor`로 조상임을 확인한 뒤 `git branch -d`(머지 안 됐으면 거부하는 쪽)로 지웠다. 삭제 후 `git branch -a`로 `master`와 `origin/master`만 남은 것도 봤다. `origin/feat/nearby-forecast`는 그전에 이미 원격에서 사라져 있었고 `git fetch --prune`이 정리했다.

> **이 줄은 두 번째로 적는 것이다.** 이전 STATE가 2026-08-07에 지웠다고 적었지만 실제로는 남아 있었다. 그래서 위에 **무엇으로 확인했는지**를 함께 적었다 — 「지웠다」는 문장만으로는 다음 사람이 믿을 근거가 없다. 되살릴 일이 생기면 `git reflog`에 tip이 남아 있다(`be27cc6`·`24f2eb6`·`50af3a2`).

**새 세션이면 여기부터:** 「한 줄 요약」 → 「다음에 할 일」의 *다음 후보* → 「사람이 해야 하는 것」 순으로 읽으면 지금 상태와 다음 할 일이 잡힌다. 나머지 절은 그때그때 참조용이다.

**낡은 문장을 조심하라.** 이 파일은 append로 자라서 아래쪽일수록 오래된 주장이 섞여 있다. **날짜가 붙은 절과 커밋 로그가 우선이고, 날짜 없는 서술이 그와 어긋나면 후자가 낡은 것이다.** 2026-08-18에 「인증키가 아직 없다」는 낡은 한 줄 때문에 실제로 잘못된 판단을 내렸다(아래 「한 줄 요약」의 인용 참고).

**작업 방식:** `master`가 기준선이다. 문서 수정 같은 건 `master`에 직접 올리고, 새 기능만 브랜치를 딴다(2026-08-07 사용자 결정).

## 한 줄 요약

**Google Maps 스타일 셸 개편 완료(2026-08-10).** **화면이 하나다.** 탭바도 상단바도 없다 — 앱 셸은 `<main className="h-dvh">` 한 줄이고 그 안에 `HomeScreen` 하나가 산다.

지도가 뷰포트를 꽉 채우고 **3단 바텀시트**(살짝 열림 16% / 절반 56% / 전체 92%)가 그 위를 덮는다. 공간을 나눠 갖지 않으므로 상세를 열어도 지도는 뒤에서 온전한 크기로 살아 있다. 검색 바와 필터 칩은 지도 위에 떠 있고, **전체(92%)로 펼치면 검색 바·칩·FAB이 전부 물러난다** — 그 열이 시트 손잡이를 통째로 덮기 때문이다.

시트 안에는 뷰가 **셋** 들어간다: **목록**(요약 스트립 → 위치 안내 → 카테고리 → 구분선 목록) / **명소 상세**(탭 일곱: 요약·인구·교통·주변·날씨·행사·안전) / **오늘의 서울**. 오늘의 서울은 시트 맨 위 요약 스트립으로 연다.

> **이 문단은 2026-08-21에 세 곳을 고쳤고, 2026-08-25에 그중 하나를 되돌렸다.** 명소 상세는 2026-08-20에 시트 밖 전체 화면 층으로 나갔다가 **2026-08-21에 시트 안으로 돌아왔다**(`375a58d`) — 「장소를 골라도 지도가 살아 있어야 한다」는 사용자 결정이다. 전체 화면으로 뺐던 근거(상세가 5,395px이라 시트에 안 들어간다)는 **같은 회차에 탭 일곱으로 가르면서 이미 사라져 있었다.** 목록 안의 「정렬」 줄은 2026-08-21에 없어졌고(혼잡도 칩이 그 축을 가져갔다), 즐겨찾기는 「★ 내 장소」 필터 칩에서 **지도 우하단 FAB**이 됐다.
>
> **같은 문단이 두 회차 연속으로 낡았다.** 이 파일이 「새 세션이면 여기부터」로 지목하는 자리라 낡으면 다음 사람이 없는 것을 찾는다 — 화면 구조를 건드린 회차는 **커밋 전에 이 문단을 다시 읽어라.**

명소 상세에 **인구 구성**(성별·연령대·상주비율)이 붙었다. `citydata_ppltn`의 같은 응답에 있던 필드라 **추가 API 호출이 0이다.**

**그 뒤 명소 상세를 셋 더 채웠다(2026-08-11~12).** 전부 `master`에 머지돼 있다.

| 더한 것 | 출처 | 추가 호출 |
| --- | --- | --- |
| **도로소통·사고통제** | `citydata`의 `ROAD_TRAFFIC_STTS`·`ACDNT_CNTRL_STTS` | 0 |
| **대체 데이터 표시** | `citydata_ppltn`의 `REPLACE_YN` | 0 |
| **요일×시간 패턴** | **없다 — 이 기기에 쌓는다** | 0 |

앞의 둘은 이미 받고 있던 응답에서 안 쓰던 필드를 읽은 것이다. 세 번째는 다르다 — **서울 API에 과거 조회가 없어서**(요청 인자 여섯에 날짜가 없다) 상세를 열 때마다 토스 Storage에 한 칸씩 쌓는다. 그래서 칸이 천천히 차고 기기마다 따로이며, 화면이 그 사실을 숨기지 않는다(빈 칸은 「여유」가 아니라 「관측 없음」). 서버 수집으로 갈아끼울 자리는 `useWeekPattern` 하나다.

**실데이터로 이미 넘어가 있다(2026-08-13).** 인증키를 붙였고 `.env`의 `VITE_USE_MOCK=false`다. 실호출 응답 한 벌이 `docs/fixtures/citydata-광화문덕수궁.json`에 있고 `citydataFixture.test.ts`가 파서에 통과시킨다. 목업은 없어지지 않았다 — `VITE_USE_MOCK=true`로 언제든 돌아간다(쿼터를 아끼며 화면을 만질 때 쓴다).

> **이 줄에는 「인증키가 아직 없다」고 적혀 있었다.** 2026-08-18에 그걸 그대로 믿고 「이 앱은 실데이터를 한 번도 본 적이 없다」는 잘못된 판단을 내렸고, 그 위에서 우선순위를 제안했다. **낡은 문장이 다음 사람을 실제로 오도한 사례다** — 커밋 로그(`git log --oneline | grep 인증키`)가 정답을 갖고 있었다.

**그 뒤 인파레이더와의 격차를 좁혔다(2026-08-14).** 사용자 지적 넷 중 셋을 했다.

| 지적 | 한 것 |
| --- | --- |
| 「전체 데이터가 다 노출된다」 | 도시정보 접이식을 없애고 자동 조회. 요약 칩 줄도 붙였다 |
| 「로드 시간이 더 짧다」 | 상세가 목록 캐시에서 씨앗을 받아 **스켈레톤 없이 즉시** 열린다 |
| 「따뜻한 톤·아이콘·다크모드」 | 크림 팔레트 + 다크 모드(기본은 밝게) + 절 아이콘 |
| 「언어지원」 | **안 했다** — 크기 때문에 설계만. 「다음 후보」 6번 |

같은 날 **주차장·따릉이를 지도에서 짚는 것**도 붙였다(사용자 추가 지적).

**주소를 쓰기 시작했다(2026-08-18).** 아래 「주소가 없어서 셋이 깨져 있었다」 절이 정본이다. 「라우터도 탭 상태도 없다」고 적힌 옛 문장을 보거든 이 절과 `AGENTS.md`의 라우팅 조항을 믿어라.

## 새 디자인(Seoul Now)을 반영했다 — 2026-08-20

**시안이 통째로 바뀌었다.** `stitch_ui_ux/`에 화면 10종(`_1` 지도 / `121` 탐색 / `_2` 명소 상세 / `_3`~`_9` 도메인 상세)과 토큰 정본 `seoul_now/DESIGN.md`가 들어왔다.

**사용자 결정: 다크모드 유지, 하단 5탭 내비게이션 보류, 상세 전체 화면이 주된 것, 나머지는 반영.**

### 1. 토큰 정본이 `docs/DESIGN.md`로 돌아왔다

`tokens.test.ts`의 양방향 대조가 **다시 살아 있다**(정본이 없던 동안은 한 줄 알림으로 비켜서 있었다).

| | 옛 정본(Seoul Flow) | 새 정본(Seoul Now) |
| --- | --- | --- |
| 중립 | 따뜻한 크림 `#fffbf4` | 차가운 `#f9f9ff` |
| primary | `#004ac6` | `#005bbf` (Seoul Blue) |
| 제목 스케일 | 32 / 24 / 20 | **24 / 20 / 16** |
| 다크 | 따뜻한 ~35° | 차가운, 같은 색상 |

**중립이 차가워진 것은 되돌림이 아니라 다른 해법이다.** 옛 근거는 「혼잡도 4색이 따뜻해서 배경도 따뜻해야 한다」였고 관찰은 맞았다. 새 정본은 배경을 데이터 쪽으로 옮기는 대신 **데이터를 팔레트 안으로 들인다** — 혼잡도 4색이 이 배색의 secondary·tertiary·error 계열에서 나온다.

**시안에 없어서 우리가 갖고 있는 것 넷**: 혼잡도 4단계, 히트맵 램프, 브랜드 색 둘, 다크 팔레트 전부. 각각의 이유는 `docs/DESIGN.md`의 절에 있다.

**본문 16px은 안 줄였다.** 시안의 `body-lg`가 16px이고, 걸으면서 읽는 화면에서 14px 본문은 다르게 잰다 — 제목은 지도를 먹지만 본문은 안 그렇다.

### 2. 명소 상세가 전체 화면 + 탭이 됐다

**고친 것은 5,395px이다**(2026-08-20 실측, 390×844). 도시 정보를 상시 펼침으로 바꾸면서 그렇게 자랐고 길찾기가 화면 열두 개 아래에 있었다.

```
DetailAppBar    뒤로 · 이름 · 저장/인스타/공유   (스크롤해도 남는다)
DetailHero      지금은 약간 붐벼요 / 74,000~76,000명 / API 안내 / 11:00 기준
DetailTabs      요약 인구 교통 주변 날씨 행사 안전   (sticky)
[panel]
MapLinkButtons  카카오맵 네이버 티맵            (sticky bottom, 탭 밖)
```

- **요약 탭의 2열 카드 여덟 칸이 목차이면서 값이다.** 접힌 절은 무엇이 있는지조차 감추지만 카드는 값까지 보여준 뒤 자세히 볼 사람만 넘긴다. 누르면 그 탭으로 간다.
- **지도와 시트는 언마운트되지 않고 `inert`다.** 뒤로가기가 즉시여야 하고 「지도에서 보기」가 그 지도를 옮겨야 한다.
- **「지도에서 보기」가 상세를 닫는다.** 예전에는 시트를 half로 내리기만 했는데(그때는 지도가 시트 뒤에 반쯤 보였다), 전체 화면을 안 닫으면 지도가 옮겨간 것을 볼 수가 없다.
- **상권 탭은 안 세웠다.** 시안에는 있지만 `LIVE_CMRCL_STTS`(24필드)를 아직 안 읽는다 — 빈 탭보다 없는 편이 낫다. 아래 「남은 일」.

**지운 것 둘.**

| 지운 것 | 이유 |
| --- | --- |
| 요약 칩 줄(`CityInfoChips`) | 카드 격자가 같은 일을 더 잘 한다. 셈법(`parkingVacancyRate`·`totalBikes`·`subwayLineCount`)은 카드가 그대로 쓴다 |
| 지도 CCTV 마커 층 | 그려지는 조건이 「상세가 열려 있을 때」인데 그때 지도가 통째로 덮인다. **되살릴 자리는 새 시안 `_1`의 지도 레이어 토글이다** |

### 3. 아이콘·톤

- 즐겨찾기가 **별 → 책갈피**다(라벨이 이미 「저장」이고 시안이 `bookmark`를 쓴다). 필터 칩의 `★` 문자도 같은 글리프로 바꿨다 — 문자는 기기 폰트를 탄다.
- 카드용 글리프 일곱을 더했다: `people`·`air`·`thermostat`·`clock`·`storefront`·`payments`·`receipt`.
- **혼잡도 핀 넷의 밝기를 지도 타일에 맞췄다.** 처음에 「보통」을 `#7d5800`으로 뒀다가 헤드리스 실측 화면에서 **갈색으로 읽혔다** — 대비 계산은 맞았지만 색상이 틀렸다. 밝은 타일에서 3:1을 지키는 선까지 올려 금색(`#b07800`)이 되게 했다.

### 4. 필터 칩에 상태 축이 생겼다

「지금 핫플」이 **「한적」·「붐빔」**으로 갈렸다. 목적 칩(아이와 나들이·데이트)은 카탈로그 태그에 기대는데 **121곳 중 19곳에만 붙어 있다** — 상태 칩은 태그가 필요 없어 전부에 걸린다. 목적 칩은 그대로 두되 뒤로 밀었다.

> **하루 만에 다시 갈렸다.** 2026-08-21에 「한적」·「붐빔」 둘이 **혼잡도 네 등급**(여유·보통·약간 붐빔·붐빔)이 됐다. 이 절의 근거(목적 칩이 19곳에만 걸린다)는 그대로 유효하고 갈래 수만 바뀐 것이다 — 정본은 아래 「시안 `_1` 상단을 마저 반영했다」 2번이다.

### 반영하지 않은 것

| 시안 | 왜 |
| --- | --- |
| 하단 5탭 내비 | **사용자가 보류**(2026-08-20) |
| 지도 레이어 토글(날씨/교통/대중교통/주차) | 121곳어치 도시정보가 없다 — 명소당 1회 호출이 든다 |
| 목록 카드의 인원·기온·도로·행사 4칸 | 같은 이유. 목록에 있는 것은 혼잡도뿐이다 |
| 행사 분류 칩(축제·전시·공연) | 서울 API의 행사 항목에 분류 필드가 없다 |
| 재난 3단 배지(심각도 높음/주의/안내) | 등급 필드가 없다. 짐작해 붙이면 앱이 하지 않은 판단을 한 것으로 읽힌다 |
| 웹폰트(Plus Jakarta Sans / Be Vietnam Pro) | 2026-08-12 결정 유지 — 화면의 거의 전부가 한글이라 폴백이 기본이 된다 |
| 주차장 요금·운영시간 화면(`_5`) | 그 필드를 아직 안 읽는다 |

## 시안 `_1` 상단을 마저 반영했다 — 2026-08-21

커밋 `d7d4e41` 하나다. **넷을 한 벌로 묶은 것은 셋이 `HomeScreen`의 같은 자리를 나눠 써서** 따로 끊으면 중간 커밋이 컴파일되지 않기 때문이다. `tsc -b --force` 0, `eslint .` 0, 테스트 102파일 **1,253개** 통과.

### 1. 전환 애니메이션이 생겼다 — framer-motion 13.1.0

첫 런타임 의존성 추가다. 뿌리는 `src/app/MotionProvider.tsx` 한 곳이고 두 겹이다.

| 겹 | 무엇을 막나 |
| --- | --- |
| `MotionConfig reducedMotion="user"` | `index.css`의 `prefers-reduced-motion` 처방은 **CSS 전환만** 끈다. framer는 JS로 매 프레임 스타일을 써서 그 미디어 쿼리가 안 닿는다 |
| `LazyMotion features={domAnimation} strict` | 번들. 빌드 뒤 gzip 실측(2026-08-21): `m`+`domAnimation` **192,030B**, `motion` 그대로면 **205,036B** — 13KB·6.8% |

**`strict`가 핵심이다.** 번들 크기는 테스트가 안 잡는다. 이 플래그가 없으면 누군가 `motion.div`를 한 번 쓰는 순간 트리 셰이킹이 풀려 전부 다시 들어오는데 **아무 신호가 없다.** `strict`는 `motion.*`를 만나면 던져서 그 회귀를 막는다 — 앞으로 쓸 것은 언제나 `m.*`다.

**프로바이더가 앱 루트가 아니라 화면마다 있다.** 한 곳에 두고 싶은 모양이지만 `m.*`는 `LazyMotion` 없이 렌더되면 기능이 하나도 안 실려 `initial` 스타일에 얼어붙는다 — 확인했다:

```
<m.div initial={{ opacity: 0, x: 12 }} animate={{ opacity: 1, x: 0 }} />
  → <div style="opacity: 0; transform: translateX(12px);">
```

그 스타일이 지워지지 않으므로 **내용이 통째로 안 보인다.** 루트에만 두면 그 화면을 따로 렌더하는 곳(테스트가 그렇다)에서 조용히 빈 화면이 된다. 화면이 스스로 들고 있으면 혼자 렌더돼도 성립하고, 중첩은 무해하다(둘 다 컨텍스트일 뿐이고 기능은 한 번만 실린다).

**두 자리에 붙였다.** 상세 층은 `AnimatePresence` + `x: '100%'`로 밀려 들어오고 나간다(`AnimatePresence`가 필요한 이유는 **나가는 쪽** 때문이다 — 조건부 렌더는 나가는 애니메이션을 재생할 틈이 없다). 상세 탭 패널은 `detailTabIndex`가 준 방향으로 갈린다 — 방향이 없으면 「탭을 옮겼다」와 「같은 탭이 새로 그려졌다」가 화면에서 구별되지 않는다.

### 2. 정렬 줄이 없어졌다 — 혼잡도 칩이 그 축을 가져갔다

**2026-08-20에 우리가 만든 겹침을 되돌린 것이다.** 그날 칩 줄에 「한적·붐빔」을 넣었는데, 바로 아래 정렬 줄이 「여유한 순·붐비는 순」이었다 — 같은 축을 두 줄이 나눠 갖고 있었다.

칩을 시안 `_1`대로 **네 등급**(여유·보통·약간 붐빔·붐빔)으로 갈랐고 `SortSegmented`(48px)는 지웠다.

- **표에서 파생시킨다.** `CONGESTION_LEVELS.map`이다 — 손으로 적으면 등급은 다섯인데 칩은 넷인 화면이 조용히 만들어진다.
- **라벨이 등급 그 자체다.** 예전에는 칩이 「한적」이라 말하고 바로 아래 목록 배지가 「여유」라 말했다. 같은 것을 두 낱말로 부르면 사용자는 그 둘이 다른 것이라고 읽는다. 이제 칩·배지·지도 마커가 한 낱말을 쓴다.
- **키는 등급이 아니라 톤**(`calm`/`normal`/`busy`/`crowded`)이다. 주소·저장소에 실릴 수 있어 한국어를 안 쓰고, `CongestionTone`을 재사용하면 톤과 프리셋이 어긋날 자리 자체가 없어진다.
- **순서가 하나가 됐다:** 가까운 순, 좌표가 없으면 여유로운 순. 「가까운 순」은 칩으로는 못 하는 일이라 남았고 고르게 할 이유가 없어 `buildNearbyList`의 기본이 됐다. `useHomeFilters`에서 `sort` 상태가 사라졌다.
- **거르기가 줄 세우기보다 낫기도 하다.** 「여유한 순」은 붐비는 곳을 아래에 남겨 스크롤해야 알 수 있지만 칩은 목록에서 아예 뺀다. 두 축은 곱해진다 — 「여유」 칩을 켜면 여유로운 곳이 가까운 순으로 나온다.

### 3. 지도 FAB이 셋이 됐다 — `RecenterButton` → `MapFabStack`

**내 장소 · 앱 공유 · 내 주변**이 우하단에 세로로 선다.

- 「내 장소」가 칩 줄에서 FAB으로 옮겨 가며 개수가 사라져 **배지로 살렸다.** `favorites.length`가 아니라 `filterCounts`가 센 값이다 — 카테고리로 좁혔거나 카탈로그에서 이름이 바뀐 곳까지 세면 칩에 2가 뜨는데 목록은 0인 화면이 된다.
- **공유는 고른 명소가 아니라 앱을 공유한다.** 지도 화면에는 「고른 명소」가 없다(마커를 누르면 곧장 상세가 덮는다). 그 명소의 저장·공유는 상세 상단 바가 이미 들고 있다.
- FAB의 자리 산식(`SHEET_RATIO[detent] + 0.02`)과 「`full`에서는 안 그린다」 규칙은 그대로다. `sheet.ts`의 `half`를 고치면 `MapFabStack`의 `BOTTOM_CLASS`가 따라와야 한다.

### 4. 장소 클릭 = 상세다 (중간 단계를 끼웠다가 되돌렸다)

작업 중에 「선택」 단계를 하나 끼웠다 — 마커를 누르면 시트가 접히며 요약 한 줄만 보이고, 그걸 다시 눌러야 상세가 열리는 식이었다. **사용자가 기존 방식 유지를 이미 말했는데 어긴 것이다.** 되돌렸다: 마커든 목록 줄이든 누르면 전체 화면 상세가 곧장 열린다.

### 이 작업이 남긴 것

- ~~**`act()` 경고 80건이 새로 뜬다.** framer가 테스트 밖에서 상태를 갱신하는 것~~ — **틀렸다. 2026-08-21에 재서 고친다. 아래 「`act()` 경고를 framer 탓으로 적었는데 아니었다」 절이 정본이다.**
- **토스 웹뷰에서 한 번도 안 돌려 봤다.** 「사람이 해야 하는 것」의 실기기 회차에 **애니메이션 항목이 하나 늘었다** — 시트 드래그·지도 팬 충돌과 같은 회차에 본다.
- **혼잡도 칩 넷 중 몇이 살아 있는지 모른다.** count가 0인 칩은 비활성(회색)이 되는데, 실데이터에서 새벽에 121곳이 전부 「여유」면 **칩 줄의 4분의 3이 회색이다.** 목업으로는 판단할 수 없다 — 「프리셋 정의가 사용자 기대와 맞는지 확인되지 않았다」와 같은 종류의 미확인이다.
- **첫 런타임 의존성이 늘었다.** 토스 미니앱은 남의 앱 안에서 열리는 웹뷰라 첫 바이트가 곧 체감 속도다. 192KB(gz)가 지금의 기준선이다.

### 죽은 참조 15곳을 정리했다

`SortSegmented`·`RecenterButton`을 지우면서 **살아 있는 파일 15곳이 없는 파일을 가리키게 됐다.** 개명은 기계적이었지만 하나는 아니었다.

**`SortSegmented.tsx`가 「상수 표가 아니라 함수인 이유」의 정본을 들고 있었다** — 모듈 최상위에서 `t()`를 부르면 import 시점의 언어로 굳는다는 i18n 함정이다. 다섯 파일(`LocationNotice`·`MapLinkButtons`·`PopulationLead`·`MapUnavailableNotice`·`ThemeSetting`)이 「거기에 한 벌 있다」고 가리키고 있었는데 **그 설명이 git 히스토리로만 남을 뻔했다.** 근거를 `src/i18n/t.ts`로 옮겼다 — 그 규칙을 강제하는 `i18n.test.ts`의 짝이고, `t()`를 부르러 온 사람이 지나가는 자리다.

이 규칙이 사람이 지키는 것이던 동안 **6개 파일 19개 문자열**이 한국어로 굳어 있었다(2026-08-18). 사전 검사로는 안 잡힌다 — 사전에 항목이 **있는데도** 화면에 한국어가 뜨는 결함이라 전부 초록이다.

### `act()` 경고를 framer 탓으로 적었는데 아니었다

**같은 날 두 번 틀리게 적었다**(커밋 `d7d4e41`·`d440d9f`의 메시지와 이 파일). 「framer가 만든 경고 80건이 **새로** 뜬다」고 썼는데 **셋 다 틀렸다.**

| 적은 것 | 실제 | 어떻게 쟀나 |
| --- | --- | --- |
| framer가 만들었다 | **아니다.** framer 이전 커밋(`469e67c`)에서 `AreaDetailScreen.test.tsx` 하나가 **이미 50건**이고, framer 이후에도 **똑같이 50건**이다 | `git worktree add /tmp/pre-framer 469e67c` + `node_modules` 심볼릭 링크로 같은 러너에서 양쪽을 돌렸다 |
| 새로 뜬다 | **아니다.** 전체 스위트가 이전 82건 → 지금 80건이다. 오히려 둘 줄었다(지운 테스트 몫) | 위와 같은 회차 |
| stderr가 시끄럽다 | **기본 리포터에서는 한 건도 안 보인다.** `npm test`(= `vitest run`)는 0건, `--reporter=dot`·`--reporter=verbose`에서만 80건이 뜬다 | 세 리포터로 각각 세었다 |

**진짜 원인은 비동기 저장소 읽기 둘이다.** 렌더가 끝난 뒤 마이크로태스크로 도착해서 `act()` 밖 갱신이 된다.

| 자리 | 경고에 찍히는 컴포넌트 |
| --- | --- |
| `favoritesStore.ensureLoaded()` → `loadFavorites().then(…)` → `emit()` | `ActionButtons`(`useSyncExternalStore` 구독) |
| `useWeekPattern`의 `useEffect` → `await loadPattern(areaName)` → `setLoaded(…)` | `AreaDetailScreen` |

둘 다 framer보다 오래됐고 **토스 Storage를 읽는 일 자체가 비동기라서** 생긴다.

**「무엇을 해도 통과하는 테스트」는 아니었다.** 두 읽기를 **영영 안 끝나게** 변이시켜(`await new Promise(() => {})`) `AreaDetailScreen.test`·`HomeScreen.test`를 돌렸더니 **6개가 죽었다** — 로드 완료에 의존하는 테스트는 제대로 기다리고 있다는 뜻이다. 나머지 136개는 통과했고, 그것들이 경고를 내는 쪽이다(기다리지 않지만 로드된 상태에 의존하지도 않는다).

**그래서 안 고쳤다.** 고치려면 기다리지 않는 테스트 백여 개에 `await`를 넣거나 제품 코드에 테스트용 이음매를 뚫어야 하는데, `npm test`에서는 보이지도 않고 가려진 결함도 없다. **고칠 거면 값어치는 「경고를 지운다」가 아니라 「로드 전 상태를 단언하는 테스트가 있는가」에 있다** — 위 변이가 그 질문에 「없다」고 답한 것이고, 새 테스트를 쓸 때 다시 물어야 하는 것도 그 질문이다.

> **이 절이 남은 이유.** AGENTS.md의 「이 파일 밖의 것이 이렇게 동작한다고 적을 때는 무엇으로 확인했는지 같은 자리에 적어라」를 **내가 어겼다.** 재 보지 않고 「framer가 방금 들어왔으니 framer 탓」이라고 적었고, 그건 관찰이 아니라 짐작이었다. 이 저장소가 그 조항을 갖고 있는 이유가 정확히 이것이다 — 코드는 맞는데 설명만 틀리면 아무 신호가 없다.

## 영어 화면에 한국어가 셋 남아 있었다 — 2026-08-25

사용자가 2026-08-21에 지적한 셋 중 마지막(「상세 페이지 영어 지원이 완벽하게 전환되지 않는다. 일부 한글로 표시되고 있다」). 앞의 둘은 `375a58d`·`07e934e`로 끝나 있었다.

### 무엇이 남아 있었나

| 자리 | 영어 화면에 뜨던 것 | 서울 API 필드 |
| --- | --- | --- |
| 재난문자 배너 머리 (`AlertBanner`) | 「호우 주의보」 | `DST_SE_NM` + `EMRG_STEP_NM` |
| 사고통제 줄 (`AccidentList`) | 「교통사고 · 차대차」 | `ACDNT_TYPE` + `ACDNT_DTYPE` |
| 지하철 방향 (`SubwayArrivals`) | 「상행」·「하행」 | `SUB_DIR` |

**셋 다 자유 문장 바로 옆자리다.** 배너의 `MSG_CN`(재난문자 본문), 사고의 `ACDNT_INFO`, 지하철의 역 이름은 전부 **못 옮기는 것**이 맞다. 옆에 못 옮기는 것이 있으면 그 상자를 통째로 「못 옮기는 상자」로 분류하기 쉬운데, **갈래 이름은 값의 종류가 유한하고 뜻이 하나라 옮길 수 있다.** 판단 기준은 「사람이 그때그때 쓴 문장인가」이지 「어느 필드 옆에 있는가」가 아니다.

### 정적 검사 다섯이 전부 초록이었다

**샜던 값이 전부 런타임에 온다.** `i18n.test.ts`의 완결성 검사는 `t('...')`에 넘긴 **리터럴**을 소스에서 긁어 세고, 변수로 넘기는 키는 `dynamicKeys()`가 「쓴다」고 **선언**한 것만 본다. 선언에서 빠진 API 값은 다섯 검사 중 어느 것도 못 잡는다. 「섞임 방지」도 안 걸린다 — 코드에 한국어 리터럴이 없으니까.

**찾은 방법은 영어로 그려 놓고 DOM의 한글을 세는 것이었다.** 그걸 `src/i18n/detailLanguageSweep.test.tsx`로 남겼다. 탭 일곱을 각각 세고(탭 밖의 값은 DOM에 아예 없다), 텍스트뿐 아니라 `aria-label`·`title`·`alt`까지 본다.

**설계의 핵심은 허용 목록을 뒤집은 것이다.** 못 옮기는 자리를 목록으로 적어 빼면 그 목록이 낡고 진짜 결함까지 눈감아 준다. 대신 **목업의 그 자리들을 라틴 문자로 채웠다** — 그러면 남은 한글은 정의상 「옮겼어야 하는데 안 옮긴 것」뿐이라 허용 목록 없이 **0개**를 단언할 수 있다. 갈래 값은 한국어 그대로 둔다. 그게 이 검사가 보려는 것이다.

빈 화면을 보며 조용히 통과하지 않도록, 한국어 화면에서 같은 자리가 한글로 그려지는지를 같은 파일이 함께 센다.

### 「확인할 수 없어 미룬다」가 또 하나 풀렸다

`subway.ts`의 `상행`·`하행`은 **일부러** 한국어로 흘려보내던 것이고, 근거는 「`Upbound`/`Downbound`가 맞는 말일 가능성이 높지만 **실응답에서 이 값을 본 적이 없다**」였다.

**그 전제가 틀렸다.** `docs/fixtures/citydata-광화문덕수궁.json`(실호출)의 `SUB_DIR`에 둘 다 들어 있다. **2026-08-21 도로소통 톤과 같은 종류의 정정이다** — 그때도 「인증키가 나오면 실제 값을 확인하고 그때 정한다」던 자리였고 인증키는 이미 붙어 있었다. **「확인할 수 없어 미룬다」고 적은 자리는 확인이 된 뒤에 다시 읽어야 한다.**

여기서 갈라 둘 것: 「행선지 규칙(`BOUND_FOR`)에서 뺀다」와 「번역하지 않는다」는 **다른 결정**인데 한 곳(`NOT_A_DESTINATION`)이 둘을 함께 하고 있었다. 앞은 그대로 필요하다 — 안 빼면 「To 상」이라는 없는 말이 나온다.

### 값 목록이 없는 것은 그대로다

`docs/citydata-spec-raw.tsv`의 74·134·135·242·243행이 **필드 이름만 주고 값의 종류는 비어 있다.** 도로소통 지수와 같은 자리라 같은 방식으로 산다 — 아는 값만 사전에 넣고, **모르는 값은 `t()`가 키를 그대로 돌려주어 한국어로 남되 죽지 않는다.** 목록은 `en.ts`와 `i18n.test.ts` 두 곳에 손으로 있고, 새 값을 보거든 함께 더해야 한다.

넣은 값의 근거: `공사`·`도로보수`·`상행`·`하행`은 **실호출 응답**, `호우`·`주의보`·`교통사고`·`차대차`는 `src/data/mockCityInfo.ts`가 그리는 값. `경보`는 `주의보`의 짝이라 함께 넣었다 — 특보가 올라갈 때 하필 그 자리만 한국어로 남으면 가장 급한 순간에 안 읽힌다.

### 검증

`tsc -b --force` 0 · `eslint .` 0 · **103파일 1,276개 통과**(이전 102파일 1,263개).

**변이 7회 전부 죽었다.** `t()` 벗기기 셋(`AlertBanner`·`AccidentList`·`subway`)과 사전 항목 빼기 넷. 중요한 것은 **일곱 모두를 스윕 테스트가 잡았다는 것**이다 — 점 단위 테스트만 있으면 다음에 새는 자리는 또 못 잡는다.

**실기기·실화면 확인은 못 했다.** 재난문자와 사고통제는 실제로 그 일이 벌어져야 응답에 실리는 값이라 지금 서울 API를 불러도 빈 배열이 온다. 화면 증거는 목업으로 그린 것뿐이다.

## 다음에 할 일

### Google Maps 스타일 셸 개편 — **완료 (2026-08-10)**

11개 태스크 전부 구현했다. `feat/gmaps-style-shell`에서 작업했고 **지금은 `master`에 들어와 있다**(그 브랜치는 이미 없다).

| 문서 | 경로 |
| --- | --- |
| 설계 | `docs/superpowers/specs/2026-08-07-gmaps-style-shell-design.md` |
| 구현 계획 (11개 태스크) | `docs/superpowers/plans/2026-08-10-gmaps-style-shell.md` |

그 앞의 **지도 홈 개편(15개 태스크, 완료 2026-08-07)**은 이 브랜치의 조상에 들어 있다.

| 문서 | 경로 |
| --- | --- |
| 설계 | `docs/superpowers/specs/2026-08-07-map-home-redesign-design.md` |
| 구현 계획 (15개 태스크) | `docs/superpowers/plans/2026-08-07-map-home-redesign.md` |

### 다음 후보 (2026-08-18 기준, 우선순위 순)

**앞의 셋은 사람이 해야 열린다** — 위 「사람이 해야 하는 것」과 짝이다.

1. **운영용 Google Maps 키 발급 + 제한** — **출시 전 항목이지 화재가 아니다.** 지금 것은 데모 키다(제한이 없는 건 실측으로 확인했다). 코드는 다 돼 있고 콘솔 작업만 남았다 — 자세한 순서와 미확인 위험은 「사람이 해야 하는 것」 참고.
2. **활용갤러리 등록** — 이 한 건이 셋을 동시에 연다: 30곳→121곳, 1시간→5분, 도시정보 3시간 캐시 해제. **지금의 타협 대부분이 여기서 나왔다.**
3. **토스 실기기 30분** — **시트 드래그와 지도 팬이 충돌하면 핵심 조작이 깨져 있는 것이다.** 그러면 지금까지의 UI 작업이 의미가 없어진다. 폰트·안전영역·`h-dvh`·서비스워커가 토스 웹뷰에서 안 뜨는지도 같은 회차에 확인된다. **2026-08-21에 항목이 하나 늘었다: 전환 애니메이션**(framer-motion)이 웹뷰에서 매끄러운지 — 상세가 밀려 들어오는 것과 탭 패널 전환 둘 다 저사양 기기에서 프레임이 떨어지면 없느니만 못하다.

**코드로 할 수 있는 것:**

0. ~~**딥링크·공유 링크·뒤로 가기**~~ — **2026-08-18에 했다.** 위 「주소가 없어서 셋이 깨져 있었다」 절 참고. 남은 것은 `VITE_PUBLIC_APP_URL`을 배포 도메인으로 채우는 일뿐이다.

4. ~~**`Age` 헤더로 실제 경과 시각 적기**~~ — **2026-08-18에 했다.** 아래 「최대 3시간 전이라는 반쪽 거짓말」 절 참고.
5. **명소 30 → 121곳 확장** — 2번에 막혀 있다. 이름은 `src/data/official-areas.ts`에, **카테고리는 매뉴얼 PDF p9~10에 121곳 전부** 있다(고궁·문화유산 5 / 관광특구 7 / 공원 33 / 발달상권 28 / 인구밀집지역 48). 좌표와 `purposes` 태그만 채우면 된다.
6. ~~**언어지원(영어)**~~ — **2026-08-18에 했고, 2026-08-25에 새던 셋을 막았다.** 아래 「영어를 지원한다」 절과 위 「영어 화면에 한국어가 셋 남아 있었다」 절 참고. **「했다」고 적힌 뒤에도 사용자가 한국어를 봤다** — 정적 검사 다섯이 소스에 적힌 것만 세고 런타임 값을 못 세기 때문이다. 지금은 `detailLanguageSweep.test.tsx`가 영어로 그려 놓고 남은 한글을 0개로 잠근다.
7. ~~**Pretendard self-host 여부**~~ — **2026-08-12에 정했다: 웹폰트를 아예 안 쓴다.** 근거는 아래 「UI 감사」의 폰트 항목. 되살리자는 말이 나오면 「미리 서브셋을 못 뜬다」(API가 임의의 한글을 준다)부터 확인해라.
8. **CCTV 실시간 영상** — **2026-08-19에 출처를 찾아 붙였다.** 쿼터를 안 쓰고 iframe도 안 걸린다. 아래 「CCTV 출처를 찾았다」 절이 정본이다.

**전략 메모:** 데이터 밀도로는 인파레이더를 못 이긴다(121곳·실시간·한도 없음 대 30곳·1시간·1,000회). 우리가 이길 수 있는 것은 **토스 안이라 설치가 없다**는 점과 **표 대신 판단을 대신해 준다**는 점이다 — 「근처 쾌적한 장소」·「평소 대비」·요일×시간 패턴은 그쪽에 없다.

### 개편으로 해소된 것

- ~~**요일×시간 패턴이 없던 것**~~ — 2026-08-12에 상세에 붙였다(7요일 × 3시간 8칸). **서울 API에 과거 조회가 없어** 쌓아서 만든다 — 상세를 열 때마다 이 기기에 한 칸씩. 추가 호출 0. 관측이 없는 칸은 「여유」가 아니라 「관측 없음」이고, 색만으로 값을 전하지 않도록 진짜 `<table>`에 칸마다 `sr-only`로 값을 적는다. 서버 수집으로 갈아끼울 자리는 `useWeekPattern` 하나다 — PLAN.md 4차.

- ~~**대체 데이터와 실측을 똑같이 보여주던 것**~~ — 2026-08-11에 `REPLACE_YN`을 읽는다. 명소 상세의 갱신 시각 바로 아래에 「이 수치는 실측이 아니라 대체값이에요」가 붙는다. **세 상태다**(대체값/실측/모름) — 모름을 실측으로 접지 않는다. 스키마에서 `z.unknown().optional()`로 열어둔 것이 핵심인데, `z.string()`으로 조이면 이 필드가 숫자로 오는 날 혼잡도까지 통째로 날아간다. 수치 자체는 숨기지 않는다 — 서울 API가 주는 최선의 추정이고 감추면 화면에 남는 게 없다.

- ~~**도로소통·사고통제가 빠져 있던 것**~~ — 2026-08-11에 붙였다. 명소 상세의 도시 정보에 「도로소통」 섹션이 생겼고 사고통제 목록이 같은 섹션 안에 들어간다. **추가 호출 0** — 이미 받고 있는 `citydata` 응답에서 `ROAD_TRAFFIC_STTS`·`ACDNT_CNTRL_STTS`를 읽을 뿐이다. `hasAnyCityInfo`에도 두 항을 함께 더했다(빠뜨리면 도로소통만 있는 명소가 「정보 없음」으로 뜨면서 화면에는 섹션이 그려진다).

- ~~**더보기 TTL과 쿼터 배분**~~ — **2026-08-14에 다시 정했다.** 접이식을 없애고 상세를 열면 자동 조회로 바꾸는 대신, 상세의 중복 혼잡도 호출(720회/일)을 없애고 `CITYINFO_CACHE_TTL_SECONDS` 기본값을 3시간으로 올렸다. 합계 960/1,000회다 — 아래 「쿼터 배분」 표가 정본이다.
- ~~**지도 카메라가 상세를 다녀오면 초기화된다**~~ — 화면이 하나가 되면서 지도가 언마운트될 자리 자체가 없어졌다. 상세는 시트 안에서만 갈리고 지도는 뒤에 깔린 채 그대로다. (`App.tsx`의 `hidden` 트릭도 함께 사라졌다 — 감출 형제가 없다.)
- ~~**`AreaSheet` 진입 시 자동 노출**~~ — 그 시트는 2026-08-07에 없어졌다. **지금 시트는 다른 것이다** — 아래 「바텀시트가 다시 생겼다」 참고.
- ~~**`useFavorites`가 인스턴스마다 따로 놀던 것**~~ — `hooks/favoritesStore.ts`가 모듈에 값을 한 벌만 들고 `useSyncExternalStore`로 잇는다. 이전에는 홈의 칩과 상세의 별이 서로를 못 봤다. 인스턴스별 `touchedRef` 가드도 스토어의 `ensureLoaded` 하나로 합쳐졌다.
- ~~**`transition-[height]`가 스크롤 위치를 버린다**~~ — 계획서의 걱정이었으나 헤드리스 크롬 실측으로 거짓임을 확인했다. full(scrollTop 300) → peek → full 왕복에서 300이 그대로 남는다. 줄어드는 쪽은 `clientHeight`가 작아져 최대 스크롤이 오히려 커진다.
- ~~**전체(92%)에서 손잡이가 오버레이 밑에 깔린다**~~ — 전체에서는 검색 바·칩 열을 아예 렌더하지 않기로 정해 해소됐다. 개발 서버를 헤드리스 크롬으로 띄워 `elementFromPoint`로 확인했다: 전체에서 손잡이 위쪽 띠의 왼쪽·가운데 모두 손잡이가 잡힌다.
- ~~**뷰가 갈릴 때 포커스가 `document.body`로 떨어진다**~~ — 시트 내용을 `tabIndex={-1}` 상자로 감싸고, **옮겨 달라고 말한 조작에서만** `focus({ preventScroll: true })`를 부른다(첫 렌더에서는 뺏지 않는다). 「뷰가 달라졌는가」로 판단하면 타이핑의 부수 효과까지 잡아 첫 글자만 입력되는 버그가 난다. 요청 신호는 **ref가 아니라 카운터 상태**여야 한다 — ref는 렌더를 일으키지 않아, 같은 명소를 다시 눌러 React가 렌더를 건너뛰면 신호가 굳어 있다가 나중에 엉뚱한 렌더에서 포커스를 훔친다.

- ~~**뷰가 갈려도 시트 스크롤이 앞 뷰 자리에 남던 것**~~ — 목록·상세·오늘의 서울이 스크롤 컨테이너 **하나를 나눠 쓰는데** 아무도 그 주인이 아니었다. 뷰를 옮길 때 `data-sheet-content`를 찾아 `scrollTop`을 0으로 되돌린다. 실측(390×844): 고치기 전에는 목록을 200 내리고 명소를 열면 상세의 「목록으로」가 `top −47.5`로 화면 밖이었고, 상세에서 돌아오면 요약 스트립이 잘려 **「오늘의 서울」로 가는 유일한 통로가 사라진 것처럼** 보였다. 고친 뒤 각각 `top 91.5` / `top 479.8`. **jsdom에는 레이아웃이 없어 「잘렸다」는 사실 자체는 못 잡는다** — 잡는 것은 `scrollTop = 0` 대입이고(jsdom이 대입값을 보존한다) 그게 처방이다. **확인법:** 목록을 스크롤한 뒤 명소를 열어 「목록으로」가 시트 맨 위에 보이는지 본다.
- ~~**별 아이콘의 채움 상태가 테스트로 안 덮인다**~~ — 상세의 별 토글이 라벨 있는 「저장/저장됨」 버튼(`aria-pressed`)이 되면서 사라졌다. 목록 행에 남은 별은 토글이 아니라 즐겨찾기일 때만 그려지는 표시다.

### 사람이 해야 하는 것 (코드로 못 푼다)

**인증키는 이미 있다(2026-08-13, 커밋 `70abbad`).** 실호출을 했고 응답 한 벌이 `docs/fixtures/citydata-광화문덕수궁.json`에 있다. 「아직 실데이터를 본 적 없다」고 적힌 옛 문장이 여기저기 남아 있을 수 있으니 **믿지 말고 이 절을 기준으로 삼아라.**

| # | 할 일 | 상태 |
| --- | --- | --- |
| 1 | data.seoul.go.kr 회원가입 | **완료** |
| 2 | 일반 인증키 발급 | **완료** (2026-08-13) |
| 3 | **활용갤러리에 앱 등록** | **미완** — 하루 1,000회 해제. 아래 참고 |
| 4 | Vercel 계정·프로젝트 연결 | 완료(프록시 배포 주소는 「현재 상태」에) |
| 5 | 토스 실기기 | **미완** — 아래 "실기기로만 확인되는 것" |
| 6 | Google Cloud 키 제한·예산 | **미완** — 아래 |

**3번이 지금 가장 값이 크다.** 한도가 풀리면 이 한 건으로 셋이 동시에 열린다 — **30곳 → 121곳**, **1시간 → 5분 갱신**, 그리고 도시정보 3시간 캐시(지금 「최대 3시간 전 기준」이라 적어 둔 자리)가 필요 없어진다. 지금의 타협 대부분이 이 한도에서 나왔다.

**6번은 출시 전 항목이지 화재가 아니다. 지금 쓰는 키가 데모 키다**(2026-08-18 사용자 확인). 아래 순서를 지킬 것.

**실측으로 확인한 사실(2026-08-18): 이 키에는 리퍼러·API 제한이 없다.** 문서를 보고 짐작한 것이 아니라 직접 쟀다 — 허용 목록에 있을 리 없는 호스트명(`--host-resolver-rules`로 매핑한 `definitely-not-our-domain.invalid`)에서 이 키로 지도를 띄웠더니 **타일까지 정상 렌더**되고 `gm_authFailure`도 안 불렸다. Geocoding으로 부르면 「이 API를 쓸 권한이 없다」가 아니라 다른 사유로 거절되므로 API 제한도 없다. **같은 방법으로 언제든 다시 잴 수 있다.**

**그래도 노출 피해는 제한적이다.** 데모 키이고, 저장소에 박혀 있지도 않다 — 커밋 이력 전체(`git log --all -S`)와 추적 파일 어디에도 없고 `.env`는 무시된다. 번들(`dist/`)에는 들어가지만 그건 의도된 동작이다.

> **여기 「누구나 긁어서 우리 결제로 쓸 수 있다」고 적혀 있었다.** 키가 무제한인 것은 사실이지만 청구로 이어지는지는 결제 연결 상태에 달렸고, 그건 콘솔 밖에서 확정할 수 없다. **재지 않은 것을 단정하지 마라** — 이 파일이 같은 이유로 이미 한 번 사람을 오도했다(위 「한 줄 요약」).

**출시 전에 할 일 (순서대로):**

1. **운영용 키를 새로 발급한다.** 지금 데모 키를 제한만 걸어 승격시키지 마라 — 데모용으로 여기저기 쓰였을 수 있고, 어디에 남았는지 추적할 수 없다.
2. 그 키에 **처음부터** 제한을 걸고 나서 `.env`에 넣는다:
   - **HTTP 리퍼러** — `https://seoul-live.web.tossmini.com/*`, `https://seoul-live.private-web.tossmini.com/*`, `https://seoul-live-now.vercel.app/*`, `http://localhost:*/*`
     («`.env.example`에는 Vercel 주소가 빠져 있다 — 그대로 넣으면 웹에서 지도가 죽는다»)
   - **API 제한** — Maps JavaScript API 하나만
   - **일일 할당량 상한** — **실제로 지출을 막는 건 이것뿐이다.** 예산 알림은 알려줄 뿐 끊지 않는다
   - 결제 예산 알림 (보조)
3. `DEMO_MAP_ID` → 자체 Map ID. 없으면 마커가 아예 렌더되지 않는다. Map ID는 키와 별개라 **함께 바꿔야 한다.**

**리퍼러 제한에는 미확인 위험이 하나 있다.** 토스 웹뷰가 `Referer`를 보내는지 모른다 — 안 보내면 제한이 그 요청을 막아 **미니앱에서만 지도가 안 뜬다**(웹은 멀쩡해서 눈치채기 어렵다). 그래서 순서는 **API 제한·할당량을 먼저, 리퍼러를 나중에** 걸고 실기기로 확인하는 쪽이다. 안 되면 리퍼러를 풀고 할당량을 조인다.

### 실호출로 풀린 것 / 아직 안 풀린 것

**한 명소(광화문·덕수궁, 강남역)만 호출했다.** 그래서 「값의 종류」를 묻는 항목은 표본이 하나뿐이라 여전히 안 풀렸다.

| 항목 | 상태 |
| --- | --- |
| `AREA_CD` 대조 | **풀림** — `POI009` 일치 |
| `citydata` 응답의 실제 키 | **풀림** — 픽스처로 고정, `citydataFixture.test.ts`가 통과시킨다 |
| 인구 구성 필드 형태 | **풀림** — `"50.0"` 소수 한 자리 문자열 |
| `REPLACE_YN` | **풀림** — `"N"`. `'Y'`/`'N'` 가정이 맞았다 |
| 주차장·따릉이 좌표 | **풀림** — 주차장 `LAT`/`LNG`(문자열), 따릉이 `SBIKE_X`=경도/`SBIKE_Y`=위도(숫자) |
| **명소 이름의 공백** | **미해결** — 30곳을 다 불러야 한다. `광장(전통)시장`처럼 괄호가 든 이름이 위험하다. `areas.test.ts`의 `it.todo`가 이 자리다 |
| **`ROAD_TRAFFIC_IDX` 값의 종류** | **미해결** — `"정체"` 한 표본뿐이라 톤을 안 붙였다 |

## 현재 상태

### 진행률 — 20개 태스크 전부 완료

| # | 태스크 | 상태 |
| --- | --- | --- |
| T1~T12 | 환경·도메인·데이터·프록시 계층 | 완료 |
| T13 | 위치 훅 | 완료 |
| T14 | 내 주변 목록 로직 | 완료 |
| T15~T17 | 공통·레이아웃 컴포넌트, 내 주변 화면 | 완료 |
| T18 | 혼잡예보 화면 | 완료 |
| T19 | 앱 조립 | 완료 |
| T20 | 커버리지·문서 | 완료 |

이후 시안 반영과 명세 대조, 지도·프리셋·도시정보 화면으로 추가 작업이 더 들어갔고, 개편이 두 번 더 있었다(지도 홈 15태스크 / 셸 11태스크).

### 검증 수치 (2026-08-13 실측)

테스트 **812개 + todo 1개** 통과 (테스트 파일 68개).
커버리지 — 구문 96.89% / 브랜치 91.52% / 함수 95.44% / 라인 97.30% (임계값 라인·구문·함수 80%, 브랜치 75%).
`npx tsc -b --force`·`npm run lint`·`npm run build` 전부 통과. 앱인토스 패키징(`seoul-live.ait`)까지 나온다.
번들 — JS 476.22 kB (gzip 144.73) / CSS 29.22 kB (gzip 6.24). 모듈 273개.

셸 개편 직후(617개, 브랜치 88.12%) → 새 기능 셋 뒤 703개 → UI 감사 뒤 732개 → **detail_page.png 대조 뒤 812개**. 늘어난 80개는 위치 폴백과 상세 넷에서 나왔고 파일 셋이 새로 생겼다(`platform/location.test.ts`·`HourlyWeather.test.tsx`·`SubwayArrivals.test.tsx`).

**아직 todo가 하나 있다** — `areas.test.ts`의 「인증키 발급 후 30곳을 실제 호출해 공백까지 일치하는지 확인한다」. **인증키는 이제 있다(2026-08-13).** 안 돈 이유는 다른 것이다: 30곳을 다 부르면 하루 한도의 3%가 한 번에 나가고, 테스트가 망에 나가면 CI에서 깨진다. **스크립트로 한 번 돌려 결과를 카탈로그에 반영하는 것이 맞다** — 테스트에 남길 일이 아니다.

**CSS가 26.43 → 29.08 kB로 늘었다** — 토큰 18개와 포커스·reduced-motion 규칙이 들어갔다. 대신 **Google Fonts 요청(229KB woff2 + 렌더 블로킹 CSS)이 사라졌으므로 실제 전송량은 크게 줄었다.**

**변이로 확인한 것 — 이번 세 기능에서 39건 중 39건 사망.** 처음 살아남은 여섯 중 다섯이 **테스트 구멍**이었고 하나만 구현 결함이었다(저장 파서가 칸 키를 검증하지 않던 것). 동치 변이 하나는 그렇다고 코드에 적어 뒀다(`cityInfoSchema.ts`의 `roadRows[0] ?? {}`).

### 파일 구조

```text
src/domain/     types, cityInfo, composition, congestion, distance, forecast, map,
                mapLinks, pattern, presets, search, sheet, summary
                                                                순수 함수. React도 네트워크도 모른다
src/data/       areas, official-areas, schema, cityInfoSchema, compositionSchema,
                mock, mockCityInfo, client, queries
src/platform/   links, googleMaps, favorites, weekPattern       토스 브리지 + 폴백, Google Maps SDK 경계
src/hooks/      favoritesStore(모듈 단일 출처), useFavorites, useCurrentLocation,
                useNearbyAreas, useHomeFilters, useCachedCityAlerts, useWeekPattern
src/app/        QueryProvider, LocationProvider, locationContext
src/tokens.test.ts                                              정본(DESIGN.md)과 index.css의 색 정합 + 대비를 잰다
src/components/ common(Icon, CongestionBadge, ToneBadge, toneClass, ErrorState,
                       SkeletonCard = SkeletonList(카드) + SkeletonRows(목록 행))
                home(BottomSheet, SearchBar, FilterChips, SummaryStrip,
                     CongestionCard, PopulationCard, ActionButtons,
                     NearbyCalmSection, WeeklyPatternCard)
                list(AreaList, AreaListItem, CategoryFilter, LocationNotice)
                map(CongestionMarker, MapUnavailableNotice, MapFabStack)
                forecast(ForecastChart)
                cityinfo(AlertBanner, WeatherCard, ParkingList, BikeList, EventList,
                         InfoSection, RoadTrafficCard, AccidentList)
                today(SummaryCard, RankList, CategoryAverages, AlertDigest, RecommendationCard)
src/screens/    HomeScreen(화면 전체), TodayScreen(시트 안 뷰)
api/            _lib/(seoul, seoulRtd, allowed-areas, http), cityinfo, hotspots, cctv, ppltn
```

**이 트리는 완전한 목록이 아니다.** 2026-08-13 셸 개편 직후의 스냅샷에서 죽은 이름 몇 개만 걷어낸 것이다 — 그 뒤 두 주 동안 명소 상세가 탭 일곱 개(`src/components/detail/`, 이 트리에 아예 없다)로 갈리고 도메인·데이터·cityinfo 쪽에 파일이 각각 스무 개 안팎 더 생겼다. 여기 남은 이름들은 **여전히 존재한다는 것만** 확인했다 — 무엇이 어디 있는지 찾으려면 `ls src/domain` 같은 명령을 직접 돌려라. 전체를 다시 세는 것은 이 손질의 범위 밖이다.

**`src/components/layout/`은 없다.** `BottomTabBar`가 Task 10에서, `TopAppBar`가 같은 태스크에서 사라져 디렉터리가 통째로 비었다. 여기에 헤더·탭바를 다시 만들지 마라 — 근거는 아래 「셸은 `<main className="h-dvh">` 하나다」.

**`src/screens/`는 둘이다.** `TodayScreen`은 독립 화면이 아니라 시트 안 뷰지만, `useAreaCongestion`으로 스스로 데이터를 가져와 컴포넌트에 내려주므로 레이어상 screens에 속한다. `HomeScreen`이 `<TodayScreen onSelectArea onBack />`으로 시트에 끼워 넣는다.

`npm run dev` → <http://localhost:5173/> 에서 실제로 뜬다. 라우터도 탭 상태도 없다 — `App.tsx`는 Provider 둘과 `<main className="h-dvh">` 한 겹뿐이고, 그 안의 유일한 자식이 `HomeScreen`이다.

**셸은 `<main className="h-dvh">` 하나다.** `HomeScreen` 루트가 `size-full`(= `height: 100%`)이라 높이가 auto인 부모를 만나면 **지도가 0px로 접힌다.** `h-dvh`를 빼거나 그 위에 높이를 안 정해주는 래퍼를 끼우지 마라.

**눈에 보이는 제목은 없고 `sr-only` h1 「서울 라이브」만 있다.** 없애면 제목 층이 시트 안 h2부터 시작해 제목으로 훑는 스크린리더 사용자에게 뿌리 없는 트리가 된다. `sr-only`라 세로 공간은 한 픽셀도 안 쓴다.

**시트 안 뷰가 갈릴 때 포커스를 옮기는 것은 상태(`focusRequest` 카운터)다, ref가 아니다.** ref로 신호를 들면 「요청했는데 렌더가 안 일어나는」 경우에 굳어 있다가 나중에 엉뚱한 렌더에서 터진다 — 실제로 그랬다. 불리언도 같은 병을 앓는다(이미 참인데 참을 넣으면 React가 건너뛴다). 0이 「아직 아무도 요청하지 않았다」이고 첫 렌더 침묵이 거기서 나온다.

### 설계 문서

작업 전에 해당 문서를 읽는다. 결정의 근거가 여기 있고, 같은 논쟁을 되풀이하지 않으려고 남긴 것이다.

| 기능 | 설계 | 계획 |
| --- | --- | --- |
| 1차 (내 주변·혼잡예보) | `docs/superpowers/specs/2026-08-03-seoul-live-design.md` | `docs/superpowers/plans/2026-08-03-seoul-live-1차.md` |
| 지도 화면 | `docs/superpowers/specs/2026-08-04-map-tab-design.md` | `docs/superpowers/plans/2026-08-05-map-tab.md` |
| 목적 프리셋 | `docs/superpowers/specs/2026-08-06-purpose-presets-design.md` | `docs/superpowers/plans/2026-08-06-purpose-presets.md` |
| 도시 정보 | 없음 — 사용자가 문서 단계를 생략하고 바로 구현하기로 정했다(2026-08-07) | 없음 |
| 지도 홈 개편 (완료) | `docs/superpowers/specs/2026-08-07-map-home-redesign-design.md` | `docs/superpowers/plans/2026-08-07-map-home-redesign.md` |
| **Google Maps 스타일 셸 개편 (완료)** | `docs/superpowers/specs/2026-08-07-gmaps-style-shell-design.md` | `docs/superpowers/plans/2026-08-10-gmaps-style-shell.md` |

**도시 정보에는 설계 문서가 없다.** 사용자가 속도를 위해 spec/plan 문서 단계를 건너뛰기로 결정했다. 결정의 근거는 코드 주석에 남겼다 — `cityInfoSchema.ts` 머리말(왜 관대한 파싱인가), `seoul.ts`의 `cityInfoCacheTtlSeconds`(쿼터 배분), `BikeList.tsx`(왜 주차장 톤을 재사용하지 않는가). 「왜 네이티브 select인가」를 담고 있던 `AreaPicker.tsx`는 지도 홈 개편 때 사라졌다 — 상세가 이미 명소가 정해진 자리라 고르는 단계 자체가 없어졌다.

**셸 개편 계획서는 실행 중에 여러 번 정정됐다.** 각 태스크의 「실측」·「진행하며 잡은 문제」 대목이 그 기록이고, 이 STATE의 미해결·해소 목록이 거기서 나왔다. 계획서의 숫자를 인용할 일이 있으면 뒤쪽 태스크의 정정을 먼저 확인해라.

### 정리한 것 (2026-08-07)

오래 떠 있던 작업 트리 변경을 커밋했다. 되살릴 일이 생기면 삭제 직전 커밋에서 꺼낼 수 있다.

| 대상 | 처리 |
| --- | --- |
| `.grok/`·`.hermes/`·`.kiro/`·`.windsurf/` | 삭제. `.claude/skills/apps-in-toss/`의 사본이었다 — 정본 한 벌만 남긴다 |
| `OPEN_API/` | 삭제. 서울 열린데이터광장 샘플 코드(Java·JSP)였고 우리 구현과 무관하다. AGENTS.md의 참고 문서 표에서도 뺐다 |
| `.gitignore` | `.gstack/` 추가 |
| `.claude/skills/apple-design/` | 추가 |

## 알아둬야 할 동작

### 전체 스위트가 무작위로 실패하면 부하를 먼저 본다

기본 `testTimeout`이 5초다. 기계가 포화되면 아무 테스트나 걸리고, 실패가 회차마다 **서로 무관한 파일**로 옮겨 다닌다(`useCurrentLocation` → `App` → `BikeList`처럼). 실패 원문은 `Test timed out in 5000ms`다.

더 헷갈리는 형태도 있다 — 워커가 죽으면 **수집이 덜 돼서** 「62파일/704개」가 「57파일/653개」로 찍힌다. 테스트가 사라진 것처럼 보이지만 `git status`·`HEAD`·파일은 멀쩡하다.

**판정은 `npx vitest run --maxWorkers=4`로 한다.** 2026-08-12에 load average 16.9(16코어)에서 겪었고, 워커를 4로 묶으니 3회 연속 깨끗했다.

### npm 명령은 파이프 없이 돌린다

`| tail`로 파이프하면 종료 코드가 tail의 것으로 잡혀 **실패가 성공으로 보인다.** npm 명령은 파이프 없이 돌린다.

### 로컬 `.env` 상태

`.env`는 gitignore 대상이라 저장소에 없다. **현재 로컬 파일에는 `VITE_GOOGLE_MAPS_API_KEY`가 채워져 있어서 `npm run dev`를 돌리면 지도가 실제로 뜬다.** 사용자가 직접 붙여넣은 데모 키다 — 값을 읽어 로그·문서·커밋에 남기지 마라.

**2026-08-07에 이 키가 `.env.example`(추적 대상)에 들어가 있는 것을 발견했다.** `.env`는 사라져 있었다. 커밋·히스토리에는 들어가지 않았음을 `git log -S`로 확인했고, `.env`를 복구한 뒤 `.env.example`을 되돌렸다. 원인은 불명이다 — 세션 밖에서 파일이 정리된 정황이다. **`.env.example`을 고칠 때 실제 키가 섞이지 않았는지 커밋 전에 확인할 것.**

`VITE_GOOGLE_MAPS_MAP_ID`는 비어 있고, 그 경우 `googleMaps.ts`가 `DEMO_MAP_ID`로 떨어진다. 출시 전 교체 대상이다.

**`.env`는 전체를 덮어쓰지 말고 Edit/append로만 고친다.** git이 추적하지 않아서 잘못 쓰면 복구가 안 된다.

`SEOUL_API_KEY`·`CACHE_TTL_SECONDS`·`CITYINFO_CACHE_TTL_SECONDS`는 서버 전용이라 `.env`에 없다. Vercel 프로젝트 환경변수에 넣는다.

`VITE_MOCK_FAIL_AREAS`는 목업 모드에서 특정 명소의 조회 실패를 흉내 낸다. 혼잡도와 도시정보 **양쪽에** 적용된다 — 도시 정보의 에러 상태도 이걸로 볼 수 있다.

### 위치 권한이 없어도 화면은 선다

좌표가 없으면 거리순 대신 **혼잡도순**으로 내려가고 상단에 허용 안내가 뜬다. 거부(`denied`)와 그 외 실패(`unavailable`)는 문구가 다르다 — 전자는 사용자가 풀 수 있고 후자는 아니다.

권한 다이얼로그 재요청은 사용자가 「허용하기」를 누를 때만 연다. 화면에 들어오자마자 팝업이 연달아 뜨는 걸 막는다.

위치는 `LocationProvider`가 앱 수준에서 **한 번만** 잡는다. 화면 안에 두면 상세에서 뒤로 갈 때마다 GPS를 다시 켠다.

**브라우저에서 「내 주변」이 내내 죽어 있었다 (2026-08-13에 고침).** T13~T17이 「완료」로 적혀 있었지만 `useCurrentLocation`이 토스 `Device.getLocation`을 **폴백 없이** 직접 불렀다. `favorites`·`links`·`weekPattern`은 셋 다 브리지 실패 시 웹 표준으로 떨어지는데 위치 훅만 그러지 않았다. 그래서 토스 앱 밖(개발 서버·Vercel 프리뷰·모바일 브라우저)에서는 항상 `unavailable`이었고, 화면은 「위치를 확인할 수 없어 혼잡도 낮은 순으로 보여드려요」를 띄우고 「내 주변」 FAB은 좌표가 `null`이라 `disabled`였다. **기능이 없는 게 아니라 확인할 방법이 없었다.**

폴백은 `src/platform/location.ts`로 옮겼다. 브리지 우선, 실패하면 `navigator.geolocation`. **거부는 떨어뜨리지 않는다** — 거부는 브리지가 살아 있다는 신호라 웹으로 넘기면 토스에서 이미 거부한 사람에게 웹뷰 팝업을 한 번 더 띄우게 된다. 그래서 「브리지가 없다」와 「사용자가 거부했다」를 갈라 잡고, 웹 쪽 `PERMISSION_DENIED`(코드 1)도 같은 `LocationDeniedError`로 모은다. 훅에는 상태 기계만 남았다.

**아직 실기기에서 재지 않았다.** 브라우저에서 좌표가 들어오는 것과 토스 웹뷰에서 브리지가 그대로 도는 것은 다른 확인이다 — 후자는 회귀가 없어야 정상이지만(브리지 경로를 안 건드렸다) 실기기에서 한 번 봐야 한다.

### 길찾기가 셋이 됐다 (2026-08-13)

detail_page.png의 「티맵 안내」를 붙여 카카오맵·네이버·티맵 셋이 한 줄에 선다.

**티맵만 https가 아니다.** 카카오·네이버는 웹 URL이라 브라우저에서도 열리지만 티맵은 `tmap://route?goalname=&goalx=&goaly=` 커스텀 스킴이라 토스 앱의 `Device.openURL`을 거쳐야 뜬다. **개발 서버에서 눌러도 아무 일이 없는 것이 정상이다.** 티맵은 공식 문서에 URL scheme 안내 자체가 없어 커뮤니티에서 검증된 형태를 따랐다 — `rGoName`/`rGoX` 계열은 안드로이드에서 좌표가 무시된다는 보고가 있어 쓰지 않았다. **확인법:** 실기기 토스 앱에서 눌러 목적지가 그 명소로 채워진 채 티맵이 뜨는지 본다. 안 되면 `mapLinks.ts`의 이 함수만 고치면 된다.

`goalx`가 경도, `goaly`가 위도다. 뒤집으면 앱이 아무 불평 없이 바다 한가운데로 안내한다 — `mapLinks.test.ts`가 이 순서를 잠근다.

**라벨에서 「길찾기」를 뺐다.** 셋이 한 줄에 서면서 320px에서 버튼 하나에 배정되는 폭이 138px → 87px로 줄어 「카카오맵 길찾기」가 안 들어간다. 헤드리스 크롬 실측(320/360/390px): 배정 87/101/111px, 필요 75(카카오맵)·63(네이버)·53(티맵)px, 셋 다 같은 `top`이고 높이가 48px 그대로라 줄바꿈이 없다. **여유가 12px뿐이니 라벨을 늘릴 때는 다시 재라.**

### 바텀시트가 다시 생겼다 (2026-08-10)

2026-08-07 개편으로 없앴던 시트가 셸 개편으로 돌아왔다. **다른 물건이다.** 옛 `AreaSheet`는 지도를 덮고 나타났다 사라지는 모달이었고, 지금 시트는 **화면 자체의 일부**라 언제나 거기 있다.

앱인토스 심사 항목("미니앱에 진입하자마자 바텀시트가 자동으로 나타나지 않아요")은 여전히 유효하다. 우리 시트는 절반(56%)으로 시작하는데, 이것이 「자동으로 나타난 것」이 아니라고 읽은 근거는 나타나고 사라지는 물건이 아니라는 점이다 — 전체로도 열지 않는다. **심사에서 지적되면 대안은 살짝 열림(16%)으로 시작하는 것이다.** `HomeScreen`의 `useState<Detent>('half')` 한 곳만 고치면 된다.

| 단계 | 비율 | 무엇이 보이나 |
| --- | --- | --- |
| `peek` | 16% | 요약 스트립과 목록 첫 항목. 지도가 주인공 |
| `half` | 56% | 목록. 기본값. **2026-08-12에 46%에서 올렸다** — 46%에서는 크롬(손잡이·요약·위치안내·카테고리·정렬)이 284px을 먹어 목록이 1.8행뿐이었다 |
| `full` | 92% | 오늘의 서울. 손잡이로도 간다. **상세는 여기 안 온다** (아래) |

0%와 100%를 쓰지 않는 이유: 한쪽이 완전히 사라지면 되돌릴 손잡이도 같이 사라진다. 정본은 `domain/sheet.ts`의 `SHEET_RATIO`다.

**드래그는 손잡이에서만 받는다.** 내용 영역에서도 받으면 전체 단계에서 상세를 스크롤할 때마다 시트가 따라 내려간다. 손잡이는 끌 수도 있고 누를 수도 있다 — 누르면 `peek → half → full → peek`으로 굴러간다(한쪽 끝에서 멈추면 그 단계가 막다른 골목이 된다).

**손잡이 히트 영역이 시트 루트 위로 20px 나가 있다.** 보이는 띠는 4px인데 손에 닿는 상자는 44px(WCAG 2.5.8)이고, 늘어난 20px을 위쪽에 몰아줬다 — 아래로 늘리면 내용 래퍼가 hit test에서 이겨 닿지 않고, 손잡이 위가 지도라 빗나간 터치는 위로 벗어나기 때문이다. **시트 루트에 `overflow-hidden`을 걸지 마라** — 그 20px이 조용히 잘려 히트 영역이 24px로 돌아가고, 테스트도 못 잡는다.

**`pointercancel`은 확정이 아니라 「없던 일」이다.** `pointerup`과 같은 핸들러를 물리면 취소가 곧 확정이 되고, cancel의 `clientY`는 구현에 따라 0으로도 와서 비율이 1.0 → 전체로 튄다. 손잡이 위아래가 지도라 웹뷰가 제스처를 가져가며 cancel을 쏘는 것은 상시 경로다.

**지도를 못 쓰면 시트를 절반에 묶는다.** `sheetDetent = mapReady ? detent : 'half'`가 그 규칙이 사는 유일한 자리다. 오버레이·FAB·시트가 모두 이 값을 보므로 규칙을 여러 곳에 흩지 마라.

### 시트가 손끝을 따라온다 (2026-08-13)

「하단 리스트가 고정이라 자유롭지 않다」는 지적을 받아 고쳤다. **예전에는 끄는 동안 높이가 변하지 않고 손을 뗄 때 한 번에 단계로 붙었다** — 시트가 손에 붙어 있지 않으니 잠긴 것처럼 느껴진다.

이제 `BottomSheet`가 끄는 동안만 제 높이(`dragRatio`)를 들고, 놓거나 취소하면 버리고 부모의 `detent`로 돌아간다. 두 상태가 동시에 사는 구간이 제스처 하나뿐이라 부모와 갈릴 여지가 없다. 끄는 동안에는 `transition-[height]`를 뗀다 — 켜 두면 손끝보다 200ms 늦게 따라와 고무줄처럼 늘어진다.

### 상세는 시트를 올리지 않는다 (2026-08-13)

「목록 누르면 시트가 올라가면 안 된다, 지도는 계속 보여야 한다」는 지적을 받아 고쳤다. `openArea`가 `setDetent('full')`을 불러, 목록에서 한 곳을 누르면 시트가 92%로 튀어올라 **지도가 통째로 사라졌다.** 지도 위에 시트를 얹은 이 셸의 전제가 명소를 누를 때마다 무너지고 있었다.

**`openArea`는 언제나 `setDetent('half')`다.** 「올리지 않는다」(현재 단계 유지)가 아닌 이유는 반대쪽 끝 때문이다 — full로 펼쳐 둔 채 누르면 상세가 full에 앉아 지도가 여전히 안 보이고, peek에서 마커를 누르면 상세가 135px 틈에 갇힌다. 어디서 열든 같은 화면이 되어야 한다는 `openArea`의 존재 이유가 단계에도 적용된다. 「목록으로」(`onBack`)와 같은 값이라 여닫는 동안 시트가 아예 안 움직인다.

딸려온 변화: **상세에서도 검색 바·필터 칩·「내 주변」이 그대로 보인다.** 그 셋은 `sheetDetent !== 'full'`일 때만 그려지는데(full에서 손잡이를 덮으므로) 상세가 half가 되면서 함께 살아남았다.

**지도가 고른 명소로 따라간다.** 서울 인파레이더가 그렇게 한다. 상세가 half에서 열려 지도가 계속 보이게 된 이상, 안 따라가면 상세는 경복궁을 말하는데 지도는 서울 전역인 채로 남는다. `openArea`가 `centerBelowSheet`로 중심을 잡고 줌을 `AREA_ZOOM`(15)로 당긴다.

**중심은 명소 좌표가 아니다 — 남쪽으로 비켜 잡는다.** 지도가 뷰포트를 꽉 채우고 시트가 그 위를 덮으므로 지도의 중심은 언제나 화면 한가운데(390×844에서 y=422)인데 half 시트의 상단이 y=371이다. 명소를 그냥 중심에 놓으면 **시트 뒤로 들어가 하나도 안 보인다.** `domain/map.ts`의 `centerBelowSheet`가 화면 높이·줌·시트 비율로 보정한다(메르카토르라 `cos(위도)`가 들어간다 — 빼면 서울에서 21% 어긋난다). 헤드리스 크롬 390×844 실측: 경복궁 핀 바닥 y=185, 보이는 띠 한가운데 y=186 — 1px 오차.

### 시트가 움직이면 지도도 움직인다 (2026-08-13)

「서울 인파레이더는 지도도 반응형인데 우리는 시트를 움직여도 지도가 고정」이라는 지적을 받아 고쳤다. 시트가 커지면 보이는 띠가 위로 줄어드는데 지도가 가만히 있으면, 바로 위에서 만든 「고른 명소가 띠 한가운데」가 도로 시트 뒤로 밀려 들어간다.

**`BottomSheet`가 끄는 동안의 비율을 부모에게 알린다**(`onDragRatioChange`). 단계만으로는 손을 뗀 뒤에나 알려져 지도가 한 박자 늦게 뚝 끊겨 따라온다 — 시트가 손끝을 따라오게 만든 것과 같은 이유다. 시트는 여전히 `dragRatio`의 주인이고, 부모는 통보만 받는다.

**`HomeScreen`이 비율 변화를 지도 중심에 민다.** `sheetRatio = dragRatio ?? SHEET_RATIO[sheetDetent]`가 바뀌면 `shiftCenterForSheet`로 그 차이만큼 옮긴다. **절대 위치가 아니라 차이인 이유**: 절대식으로 쓰려면 「지금 띠 한가운데가 어디인가」를 상태로 따로 들어야 하는데, 그러면 지도가 카메라로 돌려주는 값과 두 벌이 되어 갈린다. 직전에 반영한 비율은 ref로 든다.

**지도를 다시 잡는 곳(`focusMapOn`)은 「지금」 비율로 놓는다.** 옮겨 갈 단계의 비율을 미리 반영하면 effect가 한 번 더 밀어 **두 번 적용된다** — 실제로 그랬다: 「내 주변」이 내 위치를 0.0105° 지나쳤다. 지금 비율로 놓아 두면 effect가 이어받아 새 띠 한가운데로 옮겨 준다.

**위도↔픽셀은 정확한 메르카토르다.** `cos(위도)×상수` 선형화는 왕복이 안 닫힌다(되돌아올 때의 `cos`가 다른 위도에서 계산된다). half→full→peek→half 한 바퀴에 37.6이 37.60000029로 돌아왔다 — 끄는 동안 매 프레임 도는 계산이라 시트를 몇 번 오르내리면 지도가 흘러간다. 선형화는 이제 **테스트의 검산자**로만 산다(`map.test.ts`) — 구현을 구현으로 검사하지 않기 위한 독립 오라클이다.

헤드리스 크롬 390×844 실측(실제 터치 드래그, 손 안 뗀 중간까지):

| 단계 | 시트 상단 | 띠 한가운데 | 경복궁 핀 바닥 |
| --- | --- | --- | --- |
| half | 371 | 186 | 185 |
| 끄는 중 | 250 | 125 | 125 |
| 더 끌어올림 | 90 | 45 | 45 |
| full | 68 | 34 | 33 |

**미해결:** 진입 화면(서울 전역)은 아직 보정하지 않는다. 시청(`SEOUL_CENTER`)이 지도 한가운데라 half 시트 뒤에 있고, 보이는 띠에는 서울 북부가 온다. 명소를 고르기 전이라 「보고 있는 곳」이 없어서 미뤘다.

**테스트에서 `full`로 가는 길이 바뀌었다.** 「명소를 누른다」가 더는 그 길이 아니다 — 손잡이 클릭이나 「오늘의 서울 열기」를 써라. 이 갈아타기를 안 하면 `sheetDetent`의 클램프(`mapReady ? detent : 'half'`)와 `onBack`의 `setDetent`가 **지워도 안 죽는** 상태가 된다. 실제로 그 둘을 포함해 변이 5개를 돌려 전부 사망을 확인했다.

실측(390×844, 헤드리스 크롬 터치 이벤트): 시작 473px → 손끝을 따라 551 → 631 → 711px → 놓으면 776px(full)로 붙는다.

**놓을 때는 여전히 가장 가까운 단계로 붙는다.** Google Maps 시트와 같은 규칙이고 설계 §2.2가 그 화면을 참조한다. 임의 높이로 눌러앉히려면 `detent` 하나인 인터페이스를 비율까지 나르도록 넓혀야 하고, `RecenterButton`의 위치가 `SHEET_RATIO[detent]`에서 파생되므로 그쪽도 함께 가야 한다 — 필요해지면 그때 할 일이다.

### 로딩 속도 — 구조가 다르다 (2026-08-13)

비교 대상으로 받은 `gjdong.vercel.app/crowd`와 나란히 쟀다.

| | gjdong | seoul-live |
| --- | --- | --- |
| HTML 크기 | 13.9KB(**내용이 들어 있다**) | 1KB(빈 껍데기) |
| 첫 내용까지 | 578ms | 593ms |
| 전송량 | 912KB / 요청 19개 | 150KB / 요청 3개 |

**숫자는 비슷하지만 구조가 다르다.** 저쪽은 Next.js 서버 렌더라 HTML에 이미 내용이 실려 온다. 우리는 껍데기 → JS(476KB, gzip 145KB) → React 마운트 → 30곳 일괄 조회 → 렌더의 사슬이다.

**SSR로 따라갈 수는 없다.** 우리 배포물은 앱인토스 `.ait` 패키지라 서버가 없다. 대신 쓸 수 있는 손잡이는 셋이다:

1. **CDN을 미리 데운다.** TTL 1시간이라 **매 시간 첫 사용자가 상류 지연(~1.4초)을 혼자 먹는다.** Vercel Cron이 만료 직전에 30곳을 새로 받아 두면 모든 사용자가 HIT을 맞는다. **호출량은 늘지 않는다** — 지금도 그 720회는 사용자 요청에 얹혀 나가고 있고, 주체만 크론으로 옮기는 것이다.
2. **번들 쪼개기.** Google Maps SDK는 별도로 900KB쯤 더 받는다. 지도를 지연 로드하면 목록이 먼저 선다.
3. **`preconnect`.** 프록시 오리진과 `maps.googleapis.com`에 미리 연결해 둔다.

셋 다 아직 안 했다. 1번이 값이 가장 크고 코드 변경이 가장 작다.

### 프록시가 배포됐다 (2026-08-13)

**`https://seoul-live-now.vercel.app`** — Vercel 프로젝트 이름은 `seoul-live`다. 세 엔드포인트가 실키로 돌고, `.env`의 `VITE_API_BASE_URL`이 이 주소를 가리킨다. 빌드본을 띄워 전체 경로를 실측했다: 목록이 `/api/citydata-bulk`로 나가 「정보 없음」 0개, 거리순 정렬과 마커 30개가 그대로 뜬다.

**CDN 캐시가 실제로 동작한다 — 쿼터 전략의 전제다.** 같은 URL을 세 번 부르면 3번째가 `x-vercel-cache: HIT`(`age: 4`)다. 주의: 클라이언트가 받는 `cache-control`은 그냥 `public`이라 **헤더만 보면 캐시가 꺼진 것처럼 보인다.** Vercel이 `s-maxage`를 자기 CDN용으로 소비하고 응답에서 떼기 때문이다 — 확인은 헤더가 아니라 `x-vercel-cache`로 해야 한다.

**TTL 환경변수는 아직 안 넣었다.** `CACHE_TTL_SECONDS`·`CITYINFO_CACHE_TTL_SECONDS`가 Vercel에 없어서 코드 기본값 3600으로 떨어진다 — 값은 의도한 대로지만, 손잡이가 콘솔에 안 보이므로 조정하려면 먼저 추가해야 한다.

**활용갤러리 등록은 하지 않기로 했다(2026-08-13).** 그래서 위 표의 「30곳 / 1시간」이 임시가 아니라 **운영 설정**이다. 따라오는 결과: 명소 121곳 확장 불가, 혼잡도가 최대 1시간 묵음(서울시는 5분마다 갱신한다), 요일×시간 패턴은 기기별 누적 유지, 어제 대비 불가. 예산은 혼잡도 720회/일 + 도시정보 최대 280회/일 = 1,000회로 **여유가 0이다** — 도시 정보를 시간당 서로 다른 명소 11곳 넘게 펼치면 넘친다. 넘칠 때 손잡이는 `CACHE_TTL_SECONDS`를 7200으로 올리는 것이고(혼잡도 360회/일로 줄어 도시정보 여유가 640회가 된다) 코드 변경은 없다.

`SEOUL_SUBWAY_API_KEY`도 Vercel에 올라가 있지만 **쓰는 코드가 없다.**

### 자체 Map ID를 넣었다 (2026-08-13)

`VITE_GOOGLE_MAPS_MAP_ID`가 채워져 **`DEMO_MAP_ID` 폴백을 더 이상 타지 않는다.** 실측으로 확인했다: `googleMapsMapId()`가 자체 ID를 돌려주고, `AdvancedMarker` 30개가 그대로 렌더되며 다른 콘솔 오류가 없다.

**벡터로 뜨는지는 이 저장소의 검증 수단으로 확인할 수 없다.** 헤드리스 크롬에서 「Attempted to load a Vector Map, but failed. Falling back to Raster」가 뜨는데, **이 오류는 Map ID 설정과 무관하다** — 구글이 벡터로 제공하는 `DEMO_MAP_ID`를 같은 브라우저에서 나란히 재도 똑같이 `RASTER`가 나온다(`map.getRenderingType()`으로 확인). 원인은 환경이다:

- `--disable-gpu` 헤드리스에는 WebGL이 아예 없다(`getContext('webgl')`이 null).
- `--enable-unsafe-swiftshader --use-angle=swiftshader`로 소프트웨어 WebGL을 켜면 WebGL은 생기지만(`ANGLE (Google, Vulkan 1.3.0 (SwiftShader Device ...))`) **Google Maps가 이 렌더러를 벡터 지도에 쓰지 않는다.**

**확인법(사람이 해야 한다):** GPU가 있는 보통 크롬으로 앱을 열고 DevTools 콘솔을 본다. 「Attempted to load a Vector Map, but failed」가 **없으면** 벡터로 뜬 것이다. 있으면 `chrome://gpu`에서 WebGL이 하드웨어 가속인지 먼저 보고, 그래도 뜨면 그때가 Map ID를 벡터로 다시 만들 자리다.

**래스터로 떨어져도 앱은 선다.** 마커 30개가 그대로 렌더되는 것을 확인했다 — 벡터 여부는 기울기·회전·커스텀 스타일의 문제이지 마커의 문제가 아니다. 기울기·회전은 Map ID에서 **둘 다 껐다**(`disableDefaultUI` 때문에 되돌릴 컨트롤이 화면에 없어서다).

### 인증키가 들어왔다 — 실데이터로 돈다 (2026-08-13)

**목업을 벗어났다.** `.env`의 `SEOUL_API_KEY`로 실호출이 되고 `VITE_USE_MOCK=false`다. sample 키가 아닌 것도 확인했다 — 강남역을 요청하면 강남역이, 여의도한강공원을 요청하면 그곳이 온다.

**개발 미들웨어의 봉투를 배포와 맞춰야 한다.** 처음 붙였을 때 일괄 조회를 `{이름: 응답}`으로 평평하게 돌려줬는데, 배포의 `api/citydata-bulk.ts`는 `{ results: {이름: 응답} }`으로 감싼다. 클라이언트의 `parseBulkEnvelope`가 그 모양만 받으므로 **30곳이 전부 「정보 없음」으로 떴다** — 단일 조회와 상세는 멀쩡해서 원인이 위치나 파서에 있는 것처럼 보였다. 동시 연결 수도 배포와 같은 8로 묶었다(안 묶으면 로컬에서 안 나던 실패가 배포에서만 난다).

**「내 주변」은 브라우저에서 정상이다.** 토스 SDK의 `Device.getLocation`은 웹뷰 밖에서 **평범한 `Error`**를 던진다(「apps-in-toss 웹뷰 환경이 아니에요」) — `GetCurrentLocationPermissionError`가 아니다. 그래서 `requestCoords`가 거부로 오해하지 않고 `navigator.geolocation`으로 떨어진다. 실측으로 확인했다: 좌표가 잡히면 위치 안내가 사라지고 FAB·「거리순」이 살아나며 목록이 `강남역 10m → 가로수길 2.5km → 잠원한강공원 2.7km`로 거리순 정렬된다.

**헤드리스 크롬은 위치 권한을 끝내 안 준다.** `Browser.grantPermissions`가 OK를 돌려주고 `Emulation.setGeolocationOverride`를 걸어도 `getCurrentPosition`이 `code 1(User denied)`로 떨어진다. 그래서 화면이 「위치를 허용하면…」을 띄우는데 **이건 앱이 아니라 harness 쪽 제약이다.** 확인하려면 `Page.addScriptToEvaluateOnNewDocument`로 `navigator.geolocation`을 대역으로 세워라 — 우리 코드가 브리지 실패 뒤 웹 표준으로 떨어지는지는 그걸로 본다.

**개발 서버가 `/api/*`를 직접 처리한다.** `vite.config.ts`의 `seoul-api-dev-server` 플러그인이 `api/_lib/seoul.ts`의 `fetchArea`를 그대로 재사용해 세 엔드포인트를 흉내 낸다(`citydata`·`cityinfo`·`citydata-bulk`). Vercel CLI 없이 로컬에서 실데이터를 볼 수 있고, **인증키는 Node 쪽에만 머문다** — `import.meta.env`로 읽지 않으므로 번들에 안 들어간다. 빌드 산출물을 키 문자열로 검색해 확인했다.

**인증키에 `VITE_` 접두사를 붙이면 안 된다.** Vite는 `VITE_`로 시작하는 것만 클라이언트에 노출하는데, 서울 키가 번들에 들어가면 누구나 꺼내 하루 1,000회를 태울 수 있다. Google Maps 키는 반대로 **번들에 들어가는 게 정상**이다(타일을 브라우저가 직접 받는다) — 보호는 은닉이 아니라 Cloud Console의 리퍼러·API 제한이다.

`SEOUL_SUBWAY_API_KEY`는 받아 뒀지만 **쓰지 않는다.** 지하철 실시간 도착이 `citydata`의 `SUB_STTS`로 같은 키에서 함께 오기 때문이다.

#### 실호출이 드러낸 파서 버그 둘

명세의 출력명 표는 필드를 한 층으로 늘어놓아서 **중첩이 안 보인다.** 짐작으로 짠 두 곳이 실제로 틀렸고, **손으로 지어낸 목업과 손으로 지어낸 테스트가 같은 오해를 공유해서 둘 다 통과하고 있었다.**

| 곳 | 짐작한 모양 | 실제 모양 | 증상 |
| --- | --- | --- | --- |
| 도로소통 | `ROAD_TRAFFIC_STTS`에 지표가 바로 | `ROAD_TRAFFIC_STTS.AVG_ROAD_DATA` 안 | 카드가 **통째로 사라짐** |
| 지하철 | 행 하나가 열차 하나 | 행이 **역**이고 열차는 `SUB_DETAIL` 배열 | 역 이름만 뜨고 열차가 빔 |

둘 다 고쳤고 목업도 실제 모양으로 바꿨다. **`docs/fixtures/citydata-광화문덕수궁.json`에 실호출 응답 한 벌을 넣고 `citydataFixture.test.ts`가 파서에 통과시킨다** — 지어낸 자료끼리만 대조하면 같은 종류의 오해를 다시 만든다.

#### 확인된 값 (명세에 없던 것들)

| 필드 | 실제 값 |
| --- | --- |
| `PPLTN_TIME` | `"2026-08-13 13:35"` — `observationSlot`이 읽는 형식 그대로 |
| `REPLACE_YN` | `"N"` — `'Y'`/`'N'` 가정이 맞았다 |
| `AREA_CD` | `"POI009"` — 손으로 적은 카탈로그 값과 일치 |
| `MALE_PPLTN_RATE` 등 | `"50.0"` 소수 한 자리 문자열 |
| `ROAD_TRAFFIC_IDX` | `"정체"` — 목업이 빌려 쓴 셋 중 하나. **아직 한 표본이라 톤은 안 붙인다** |
| `ROAD_TRAFFIC_SPD` | `12` — **문자열이 아니라 number** |
| `FCST_DT` | `"202608131200"` — 붙여 쓴 12자리. `forecastHourLabel`이 읽는 형식이 맞았다 |
| `SUB_STN_LINE` | `"3"` 숫자만. 열차 쪽 `SUB_LINE`이 `"3호선"` |
| `SUB_ARMG1` | `"전역 출발"` / `"9분 후 (동대입구)"` — 괄호까지 포함해 온다 |
| 문화행사 키 | **`EVENT_STTS`** (명세는 `CULTURALEVENTINFO`). 파서의 후보 목록에 둘 다 있어 살았다 |
| 행사 `PAY_YN` | `null` — 유무료를 **모르는** 것이지 무료가 아니다 |

`citydata` 한 응답에 섹션이 17개다. 우리가 안 읽는 것: `BUS_STN_STTS`(32) · `CHARGER_STTS`(45) · `LIVE_CMRCL_STTS` · `LIVE_SUB_PPLTN` · `LIVE_BUS_PPLTN` · `LIVE_YNA_NEWS`.

**실기기에서 화면까지 확인했다**(헤드리스 크롬 390×844, 강남역): 추정 인구 86,000~88,000명 · 도로소통 「정체」 · 시간대별 예보 24칸 · 통합대기 「좋음」 · 지하철이 detail_page.png와 같은 모양(`신논현 9호선` → `개화행 / 5분 30초 후 (삼성중앙)`).

### detail_page.png 대조로 붙인 것 (2026-08-13)

참고 화면(`detail_page.png` — 서울 인파레이더 상세)과 우리 상세를 대조해 **추가 API 호출이 0인 것**부터 붙였다. 넷 다 이미 부르고 있는 응답 안에 있던 필드이거나 이미 기기에 쌓고 있던 값이라 하루 1,000회 예산을 한 푼도 더 쓰지 않는다.

| 붙인 것 | 출처 | 호출 |
| --- | --- | --- |
| 시간대별 예보 | `citydata`의 `FCST24HOURS`(명세 200~206행), `WEATHER_STTS` 안에 중첩 | 0 |
| 지하철 실시간 도착 | `citydata`의 `SUB_STTS`(명세 62~78행) | 0 |
| 「평소 대비」 한 줄 | 이미 쌓고 있는 요일×시간 관측(`useWeekPattern`) | 0 |
| 티맵 안내 | 딥링크. 위 「길찾기가 셋이 됐다」 참고 | 0 |

**아직 안 붙인 것과 이유:**

- **1시간·3시간·한 달 전 대비 %** — **이 구조로는 못 만든다.** 저장하는 것이 `PatternCell { rankSum, count }`라 인구 수도, 시간별 값도 없다(3시간 묶음의 랭크 합뿐이다). 값을 더 쌓게 고쳐도 관측이 **사용자가 상세를 열 때만** 생겨서 「1시간 전」 칸은 거의 항상 빈다. 서버 수집이 선행돼야 하고, 그건 활용갤러리 등록에 막혀 있다(PLAN.md 4차와 같은 벽이다).
- **주변 CCTV 목록** — **이 항목의 결론은 2026-08-19에 뒤집혔다. 아래 「CCTV 출처를 찾았다」 절이 정본이다.** (남겨 두는 이유는 조사 방법의 실패담이기 때문이다 — 아래 취소선 안의 넷은 각각은 사실이지만 **엉뚱한 곳만 뒤졌다.** 서울시가 스스로 만든 웹이 답을 갖고 있었는데 그 웹을 안 열어 봤다.) ~~**쓸 만한 출처가 없다.** (1) `citydata` 명세 282행 전체에서 CCTV가 **0회**다. (2) 좌표가 있던 「서울시 안심이 CCTV 연계 현황」은 **종료된 서비스**이고 교통용도 아니다. (3) 교통 CCTV의 원천은 TOPIS인데 공개 OpenAPI 문서가 없고 지도 페이지의 내부 AJAX에 기대야 한다 — 문서화되지 않았고 언제든 바뀐다. (4) ITS 국가교통정보센터는 좌표와 스트림을 함께 주지만 **고속도로·국도**라 도심 명소 주변이 아니다. 여기에 앱인토스가 iframe을 금지해 영상 재생은 어차피 범위 밖이다. **결론: 문서화된 출처가 생기기 전에는 만들지 않는다.**~~
- ~~**상단 요약 칩 행**~~ — **2026-08-14에 붙였다.** 여기 적혀 있던 충돌(펼칠 때만 조회하는 구조)은 도시정보를 자동 조회로 바꾸면서 사라졌다. 아래 「요약 칩과 오늘 표시」 참고.

### 명소 상세의 도시 정보

**명소 하나를 기준으로만 성립한다.** `citydata`가 장소 단위 API라 다른 방법이 없다. 「더보기」 탭이던 시절에는 탭 안에서 명소를 골라야 했지만(`AreaPicker`), 지금은 상세가 이미 명소가 정해진 자리라 고르는 단계가 통째로 없어졌다.

**상세를 열면 자동으로 조회한다(2026-08-14).** 예전에는 접힌 채로 시작해 「이곳의 도시 정보」를 눌러야 나갔다 — 쿼터를 지키던 자리다. 그 720회는 다른 데서 되찾았고(`findSeededSnapshot`), 지금은 접이식이 없다. **안쪽 절을 다시 접어도 호출량은 1원도 안 준다** — `citydata` 한 번이 절 전부를 주기 때문이다.

섹션은 재난문자 배너 → 날씨·대기 → 도로소통 → 지하철 → 주차장 → 따릉이 → 문화행사 순이고, 맨 위에 요약 칩 한 줄이 있다. 재난문자만 `role="alert"`로 올린다 — 나머지는 찾아 읽는 정보지만 이건 지금 알아야 하는 내용이다.

**도시 정보가 실패해도 위쪽 혼잡도·예측·길찾기는 그대로 남는다.** 반대로 혼잡도가 실패해도 도시 정보는 나온다. 둘은 다른 엔드포인트다.

**"정보 없음"과 "만차"를 구분한다.** 주차장이 실시간 정보를 주지 않으면(`CUR_PRK_YN`이 `N`) "실시간 미제공"이라고 쓴다. 둘을 묶으면 실시간을 안 주는 주차장이 전부 만차로 보이는데, 그 앞을 지나가는 사용자에게는 정반대의 안내다.

**따릉이는 주차장과 톤이 반대다.** 주차장은 빈 자리가 많아야 좋고 대여소는 자전거가 남아 있어야 좋다. `parkingTone`을 재사용하지 않는 이유이고, `BikeList.test.tsx`가 이걸 고정한다.

### 오늘의 서울은 추가 호출이 0이다

이 뷰의 존재 근거다. `useAreaCongestion`으로 이미 받아둔 121곳 혼잡도를 `domain/summary.ts`가 다르게 집계할 뿐이다.

시트 안 뷰가 되면서 여는 길이 시트 맨 위 **요약 스트립**이 됐고, 돌아오는 길은 뷰 안의 「목록으로」다. 요약 스트립도 같은 `summarize()`를 쓰므로 여기서도 호출이 늘지 않는다.

**재난문자는 캐시에 있는 것만 모은다.** `useCachedCityAlerts`가 `queryClient.getQueryData(areaPayloadKey(name))`를 읽고, 없으면 조회하지 않는다. 사용자가 상세를 연 명소만 여기 잡힌다. 목록의 121곳을 새로 부르면 하루 2,904회가 그대로 더해진다 — 한도의 세 배다.

**그 키는 손으로 적으면 안 된다.** 2026-08-27에 키(`['cityInfo', name]` → `['areaPayload', name]`)와 캐시에 앉는 모양(파싱된 `CityInfo` → 원본 `AreaPayload`)이 함께 바뀌었는데, 이 훅만 옛 키를 손으로 들고 있어서 **조용히 항상 빈 값**이 됐다. 지금은 `areaPayloadKey` 하나가 키를 만드는 유일한 자리다.

**같은 경보가 여러 명소에 실려 온다.** `AlertDigest`가 문구로 중복을 지운다 — 안 지우면 폭염 경보 하나가 30줄이 된다.

### 도시 정보는 호출량을 늘린다

PLAN.md와 이전 STATE는 "`citydata` 하나로 다 오니 호출이 늘지 않는다"고 적었지만, **그건 `citydata_ppltn`을 `citydata`로 교체할 때 이야기다.** 실제 구현은 `/api/cityinfo`라는 **별도 엔드포인트**를 얹었으므로 호출이 더해진다.

**아래 표는 2026-08-14의 정본이었고 지금은 한 칸도 맞지 않는다. 정본은 `AGENTS.md`의 쿼터 조항 하나다** — 여기서 다시 세지 마라. 옛 표를 남겨 두는 것은 **무엇이 왜 바뀌었는지**가 다음 결정의 근거이기 때문이다.

| | 서비스 | 하루 호출량 | 지금은 |
| --- | --- | --- | --- |
| 혼잡도 일괄(전 화면) | `citydata_ppltn` | 30곳 ÷ TTL 1시간 = 720회 | **0회** — 출처가 SeoulRtd로 갔다(인증키를 안 쓴다) |
| 상세 혼잡도 | — | 0회 — 씨앗(`findSeededSnapshot`) | **0회이되 이유가 다르다** — 도시정보와 한 응답이다. 씨앗은 멈춰 있다 |
| 도시 정보 | `citydata` | 본 명소 수 ÷ TTL 3시간. 최악 30곳 = 240회 | **상세 전체**가 24 × 연 명소 수. 최악 41곳 = 984회 |
| 합계 | | 960 / 1,000 | **984 / 1,000** (41곳까지) |

바뀐 것이 셋이다. (1) 목록·지도가 인증키 없는 상류로 옮겨 **720회가 통째로 0이 됐다.** (2) 상세가 `citydata` **한 번**으로 혼잡도와 도시정보를 함께 받는다(2026-08-27) — 그전까지 명소당 하루 8회를 더 쓰던 `citydata_ppltn` 호출이 사라졌다. (3) TTL이 3시간에서 1시간이 됐다. 합쳐서 천장이 **30곳에서 41곳으로** 올라갔다.

**예전에는 1,440회로 이미 넘고 있었다.** 상세가 목록이 이미 받아 둔 혼잡도를 다시 물어 720회를 더 썼는데, 그건 CDN 캐시 키가 따로라 정말로 호출이었다. 그 왕복을 없앤 자리에 도시정보 자동 조회를 넣었다.

**「`findSeededSnapshot`을 지우면 표가 1,680회로 무너진다」고 적혀 있었다 — 이제 아니다.** 그 함수는 2026-08-20에 이미 동작을 멈췄고(목록이 그 캐시를 안 채운다) 쿼터에는 아무 영향이 없다. 지금 그것을 되살리려는 이유는 호출량이 아니라 **상세를 열 때 큰 글씨가 왕복 없이 뜨게 하려는 것**이다.

**「교체하지 않고 나눴다」고 적혀 있었다 — 2026-08-27에 합쳤다.** 막던 이유는 응답 크기였다: `citydata`는 훨씬 크고, 30곳을 한 번에 받던 `citydata-bulk`에 얹으면 인구 목록만 필요한 지도·목록까지 매번 큰 응답을 받아야 했다. 그 전제가 사라졌다 — 지도·목록이 SeoulRtd로 가면서 큰 응답을 받는 자리가 **사용자가 실제로 연 상세 한 곳뿐**이 됐다.

손잡이는 **`CITYINFO_CACHE_TTL_SECONDS`** 환경변수다(Vercel에 넣는다). 비워두면 `CACHE_TTL_SECONDS`와 같은 값으로 떨어진다. 늘리면 도시 정보 호출이 그만큼 줄고, 대신 주차 여유 면수가 그만큼 묵는다. **활용갤러리에 등록해 한도가 풀리면 이 손잡이는 의미가 없어진다** — 등록이 근본 해결책이다.

**인구 구성은 이 표에 들어가지 않는다.** 상세가 받는 `citydata` 응답의 인구 행(`LIVE_PPLTN_STTS`)에서 함께 읽으므로 위 「상세 전체」 24회 안에 이미 들어 있다 — 추가 호출이 진짜로 0이다.

### 시안(stitch_ui) 반영 상태

> **이 절은 옛 시안(Seoul Flow) 기준이고 2026-08-20에 대체됐다.** 아래 문장의 정본 경로를 그대로 믿지 마라 — 지금 정본은 `docs/DESIGN.md`(Seoul Now)다. 절을 통째로 남긴 것은 **「의도적으로 다르게 둔 것」 표의 근거들이 아직 유효해서**다. 갈래 수나 경로가 아니라 그 이유를 읽어라.

디자인 토큰의 출처는 정본 하나다(**지금은 `docs/DESIGN.md`**, 이 절을 쓸 당시에는 `stitch_ui/seoul_flow/DESIGN.md`였다). `src/index.css`에서 값을 직접 고치지 말고 그 파일을 고친 뒤 옮긴다.

**반영됨:** 색상 **65개**(시안의 47개 + 우리가 더한 18개 — 혼잡도 4단계·`on-*-container`·히트맵 램프·브랜드 색), 타이포 스케일 7단계(`text-display-lg`~`text-label-sm`), 반경 4종, Level 2 그림자(`shadow-floating`), 가로 스크롤 추천 카드, 정렬 기준 전환, 우하단 FAB, 차트 격자, 길찾기 아이콘, "근처 쾌적한 장소", 요일×시간 히트맵.

**2026-08-12에 정본을 실제와 맞췄다.** 그전까지 DESIGN.md는 Stitch가 뽑아낸 뒤 한 번도 조정되지 않아 열 곳에서 사실과 달랐다. **`src/tokens.test.ts`가 두 파일의 색을 양방향으로 대조하므로 이제 한쪽만 고치면 죽는다.**

**의도적으로 다르게 둔 것:**

| 시안 | 우리 | 이유 |
| --- | --- | --- |
| 카드·목록의 명소 사진 | 텍스트만 | 서울 API가 이미지를 주지 않는다. 30곳 사진 자산이 준비되면 붙인다 |
| "정렬 기준 ˅" 드롭다운 | 세그먼트 버튼 셋 | 메뉴를 여는 탭 한 번이 낭비다. 선택지가 셋(거리·여유·붐빔)이라 지금 무엇이 골라져 있는지 한눈에 보이는 쪽이 낫다 |
| 차트의 "어제" 비교선 | 없음 | 과거 데이터를 누적할 저장소가 없다 |
| 글래스모피즘 상단바 | 없음 | 2026-08-10 개편으로 상단바 자체가 사라졌다. 지도 위 검색 바가 그 층을 쓰고 토스가 네이티브 헤더를 준다 |
| 상단바 햄버거·검색 아이콘 | 검색만, 지도 위에 | 햄버거는 열 메뉴가 없어 죽은 버튼이 된다. 검색은 시안의 상단바가 아니라 지도 위 떠 있는 바가 됐다 |
| 하단 4탭 | 없음 | 화면이 하나다. 갈 곳이 남지 않았다 — 설계 §2.2 |
| 명소 카드 | 구분선 목록 | 카드는 여백을 많이 먹는데 시트가 화면의 절반뿐이라 한 번에 보이는 행 수가 곧 값어치다 |
| CCTV 버튼 | 없음 | 1차 범위 밖 |
| 요일×시간 히트맵 | **있다** | 2026-08-11에 붙였다. 다만 시안과 달리 조회가 아니라 **이 기기에 쌓는 것**이다 — 서울 API에 과거 조회가 없다 |

## 확인된 사실

### 일괄 조회는 불가능하다

"`AREA_NM`을 비우면 전체가 오지 않을까"라는 기대가 있었으나 **공식 명세가 명시적으로 부정한다**: "한 번에 1개 장소씩만 호출 가능".

명소 1곳당 1회 호출이 확정이고, 서버 측 공용 캐시 없이는 서비스가 성립하지 않는다.

| | 클라이언트 직접 호출 | 프록시 캐시 경유 |
| --- | --- | --- |
| 앱 1회 열 때 | 30회 소진 | 0회 (캐시 히트) |
| 하루 1,000회로 버티는 양 | 총 33번 열면 끝 | 사용자 수 무관 |
| 실제 호출량 | 사용자 수에 비례 | 30회/시간 × 24 = 720회 고정 |

### 명소 이름 검증 (2026-08-03)

「실시간 도시데이터 매뉴얼」 V8.5 제2장에서 공식 주요장소 **121곳** 전체를 추출해 `src/data/official-areas.ts`에 넣었다. 우리 카탈로그 30곳이 **전부 포함됨을 확인**했고 `areas.test.ts`가 이를 고정한다. 일부러 오타를 넣어 테스트가 실제로 잡는 것도 확인했다.

남은 건 공백뿐이다. PDF 추출이 `광장( 전통) 시장`처럼 공백을 넣어서 괄호 주변을 정규화했지만, 실제 API 형태와 같은지는 호출해봐야 안다.

### 공식 명세 대조 (2026-08-04)

출처: 저장소 루트의 `서울시+실시간+도시데이터.xls`. 확장자와 달리 **HTML 문서**이고, 내용은 `citydata` 서비스 명세다(우리가 쓰는 `citydata_ppltn`은 이 중 인구 부분).

- **`sample` 키는 광화문·덕수궁만 준다** — 실험으로 추정하던 걸 명세가 못 박았다.
- **`AREA_NM`에 장소명 대신 장소코드도 넣을 수 있다.** 응답에도 `AREA_CD`가 실려 온다.
- **URL 형식이 우리 구현과 일치한다** — `.../{키}/{타입}/{서비스}/{시작}/{끝}/{장소명}`.
- **에러 코드 전체 표 확보** — 이걸로 재시도 정책 결함을 하나 잡았다(아래 이력 참고).

명세로도 해결되지 않은 것:

- 명세 **내부에서 장소 수가 121곳과 120곳으로 엇갈린다**(설명은 121, `AREA_NM` 항목은 "주요 120장소명 목록"). 우리는 매뉴얼 V8.5 기준 121곳을 쓴다.
- 장소코드의 정본은 명세가 가리키는 `서울시 주요 121장소명 목록(코드포함).xlsx`인데 **그 파일이 저장소에 없다.** `POI014` 같은 값이 맞는지 확인 못 했다.

### 2차 계획의 전제가 틀렸다

"도시정보(더보기) 화면에는 API 5종을 추가로 붙여야 한다"고 적어뒀는데, **`citydata` 하나에 전부 들어 있다.** 주차장·따릉이·도로소통·지하철·버스·문화행사·날씨·미세먼지·전기차충전소·사고통제·상권·재난문자·연합뉴스가 모두 같은 응답의 필드다. 그래서 도시 정보에 섹션을 더 붙여도 호출은 늘지 않는다 — 도로소통·사고통제를 그렇게 붙였다(위 "개편으로 해소된 것").

~~"호출 횟수는 늘지 않는다"~~ — **이 결론은 절반만 맞았다.** 한 화면 안에서는 맞지만, `citydata_ppltn`을 그대로 두고 `citydata`를 **추가로** 부르면 호출이 더해진다. 실제 구현이 그쪽이다. 위 "도시 정보는 호출량을 늘린다" 참고.

### Toss Design System은 쓰지 않는다 (2026-08-06 결정)

두 번 나온 질문이라 답을 못 박아둔다. **이 앱은 TDS(`@toss/tds-mobile`)를 쓰지 않는다.** 디자인 시스템의 출처는 `docs/DESIGN.md` → `src/index.css` 토큰 → Tailwind다. (2026-08-20까지는 그 첫 칸이 `stitch_ui/seoul_flow/DESIGN.md`였다. 갈아 끼운 것은 **정본 파일이지 이 결론이 아니다** — TDS를 안 쓴다는 답은 그대로다.)

이유는 취향이 아니라 버전이다. `@toss/tds-mobile@2.5.1`의 peerDependencies가 **React 18에서 끊긴다.** 이 프로젝트는 React 19.2.8이라 도입하려면 React를 내려야 하고, 그러면 지도에 쓰는 `@vis.gl/react-google-maps`(React 19 지원을 명시)까지 흔들린다. 시안 화면 넷이 이미 자체 토큰으로 그려져 있어 얻을 것도 적다.

사용자가 「디자인은 유지합니다」로 확정했다. 다음 세션에서 "토스 앱이니까 TDS를 써야 하지 않나"로 되돌아가지 마라.

## PWA — 웹 배포만 설치형이 된다 (2026-08-13)

`vite-plugin-pwa`로 매니페스트와 서비스워커를 붙였다. **적용 대상은 Vercel 웹 배포뿐이고 토스 미니앱은 아니다.**

**핵심 제약: 두 배포가 `dist` 하나를 나눠 쓴다.** `npm run build`가 `vite build`를 한 번 돌고 그 결과를 `ait build`가 그대로 포장하므로, `sw.js`와 `manifest.webmanifest`는 `.ait` 안에도 들어간다. 파일이 들어가는 건 몇 KB라 상관없지만 **등록되면 안 된다** — 토스 웹뷰에서 서비스워커가 옛 자산을 붙들면 `ait deploy`로 새 판을 올려도 사용자는 낡은 화면을 보고, 미니앱에는 「캐시 지우기」로 가는 길이 없다. 그래서 `injectRegister: null`로 자동 주입을 끄고 `src/platform/pwa.ts`가 환경을 보고 등록한다.

**환경 판별은 `getAppsInTossGlobals()`가 성공하는지로 한다.** SDK에 「환경이 무엇인가」를 묻는 함수가 따로 없다. 웹뷰 밖에서 무엇이 나오는지는 탐침으로 확인했다 — `TypeError`(호스트가 주입하는 전역이 없어 그 속성을 읽다가 난다). 예외 종류는 안 좁힌다: 그 메시지는 SDK 내부라 판올림에 바뀐다.

**캐시 전략**

| 대상 | 방식 | 이유 |
| --- | --- | --- |
| 셸(JS·CSS·HTML·아이콘) | precache | 13개 514KB. 오프라인에서도 화면이 뜬다 |
| 같은 오리진 `/api/*` | NetworkFirst, 3초, 1시간, 60개 | 「지금 붐빔」이 전부라 캐시를 먼저 주면 앱이 거짓말을 한다. 캐시는 오프라인일 때의 마지막 기억 |
| 구글 지도 | **안 함** | 서드파티 opaque라 용량을 못 재고 약관이 타일 저장을 제한한다 |

`/api/*` 매칭을 **같은 오리진으로 좁혔다.** 경로만 보면 아무 서드파티의 `/api/`도 걸린다. 서비스워커가 사는 곳은 웹 배포뿐이고 거기서는 `/api/*`가 같은 오리진의 Vercel 함수다 — 절대 URL(`VITE_API_BASE_URL`)은 토스 빌드에서만 쓴다. **부작용:** 로컬 `vite preview`는 `.env`의 절대 URL을 쓰므로 오프라인에서 혼잡도가 안 나온다. 그건 로컬 설정 탓이고 배포본과 다른 상태다.

**오프라인이 표현 가능한 상태가 됐다.** 예전에는 끊기면 화면 자체가 안 떠서 표현할 일이 없었다. 지금은 셸이 캐시에서 뜨고 목록도 서므로 **지도만 회색 빈칸으로 남는다.** `MapUnavailableReason`에 `'offline'`이 늘었고 판정 순서는 키 없음 → 오프라인 → 로드 실패다(오프라인은 잠깐이고 키 없음은 고치기 전까지 영영 그대로다).

**`loadFailed`로는 오프라인을 못 잡는다 — 실측이다.** 오프라인에서 구글 지도 SDK는 브라우저 HTTP 캐시에서 살아 돌아오고, 못 받는 것은 그다음의 지도 설정(`Unable to fetch configuration for mapId`)이다. 스크립트 로드는 성공했으므로 `APIProvider`의 `onError`가 **안 불린다.** 그래서 연결 여부를 `useOnlineStatus`로 따로 본다.

**아이콘은 `public/icon.svg`가 정본**이고 PNG는 `node scripts/render-icons.mjs`가 헤드리스 크롬으로 렌더한다(sharp·ImageMagick이 없고 아이콘 하나에 네이티브 의존성을 들이지 않는다). 빌드에 걸지 않았다 — 결과 PNG를 저장소에 넣고, `icon.svg`를 고쳤을 때만 다시 돌려 함께 커밋한다. 핀 실루엣은 지도 마커와 같은 path이고 빨간 점은 `--color-crowded`다. 마스커블 안전지대(가운데 80%)를 지키므로 그림 하나가 `any`와 `maskable`을 겸한다.

**실측(헤드리스 크롬):** 서비스워커 activated, 매니페스트 파싱 오류 0, 프리캐시 8개, 오프라인 새로고침에서 셸·목록·검색 정상, 지도 자리에 한국어 오프라인 안내.

**미확인:** 배포본에서의 `/api/*` 캐시(같은 오리진이라야 걸리는데 로컬은 절대 URL이다), 설치 배너, iOS 홈 화면 추가. 실기기·배포본 몫이다.

## 시간대별 인파가 막대그래프가 됐다 (2026-08-14)

`detail_page_sample.png`(서울 인파레이더 상세)를 참고해 예측 절을 고쳤다.

**예전에는 혼잡도 4단계를 y축에 그대로 쓴 점선이었다.** 그러면 40,000명과 46,000명이 같은 높이가 되어 「어느 시각이 정점인가」를 그래프가 말하지 못했다 — 정점을 아는 것이 이 절의 유일한 용도인데도. 지금은 **높이가 인원, 색이 단계**다. 둘을 갈라 두는 값은 명소마다 「붐빔」의 절대 인원이 다르다는 데 있다: 남산공원의 붐빔과 강남역의 붐빔은 자릿수가 다르고 그 차이는 높이로만 보인다.

**샘플에서 못 옮긴 것 셋 — 데이터가 없다.**

| 샘플 | 우리 | 왜 |
| --- | --- | --- |
| 24시간(과거+현재+미래) | 지금 + 앞으로 12시간 | 서울 API 요청 인자에 날짜가 없다. 실데이터 예보는 12개 |
| 어제 곡선(점선) | 없음 | 같은 이유 |
| 1시간·3시간·한 달 전 대비 | 없음 | 같은 이유. 「평소 대비」(이 기기 누적)가 그 자리를 일부 대신한다 |

그래서 제목이 **「시간대별 인파」**다. 「24시간 인파 흐름」은 샘플의 제목이지만 우리가 쓰면 거짓말이고, 「시간대별 예상」도 이제 틀리다 — 첫 막대는 예보가 아니라 지금 실측이다.

**축은 샘플과 같은 규칙으로 잡힌다.** 눈금 후보에 1.5와 3을 넣었더니(1·2·5만 쓰는 흔한 규칙 대신) 46,000에서 0/1.5만/3만/4.5만/6만이 나온다 — 샘플의 축과 같은 값이다. 1·2·5만으로는 눈금 2만, 축 8만이 되어 위가 텅 빈다.

**SVG를 안 쓴다.** 막대는 사각형이라 div 높이로 충분하고, SVG는 `preserveAspectRatio`가 글자를 찌그러뜨려(옛 차트가 축 라벨을 밖으로 뺀 이유) 값을 막대 안에 둘 수 없다. 값을 소리로 내보내려면 그 글자가 막대와 같은 요소 안에 있어야 한다.

실측(390×844)에서 잡은 것 둘: 가로축 글자가 「지/금」으로 접혔고(칸 21px, 「17시」는 28px 필요 → `whitespace-nowrap`으로 넘치게 뒀다. 넘치는 쪽은 세 칸 걸러 비어 있다), 세로축을 `justify-between`으로 펼치니 「0」이 바닥선보다 위에 떴다(절대 위치 + `-translate-y-1/2`로 눈금 가운데를 격자선에 맞췄다).

**측정 함정:** PWA를 붙인 뒤로 헤드리스 크롬이 **프리캐시된 옛 번들**을 내준다. 방금 고친 것이 화면에 안 나와 한 회차를 헛짚었다. 측정 스크립트는 첫 방문 뒤 `serviceWorker.getRegistrations() → unregister`와 `caches.delete`를 돌리고 **다시 navigate**해야 한다.

## 인파레이더와의 격차를 좁힌다 (2026-08-14)

사용자 지적: 「서울 인파레이더는 목록 선택 시 전체 데이터가 다 노출된다. 로드 시간도 더 짧다. 따뜻한 톤·아이콘·다크모드·언어지원. 퀄리티가 낮으면 소비가 되지 않는다.」 넷 중 셋을 했다.

### 상세가 즉시 열린다 — 그리고 하루 720회가 비었다

**홈의 일괄 조회가 30곳을 통째로 받아 두는데 상세가 그중 한 곳을 다시 물었다.** 이미 메모리에 있는 값을 받으려고 왕복 한 번을 더 기다렸고 그동안 스켈레톤이 떴다. 인파레이더가 즉시 열리는 것과 갈리던 자리다.

**그 왕복은 CDN 캐시 키가 따로라 서울 API 호출을 또 썼다.** 최악 30곳 × 24시간 = 720회/일. 일괄 조회의 720회와 합쳐 **1,440회로 이미 하루 1,000회 한도를 넘고 있었다** — 도시정보를 붙이기 전부터.

`queries.ts`의 `findSeededSnapshot`이 `['areas', ...]` 캐시에서 그 자리를 찾아 `initialData`로 깐다. `initialDataUpdatedAt`을 같이 넘기는 것이 핵심이다 — 안 넘기면 목록이 아무리 묵어도 상세가 영영 다시 받지 않는다. 목록이 5분을 넘겼으면 화면은 즉시 그리면서 뒤에서 새로 받는다.

### 도시 정보가 통째로 펼쳐진다

되찾은 720회를 여기 넣었다. 접이식을 없애고 상세를 열면 자동으로 조회한다. 대신 `CITYINFO_CACHE_TTL_SECONDS` 기본값을 3시간으로 올렸다.

| 항목 | 주기 | 하루 호출 |
| --- | --- | --- |
| 혼잡도 일괄(30곳) | 1시간 | 720 |
| 상세 혼잡도 | — | 0 |
| 도시정보(최악 30곳) | 3시간 | 240 |
| 합계 | | **960 / 1,000** |

**안쪽 절을 접어도 호출량은 안 준다.** `citydata` 한 번이 주차장·따릉이·날씨·문화행사·지하철·재난문자를 통째로 준다. 무엇을 접고 펼칠지는 순전히 화면 문제다.

**캐시가 길어진 만큼 기준 시각을 적는다.** 「4분 후 도착」·「잔여 568면」은 상대·순간 값이라 3시간 캐시를 견디지 못한다. `InfoSection`의 `note`가 그 자리다. **제대로 고치려면 응답의 `Age` 헤더를 화면까지 가져와 실제 경과를 적어야 한다 — 아직 안 했다.** 지금 지하철 절은 「최대 3시간 전 기준」이라고만 말한다.

### 따뜻한 톤 + 다크 모드

중립색이 파랑-보라(#faf8ff, 색상 ~250°)였는데 그 위의 혼잡도 4색이 전부 따뜻한 계열이라 배경과 데이터가 서로 밀어냈다. 크림(~38°)으로 옮겼다. **강조색은 파랑 그대로다** — 따뜻한 강조색은 「약간 붐빔」(#ea580c)과 색상이 붙어 길찾기 버튼과 혼잡도가 같은 색으로 읽힌다(사용자 선택).

다크는 **이름을 따로 두고**(`--color-dark-*`) `:root[data-theme='dark']`에서 `var()`로 갈아 끼운다. 같은 이름에 색을 두 번 적으면 `tokens.test.ts`가 파일에서 색을 읽을 때 라이트 대비를 재는 척하며 다크 값을 잰다.

**실측(390×844, 헤드리스 크롬)에서 잡은 것 둘:**

- **카드가 배경보다 어두웠다.** Material 3의 다크 램프는 `-lowest`가 표면보다 어둡지만 이 앱은 그 토큰을 **카드 바탕**으로 쓴다 — 낮에는 크림 위 흰 카드라 떠 보이는데 밤에만 가라앉아 본문 자리가 구멍처럼 파였다. 쓰임을 따라 방향을 맞췄다.
- **지도만 밝게 남았다.** 화면의 절반이 지도라 밤에 눈이 부셨다. 우리 CSS 변수는 남의 캔버스 안까지 못 미친다 — `<Map colorScheme="FOLLOW_SYSTEM">`으로 지도에게 따로 말한다. 실측에서 타일이 실제로 어두워지는 것을 확인했다.

**브랜드 버튼 글자를 `--color-brand-ink`로 뺐다.** `on-surface`는 밤에 크림색으로 뒤집히는데 카카오 노랑은 남의 자산이라 그대로여서, 노랑 위 크림 글자가 1.2:1로 사라진다. 이 토큰은 어느 모드에서도 안 바뀐다.

**`tokens.test.ts`에 다크 대비 11쌍 + 뒤집힌 히트맵 램프 + 완결성 검사를 더했다.** 마지막 것이 중요하다 — `src`를 훑어 「화면에서 쓰는 색인데 다크 블록에 없는 것」을 찾는다. 새 색을 쓰기 시작하면 거기서 죽는다. 변이 5개로 확인했다(override 삭제·램프 반전·brand-ink 뒤집기·배지 대비 파괴·소스 못 읽게 하기).

**다크 히트맵은 방향이 뒤집힌다.** 라이트에서는 붐빌수록 어두워지지만 어두운 배경에서 같은 방향이면 「붐빔」이 배경에 잠긴다. 등비 간격은 라이트와 같은 방법으로 역산했다(1.68 / 1.70 / 1.68).

### 언어지원 — 아직 안 했다

사용자가 영어를 골랐다. **설계까지만 하고 손대지 않았다.** 이유는 크기다: 화면에 보이는 한국어 문자열이 고유 167개, 파일 51개다. 절반만 옮기면 영어 모드에서 한국어와 영어가 섞여 안 하느니 못하다.

**옮길 때 쓸 방법:** 한국어 문자열 자체를 키로 쓴다(`t('붐빔')`). 그러면 (1) 키를 새로 짓지 않아도 되고 (2) 기본값이 한국어라 **기존 테스트 900여 개가 그대로 통과한다.**

`t()`는 훅이 아니라 **모듈 함수**여야 한다. 훅으로 만들면 51개 파일마다 배선을 손으로 넣어야 해서 위험한데, 모듈 함수면 import 한 줄 + 문자열 감싸기라 기계로 옮길 수 있다. 언어 상태는 `favoritesStore.ts`와 같은 모듈 스코프 스토어에 두고, 앱 루트가 `useSyncExternalStore`로 구독해 언어가 바뀌면 트리 전체를 다시 그린다.

**나눠야 할 것:** 한국어 문자열 중 상당수가 **API 값**이라 번역 대상이 아니다 — `CongestionLevel`(여유/보통/약간 붐빔/붐빔), `AreaCategory`, 대기질 등급(좋음/보통/나쁨/매우나쁨), 도로소통 지수. 이것들은 도메인에 한국어로 남고 **화면에서만** 번역한다.

**끝까지 못 가는 것:** 서울 API가 주는 자유 문장(`message`), 행사 이름, 주차장·역 이름은 한국어로만 온다. 혼잡도 안내 문장은 우리가 단계별로 영어 문장을 새로 써야 한다.

**완결성을 잠글 방법:** `src`를 훑어 (1) `t()`에 넘긴 문자열이 전부 영어 사전에 있는지, (2) `t()`로 안 감싼 한국어 리터럴이 남아 있는지를 검사한다. 둘째가 섞임을 막는 자리다.

## 요약 칩과 오늘 표시, 그리고 지도가 사라지던 버그 (2026-08-14)

### 상단 요약 칩 줄

샘플의 「주차 45% · 정체 · 행사 12 · 따릉이 131대」 자리다. 도시정보가 자동 조회로 바뀌면서 **추가 호출 0으로** 가능해졌다 — 이미 받아 둔 응답을 다시 셀 뿐이다.

샘플과 갈린 두 가지:

- **누르면 그 절로 간다.** 상세가 통째로 펼쳐지며 매우 길어졌는데(실측 스크롤 높이 2,486px) 칩이 요약만 하면 값을 확인하러 한참 스크롤해야 한다. 화면과 함께 **포커스도** 옮긴다.
- **순서가 아래 절과 같다**(도로·지하철·주차·따릉이·행사). 샘플은 주차가 맨 앞이지만 그쪽 칩은 누를 수 없어 순서가 목차일 필요가 없다.

세는 규칙에서 갈린 자리: 면수를 **모르는 주차장은 분모에서도 뺀다**(분자에서만 빼면 300면 중 45면 → 15%가 되는데 아는 것 중에는 45%가 비어 있다). 셀 수 있는 것이 없으면 `null`이다 — 0%로 접으면 「자리가 하나도 없다」는 정반대 뜻이 된다. 지하철은 열차가 아니라 역·호선을 센다.

### 히트맵 — 24칸으로 안 간다

**샘플은 7×24인데 우리는 7×8이다. 따라가지 않는다.** 그쪽은 서버에 과거를 쌓아 「표본 265일간」을 그리지만, 우리는 서울 API가 과거를 안 줘서 이 기기에서 상세를 열 때마다 한 칸씩 쌓는다. 56칸도 다 채우려면 56번을 서로 다른 요일·시간대에 열어야 한다 — 168칸으로 늘리면 표가 세 배로 비고 「관측 없음」이 화면의 거의 전부가 된다. 390px에서 24열이면 한 칸이 12.5px이기도 하다.

서버 수집이 생기면(PLAN.md 4차) 그때가 맞다. 바꿀 자리는 `PATTERN_BUCKET_HOURS` 하나다.

**대신 진짜 결함을 고쳤다: 어느 줄이 오늘인지 알 수 없었다.** 지금 시간대 **열**은 짚어 주면서 오늘 **행**은 안 짚어서, 「지금 이 시각」이 일곱 요일에 걸쳐 강조돼 표 안에서 제 위치를 못 찾았다. 이제 행과 열이 만나는 한 칸에 굵은 테두리가 간다(`ForecastChart`의 「지금」 막대와 같은 규칙 — 채움은 이미 혼잡도를 말하므로 다른 채널을 쓴다).

### **`scrollIntoView`가 지도를 화면 밖으로 보냈다**

칩을 누르면 절로 가게 만들면서 처음엔 `target.scrollIntoView()`를 썼다. 실측(390×844)에서 **지도가 통째로 사라졌다.**

원인: **`scrollIntoView`는 스크롤 가능한 조상을 전부 거슬러 올라가며 스크롤한다.** 시트 내용 상자(의도한 것, 2037px)와 **홈 화면 루트가 함께 524px** 밀렸다. 루트의 자식은 전부 `absolute`라 함께 위로 올라갔고 지도가 잘려 나갔다.

**루트에는 이미 `overflow-hidden`이 걸려 있었는데도 그랬다.** `overflow: hidden`은 눈에만 감출 뿐 그 요소를 **여전히 스크롤 가능한 상자로 둔다** — JS가 `scrollTop`을 넣거나 `scrollIntoView`가 거슬러 오면 그대로 밀린다.

두 겹으로 고쳤다:

1. **스크롤할 상자를 직접 지목한다.** `data-sheet-content`를 찾아 `scrollTo`로 그 상자 하나만 민다. 조상은 건드리지 않는다.
2. **루트를 `overflow: clip`으로 바꿨다**(`index.css`의 `clip-strict` 유틸리티, `hidden`을 폴백으로 함께 적는다). `clip`은 스크롤 메커니즘 자체를 안 만들어 **프로그램으로도 못 민다.** 실측으로 확인했다 — `root.scrollTop = 500`을 넣어도 0으로 남는다.

**이건 잠재된 결함이었다.** 내 코드가 아니어도 브라우저의 포커스 자동 스크롤이나 보조기술이 같은 일을 할 수 있었다. 지금은 경로 자체가 막혔다.

한 클래스로 묶은 이유: `overflow-hidden`과 `overflow-clip` 유틸리티를 나란히 쓰면 어느 쪽이 이기는지가 클래스 순서가 아니라 Tailwind의 생성 순서에 달려 불확실하다.

### jsdom에 없는 것은 테스트 설정에서 채운다

`matchMedia`·`scrollIntoView`가 jsdom에 없다. **컴포넌트에 `?.`를 다는 대신 `src/test/setup.ts`에서 채운다** — 실제 브라우저에는 둘 다 있으므로, 없는 경우를 대비한 분기를 제품 코드에 두면 어떤 테스트로도 못 죽이는 죽은 코드가 된다.

## 다크 모드는 지원이지 기본이 아니다 (2026-08-14)

사용자 지적: **「다크모드 지원이지 다크모드가 기본은 아닌데」.** 맞다. 처음 구현은 `@media (prefers-color-scheme: dark)`였고, 그러면 폰이 어두운 사용자에게 다크가 **기본**이 된다 — 지원한 것이 아니라 기본으로 삼은 것이었다. 이 앱의 얼굴은 밝은 크림색이고 스크린샷·심사·처음 여는 화면이 전부 그쪽이다.

**기본은 밝게다**(`domain/theme.ts`의 `DEFAULT_THEME`). 세 값을 둔다 — 밝게 / 어둡게 / 기기 설정. 기기를 따르고 싶으면 「기기 설정」을 고르면 되고, 그건 **고른 것이라 기본이 아니다.**

### CSS가 아니라 JS가 기기를 읽는다

`index.css`는 이제 `[data-theme='dark']` 속성만 본다. CSS가 `prefers-color-scheme`를 직접 보면 「시스템을 골랐을 때만 기기를 본다」를 표현할 수 없어 **다크 블록을 두 벌로 적게 되고, 두 벌이 되는 순간 갈린다.** `hooks/themeStore.ts`가 셋을 하나로 합쳐 `data-theme` 한 글자로 내보낸다.

저장은 `platform/theme.ts`(토스 Storage → localStorage 폴백, `favorites.ts`와 같은 모양). **저장하는 값은 해석 결과가 아니라 고른 값이다** — 「기기 설정」을 `dark`로 저장하면 다음에 기기를 밝게 바꿔도 앱이 어두운 채로 남는다.

### 함께 옮겨야 했던 둘

- **주소창 색**(`index.html`의 `theme-color`). `media`로 두 벌 두던 것을 한 벌로 줄이고 `apply()`가 함께 고친다. 안 그러면 폰이 어두운 사용자가 앱을 밝게 쓸 때 **주소창만 검게** 남는다.
- **`color-scheme`**. `light dark`는 「기기를 따르라」는 뜻이라 같은 상황에서 **스크롤바와 입력창만 검게** 남는다. `:root`는 `light`, 다크 블록에서 `dark`로 덮는다.

### 실측 (390×844, 기기를 어둡게 두고)

| | 앱 | 배경 | 주소창 | colorScheme | 저장값 |
| --- | --- | --- | --- | --- | --- |
| 첫 진입 | `light` | `#fffbf4` | `#fffbf4` | light | 없음 |
| 어둡게 누름 | `dark` | `#16120c` | `#16120c` | dark | `dark` |
| 다시 밝게 | `light` | `#fffbf4` | `#fffbf4` | light | `light` |

### 설정 자리는 교환이다

「오늘의 서울」 맨 아래에 뒀다. 이 앱에는 설정 화면이 없고, 지도 위 오버레이나 목록 머리에 두면 하루에 한 번 쓸까 말까 한 것이 상시로 자리를 먹는다. **대신 눈에 잘 안 띈다** — 실사용에서 「다크 모드가 없다」는 말이 나오면 그때 자리를 올리는 것이 맞다.

**첫 페인트에는 `data-theme`이 없어 라이트로 그려진다.** 기본이 밝게라 대부분은 깜빡임이 없고, 어둡게를 저장한 사용자만 잠깐 밝은 화면을 본다 — 저장소 읽기가 비동기(토스 브리지)라 피할 수 없다.

### 변이에서 배운 것

「밝게를 고른 사용자는 기기가 바뀌어도 그대로다」 테스트가 **`watchSystemTheme`의 `setting === 'system'` 가드를 못 죽였다.** `apply()`가 이미 `resolveTheme`로 고른 값을 존중하므로 가드가 없어도 칠해지는 색이 같기 때문이다. 가드의 실제 값은 다른 데 있다 — 밝게·어둡게를 고른 사용자에게 기기 변화는 아무 일도 아닌데 `emit()`하면 트리 전체가 까닭 없이 다시 그려진다. 그 사실을 재는 테스트를 따로 뒀다.

## 주차장·따릉이를 지도에서 짚는다 (2026-08-14)

사용자 지적: 「서울 인파레이더는 따릉이나 주차장 우측에 아이콘이 있고 바로 지도에서 이동해서 위치를 알려준다.」

### 좌표는 이미 오고 있었다 — 우리가 안 읽었을 뿐이다

**`docs/fixtures/citydata-광화문덕수궁.json`(실응답)으로 확정했다.** 명세 엑셀만 봤다면 틀렸을 자리다:

| | 필드 | 타입 | 비고 |
| --- | --- | --- | --- |
| 주차장 | `LAT` / `LNG` | 문자열 | 이름 그대로 |
| 따릉이 | `SBIKE_X`(경도) / `SBIKE_Y`(위도) | **숫자** | **축 이름이 위경도와 반대** |

엑셀 명세에는 `SBIKE_X`·`SBIKE_Y`만 있고 `PRK_X`/`PRK_Y`가 없어서 「주차장은 좌표가 없다」로 결론 낼 뻔했다. 실응답에는 `LAT`/`LNG`로 있다. **`ADDRESS`·`ROAD_ADDR`·`CUR_PRK_TIME`도 함께 온다** — 마지막 것은 「최대 3시간 전 기준」이라는 뭉뚱그린 문구를 실제 관측 시각으로 바꿀 수 있다는 뜻이다(아직 안 했다).

**X/Y 축은 뒤집기 쉬운 자리라 테스트로 잠갔다.** 뒤집으면 위도가 126이 되어 지도가 중국 어딘가로 간다. `coordsOrNull`이 범위(위도 ±90, 경도 ±180)와 `0,0`을 거른다 — `0,0`은 서아프리카 앞바다라 좌표를 못 채운 행이 대서양으로 날아가는 것을 막는다.

### 시트가 내려오는 것이 핵심이다

아이콘은 도시 정보 절에 있어 **사용자는 거의 언제나 시트를 full로 올린 채 누른다.** 지도만 옮기고 단계를 그대로 두면 화면에 아무 변화가 없다 — 누른 보람이 하나도 안 나타난다. 그래서 `showFacilityOnMap`이 `setDetent('half')`를 함께 부른다. 변이로 확인했다.

줌은 17이다(명소는 15). 명소는 「이 동네」지만 주차장은 「이 건물」이라, 같은 줌이면 핀이 어느 골목인지 구별되지 않는다.

### 핀 색을 혼잡도에서 뺐다

`FacilityMarker`는 팔레트의 **강조색(파랑)**을 쓴다. 혼잡도 4색(초록·노랑·주황·빨강) 중 어느 것을 써도 「이 주차장이 붐빈다」로 잘못 읽힌다 — 이 앱에서 그 4색은 값이지 장식이 아니다. 파랑은 4색 어디에도 없어서 값과 헷갈리지 않는다.

명소를 갈아타면 핀을 지운다(`openArea`). 안 지우면 경복궁 상세를 보는데 지도에는 강남역 주차장 핀이 남는다.

### 좌표가 없으면 버튼을 아예 안 그린다

실응답에도 `LAT`/`LNG`가 빈 문자열로 오는 주차장이 있다. 눌러도 아무 일이 안 일어나는 버튼은 고장으로 보인다.

아이콘만 있는 버튼이라 접근성 이름에 **시설 이름을 앞에 붙인다** — 「지도에서 보기」만 적으면 한 화면에 같은 이름의 버튼이 열 개가 되어 스크린리더로 훑을 때 구별되지 않는다.

### 실측 (390×844)

시트를 전체로 올린 뒤 「홍대입구역(2호선) 1번 대여소 지도에서 보기」를 눌렀다 → 시트가 **전체 → 절반**(top 371), 지도가 줌 17로 그 좌표로 이동, 파란 이름표 핀이 시트 위쪽에 보인다.

## 시설에 거리를 붙이고 따릉이 정렬을 바꿨다 (2026-08-18)

샘플의 「광화문역 5번출구 **120m** 19대」 자리다. 이름만 있으면 「5번출구」가 걸어갈 만한 거리인지 알 수 없다. **앞서 넣은 시설 좌표가 여기서 값을 했다** — 좌표를 읽는 일과 쓰는 일이 하루 차이로 갈렸다.

### 기준점은 사용자 위치가 아니라 명소 중심이다

**고른 것이다.** 상세는 지금 있는 곳이 아니라 **가려는 곳**을 보는 화면이라, 부산에서 광화문을 열어도 「120m」가 뜻을 가져야 한다. 사용자 위치를 쓰면 그 숫자가 「320km」가 되어 아무 쓸모가 없고, 위치 권한 여부에 따라 있다 없다 한다.

### 따릉이와 주차장의 정렬 규칙이 갈렸다

| | 정렬 | 이유 |
| --- | --- | --- |
| 주차장 | **빈 자리 많은 순**(그대로) | 차로 간다. 200m 더 가까워도 만차면 무의미하다 |
| 따릉이 | **거리순**(바꿨다) | 걸어간다. 500m의 20대보다 **120m의 5대가 낫다** |

**거리만으로 세우면 맨 앞이 「가장 가까운데 못 빌리는 곳」이 된다.** 그래서 두 단계다 — 빌릴 수 있는 곳이 먼저, 그 안에서 가까운 순. 0대와 「모름」은 둘 다 헛걸음이라 같이 뒤로 민다.

명소 좌표를 모르면(카탈로그에 없는 이름) 기준점이 없으므로 예전 규칙인 대수순으로 떨어진다.

### 걸린 함정 하나

**좌표가 없는 시설을 `0m`로 접으면 안 된다.** 그러면 가장 가까운 곳으로 올라가 목록 맨 앞을 차지한다 — 정반대를 말하는 셈이다. `null`로 두고 정렬에서 맨 뒤로 민다. 변이로 확인했다(5개 전부 사망).

실측(390×844): `홍대입구역(2호선) 1번 대여소 · 350m · 거치대 18대 · 3대`.

## PC를 지원한다 — 시트가 왼쪽 패널이 된다 (2026-08-18)

사용자 지적: 「서울 인파레이더는 반응형이야 PC에서도 지원되도록. 현재 앱은 모바일만이고.」 맞았다. 1440×900 실측 결과는 **반응형이 아니라 그냥 늘어난 것**이었다:

| 요소 | 폭 | 증상 |
| --- | --- | --- |
| 목록 행 | 1,400px | 명소 이름과 혼잡도 배지가 **1.4m 떨어져** 눈이 못 잇는다 |
| 검색창 | 1,358px | 한 줄 입력이 화면을 가로지른다 |
| 정렬 3버튼 | 1,400px | 「거리순」이 왼쪽 끝, 「붐비는 순」이 오른쪽 끝 |
| 시트 | 하단 504px | 세로 비율로 잡혀 있어 PC에서는 위쪽 지도가 텅 빈다 |

원인은 `h-dvh`·시트 detent·`centerBelowSheet`가 전부 **세로가 긴 화면**을 전제로 짜였다는 것이다.

### 768px에서 시트가 왼쪽 패널(400px)로 바뀐다

사용자가 고른 안이다(구글맵·인파레이더와 같은 표준 패턴). **모바일 동작은 한 줄도 안 바뀐다** — 390px 실측에서 시트 y=371 h=473, 손잡이·FAB 위치 전부 그대로다.

- 검색·필터 칩이 **지도 위 오버레이에서 패널 안으로** 들어간다. 폭이 400px로 묶인다.
- 손잡이를 안 그린다. 패널은 늘 전체 높이라 접었다 펼 세로가 없다.
- FAB은 지도 우하단(`bottom-6`)에 붙는다 — 조건이 오버레이와 **다르다**. 검색은 패널로 들어가지만 「내 주변」은 지도를 움직이는 버튼이라 지도 위에 남아야 한다.

### 지도 보정의 축이 바뀐다 — 여기가 핵심이다

좁은 화면은 시트가 **아래**를 덮으므로 중심을 남쪽으로 밀었다(`centerBelowSheet`). PC는 패널이 **왼쪽**을 덮으므로 서쪽으로 민다(`centerRightOfPanel`).

**둘을 한 식으로 합치지 않았다.** 「비율」과 「픽셀 폭」이라는 서로 다른 단위를 억지로 묶게 되고, 나눠 두면 각각 검산할 수 있다. 경도 쪽은 훨씬 간단하다 — 메르카토르는 세로만 늘이므로 가로는 세계 픽셀 폭에 −180~180이 고르게 퍼진다(위도는 `latitudeToPixel` 왕복이 필요했다).

**패널 폭은 `PANEL_WIDTH_PX`(useWideScreen.ts)가 정본이다.** CSS의 `w-100`과 갈리면 지도가 어긋난다.

### 실측에서 잡은 결함 둘

- **첫 화면의 지도 중심이 패널을 고려하지 않았다.** 서울이 보이는 띠의 가운데가 아니라 왼쪽에 치우쳐 앉았다. 패널 폭 변화(0→400)를 차이로 미는 effect를 두어 첫 렌더와 리사이즈를 함께 처리한다 — 시트 비율 effect와 같은 구조다. 그래서 `centerRightOfPanel`은 **음수 폭도 받는다**(패널이 사라질 때 −400).
- **칩 줄에 가로 스크롤바가 드러났다.** 모바일은 오버레이 스크롤바라 안 보였는데, 데스크톱 브라우저에서는 자리를 차지한 채 상시로 보인다. `scrollbar-none` 유틸리티로 감췄다(터치·트랙패드·키보드 스크롤은 그대로).

### 남은 것

- **PC 상세에는 검색창이 없다.** 목록 뷰에만 있어서 상세를 보는 동안은 「목록으로」를 눌러야 한다. 모바일은 지도 위 오버레이라 상세에서도 검색이 보인다 — 의도한 차이지만 확인되진 않았다.
- **태블릿 세로(768×1024)가 경계에 딱 걸린다.** 패널 400 + 지도 368이라 아슬아슬하다. 실기기로 볼 자리다.

## 다크모드 토글을 앞으로 꺼냈다 (2026-08-18)

사용자 요청: 「서울 인파레이더와 같이 다크모드 토글 기능」. **인파레이더는 상단바에 둔다** — 우리는 상단바를 걷어냈으므로(세로가 가장 귀한 자원) **검색 줄 오른쪽 끝**이 그 자리다. 좁은 화면에서는 지도 위 오버레이, 넓은 화면에서는 패널 안 — **둘 다 검색과 같은 줄**이라 자리를 새로 먹지 않는다.

앞서 「설정이 「오늘의 서울」 맨 아래라 눈에 잘 안 띈다 — 그 말이 나오면 자리를 올리는 것이 맞다」고 적어 뒀는데, 그 말이 나왔다.

### 토글은 두 값, 설정은 세 값

버튼은 **「지금 반대로」**만 한다. 세 값(밝게/어둡게/기기 설정)을 한 버튼으로 돌리면 누를 때마다 다음이 무엇인지 알 수 없고 아이콘 하나로 세 상태를 그릴 수도 없다. 셋 중 고르는 일은 「오늘의 서울」의 `ThemeSetting`이 그대로 맡는다 — **자주 쓰는 조작은 앞에, 드물게 정하는 것은 설정에.**

**고른 값이 아니라 지금 칠해진 색을 봐야 한다**(`useResolvedTheme`). 설정만 보면 `'system'`은 밝은지 어두운지 알 수 없어, 어두운 화면에서 「어둡게 바꾸기」라고 말하게 된다 — 눌러도 아무것도 안 바뀐 것처럼 보인다.

아이콘은 **「지금」이 아니라 「누르면 무엇이 되는지」**를 그린다(밝을 때 달). 버튼은 결과를 약속하는 물건이라 이쪽이 덜 헷갈리고, 접근성 이름이 같은 말을 하므로 눈과 소리가 어긋나지 않는다.

### **테스트가 진짜 경쟁 조건을 잡았다**

토글 테스트 하나가 이유 없이 실패해서 파고들었더니 `themeStore`의 실제 버그였다:

**저장소 읽기가 비동기(토스 브리지)라, 앱이 뜬 직후 토글을 누르면 그 뒤 도착한 저장값이 방금 누른 것을 덮는다.** 사용자에게는 **화면이 바뀌었다가 저절로 되돌아가는** 것으로 보인다.

`favoritesStore`가 별에서 **같은 종류의 경쟁**을 겪고 같은 방법으로 막아 뒀다(`chosen` 플래그 = 그쪽 `touchedRef`). 비동기 읽기 + 사용자 조작이 있는 스토어를 새로 만들 때마다 이 함정이 있다고 봐야 한다.

### 지도 타일도 함께 뒤집어야 했다

`colorScheme="FOLLOW_SYSTEM"`이 남아 있었다. 테마가 기기를 따르던 시절에는 맞았지만 **사용자가 고르게 된 뒤로는 틀렸다** — 기기가 밝은데 앱을 어둡게 쓰면 **지도만 밝게 남는다**(390px 실측에서 실제로 그랬다). 이제 `resolvedTheme`을 그대로 넘긴다.

**설정을 사용자 손에 옮기면 「시스템을 보던 자리」를 전부 찾아 고쳐야 한다.** 지금까지 셋이었다 — CSS 블록, `theme-color`/`color-scheme` 메타, 그리고 이 지도.

## 영어를 지원한다 (2026-08-18)

화면 문자열 **155개**를 옮겼다. 설계는 앞서 적어 둔 그대로 갔다 — **키가 한국어 원문 자체**이고 `t()`는 훅이 아니라 모듈 함수다. 그래서 감싸기만 하면 한국어 화면이 한 글자도 안 바뀌어 **기존 테스트 1,000여 개가 그대로 통과**했고, 옮기는 도중에도 화면이 한 번도 깨지지 않았다.

### 설계에서 갈린 자리 넷

1. **`domain/`에서 `t()`를 부르지 않는다.** 스크립트가 도메인까지 감쌌다가 되돌렸다 — 도메인은 순수해야 하는데 `t()`는 모듈 상태를 읽고, `domain/types.ts`는 `api/`와 공유돼 **서버리스 번들까지 i18n을 끌고 간다**. 도메인은 한국어 값을 두고 화면이 감싼다.
2. **도메인이 표시 문구를 만들던 자리는 키와 값을 돌려주게 바꿨다.** 요약 칩이 「주차 45%」를 완성해 주던 것을 `{ label: '주차 {비율}%', labelParams: { 비율: 45 } }`로 바꿨다.
3. **값과 표시를 갈랐다.** 필터 값 `'전체'`를 감싸면 필터가 아무것도 못 찾는다 — **타입 시스템이 네 곳에서 잡아 줬다**(`CategoryFilterValue`).
4. **모듈 최상위 `t()`는 import 시점 언어로 굳는다.** 상수 표(요일·단계 이름)는 한국어 키만 담고 쓰는 자리에서 감싼다.

### 사전으로 못 푸는 것이 하나 있었다

**축 눈금.** 한국어는 만 단위(4.5만), 영어는 천 단위(45k)라 **문자열이 아니라 나눗셈이 다르다.** 사전은 치환만 할 뿐이라 `src/i18n/format.ts`로 갈랐다.

### 로케일 추측을 걷어냈다

처음엔 `navigator.languages`에 한국어가 없으면 영어로 시작하게 했다. **jsdom의 `en-US` 때문에 테스트 63개가 통째로 영어로 떠서 깨졌고**, 그게 기기를 영어로 쓰는 한국인에게 벌어질 일을 그대로 보여줬다. 기본은 언제나 한국어다.

대신 **언어 버튼을 검색 줄에 상시로 내놓는다.** 화면 테마와 달리 언어는 설정에 묻으면 안 된다 — 지금 읽을 수 없는 말로 적힌 메뉴를 헤쳐야 한다면 정작 그 기능이 필요한 사람이 못 찾는다. 버튼 글자는 「바뀔 언어」(`EN`/`한`)이고, **「한국어로 바꾸기」는 영어 화면에서도 한국어로 남는다.**

### 완결성을 셋으로 잠갔다 (`i18n.test.ts`)

| 검사 | 막는 것 |
| --- | --- |
| 사전 완결성 | `t()`에 넘긴 키가 사전에 없어 한국어로 떨어지는 것 |
| 역방향 | 문구를 고친 뒤 옛 키가 남아 쌓이는 것 |
| **섞임 방지** | 화면에 `t()`로 **안 감싼** 한국어가 남는 것 |

**셋째가 핵심이다.** 사전이 완전해도 감싸는 걸 한 군데 잊으면 그 자리는 영어 모드에서 한국어로 남고, 사용자는 「번역이 덜 됐다」가 아니라 「영어를 지원하지 않는다」로 읽는다. 실제로 이 검사가 **JSX 텍스트 노드 20곳**을 잡아냈다 — 「추정 인구 {n}명」처럼 값이 낀 자리라 스크립트가 못 건드린 것들이다.

### 번역할 수 없는 것은 남긴다

서울 API의 자유 문장(`message`·재난문자), 고유명사(주차장·대여소·행사 이름). 영어 화면에도 한국어가 남는 것은 결함이 아니다.

**명소 이름과 도로소통 지수는 이 목록에 있었지만 2026-08-18에 나왔다** — 아래 「영어가 절반만 되어 있었다」 참고.

## 영어가 절반만 되어 있었다 (2026-08-18)

사용자가 「영어가 일부 변경되지 않는다, 인사동 같은 타이틀이 그대로다」라고 짚었다. 파 보니 **원인이 넷**이었고 위 검사 다섯 개가 전부 초록인 채였다. **사전이 완전하다는 것과 화면이 그 사전을 읽는다는 것은 다른 말이다.**

| 원인 | 규모 | 왜 검사를 통과했나 |
| --- | --- | --- |
| 모듈 최상위 `t()` | 6개 파일 **19개 문자열** | 사전에 있고 `t()`로 감싸기까지 했다. import 시점에 값이 굳을 뿐 |
| 명소 이름에 영어가 없음 | 30곳 | 애초에 사전에 넣을 대상이 아니었다 |
| 도메인 값을 화면이 안 감쌈 | 연령대 8칸 · 거주 알약 · 도로소통 | 키는 변수라 정적으로 못 센다. `dynamicKeys()`가 「쓴다」고 **선언**만 하고 있었다 |
| 도메인이 완성된 글자를 만듦 | 시간대 날씨 24칸 | `forecastHourLabel`이 「14시」를 지어 돌려줬다 — 도메인은 언어를 모른다 |

### 굳은 상수 19개 — 가장 컸다

정렬 줄·화면 테마·지도 앱 버튼(KakaoMap·Naver·TMAP)·위치 안내·평소 대비 문구·지도 실패 안내. **전부 사전에 영어가 있는데도 한국어로 떴다.** 「모듈 최상위에서 `t()`를 부르지 마라」는 규칙은 이미 적혀 있었지만 사람이 지키는 규칙이었다.

**이제 기계가 잠근다** — `i18n.test.ts`의 「모듈 최상위에서 t()를 부르지 않는다」가 화면 파일을 **TypeScript 파서로 읽어** `t()` 호출을 감싸는 함수가 하나도 없으면 죽는다. 정규식이 아니라 AST인 이유는 「함수 안인가」가 글자로는 안 보이기 때문이다. 처음 돌렸을 때 19개를 그대로 뱉었다.

### 명소 이름은 사전이 아니라 카탈로그에 넣었다

`AreaCatalogEntry.nameEn`이고 **선택 항목이 아니라 필수**다. 이유가 셋이다 — (1) 이름·좌표·코드가 한 줄에 있어야 121곳으로 늘릴 때 한 곳만 본다, (2) 완결성 검사는 `t()`에 **리터럴로** 적은 문자열을 세는데 명소 이름은 언제나 변수라 사전에 넣으면 「안 쓰는 항목」으로 잡힌다, (3) 필수라 빠뜨리면 **컴파일이 막힌다.**

**`name`은 그대로 한국어다.** 그건 서울 API 호출 키이자 카카오맵 검색어다. `areaDisplayName(entry)`가 화면용으로만 고른다.

**분류어는 뺐다.** 「Myeongdong Special Tourist Zone」은 바로 아랫줄의 카테고리(`Tourist zones`)와 같은 말을 두 번 하고, 그 길이 때문에 상세 제목이 잘렸다(390px 실측: 「Hongik Univ. Station (…」). 짧은 쪽이 화면에도 맞고 실제로 부르는 이름에도 가깝다. 상세 제목은 `truncate` → `line-clamp-2`로 바꿔 긴 이름도 안 잘린다 — 한국어 30곳은 전부 한 줄이라 달라지는 것이 없다.

**검색은 두 이름을 다 본다**(`searchAreas`). 화면 언어로 가르지 않는 것이 일부러다 — 안 그러면 **화면에 보이는 줄을 검색으로는 못 찾는** 경우가 생긴다.

### 도로소통은 키에 「도로」를 붙여 풀었다

`원활`이 혼잡도 헤드라인과 다투던 문제(위 참고)는 **접두어로 칸을 가르면** 끝난다 — `도로 원활` → `Traffic clear`. 대가는 한국어 칩이 2자에서 5자로 넓어지는 것이고, 칩 줄이 가로로 스크롤되므로 감당할 수 있다. 덤으로 「지하철 2」·「주차 50%」 옆에서 무엇에 대한 값인지가 분명해졌다.

값의 종류는 여전히 모른다. 모르는 값이 오면 `t()`가 키를 그대로 돌려줘 「도로 ○○」로 뜬다.

### 브라우저로 훑어서 확인했다

영어로 띄운 뒤 `#root`의 **텍스트 노드에서 한글을 전부 긁었다.** 눈으로 훑는 것과 달리 소리 전용 줄(`sr-only`)까지 잡힌다 — 혼잡예보 막대의 「20,000~22,000명」이 그렇게 나왔다. **화면으로는 영영 못 볼 자리다.**

남은 한글은 전부 고유명사이거나 서울 API 자유 문장이다.

### 지하철 절은 한 번 더 나왔다

처음에는 **일부러 남겼다** — 「3호선」은 옮길 수 있지만 옆의 「대화행」·「전역 출발」이 자유 문장이라 한 줄이 섞이면 통째로 한국어인 것보다 못하다고 봤다. **틀린 판단이었다.** 사용자가 「자세히 보기 페이지에 영어로 다 변경이 되지 않았다」고 다시 짚었고, 실제 화면을 보니 절 머리(`Subway arrivals`·`As of up to 3 hours ago`·`1 more`)만 영어이고 **안쪽 열두 줄이 전부 한국어**였다. 그건 「섞임」이 아니라 「번역 안 함」으로 읽힌다.

`src/i18n/subway.ts`가 **모양이 정해진 조각만** 옮긴다.

| 원문 | 영어 |
| --- | --- |
| `3호선` | `Line 3` |
| `대화행` | `To 대화` |
| `전역 출발` | `Left prev. station` |
| `9분 후 (동대입구)` | `in 9 min (동대입구)` |
| `4분 30초 후 (무악재)` | `in 4m 30s (무악재)` |
| `[24]번째 전역 (수원)` | `24 stations away (수원)` |

**규칙은 「아는 모양이 통째로 맞을 때만」이다.** 부분일치로 잡으면 「전역 출발 지연」이 「Left prev. station」이 된다. 처음 보는 문구는 손대지 않는다 — **실패해도 「틀린 영어」가 아니라 「한국어」인 것이 이 설계의 값이다.** 도메인이 이 필드를 파싱하지 않기로 한 규칙과 어긋나지 않는다: 그건 「분」을 뽑아 정렬하거나 색을 붙이는 것을 막는 규칙이고, 여기서는 뜻을 만들지 않는다.

**역 이름은 지어내지 않았다.** 서울 지하철 역명의 로마자 표기가 이 앱에 없는 데이터이고, 틀려도 테스트가 못 잡는다. 주차장·대여소·행사 이름과 같은 규칙이다.

**「행」으로 끝난다고 다 행선지가 아니다.** `상행`을 거르지 않아 「To 상」이라는 없는 말을 만들었고, **테스트에 그 결과를 기대값으로 적기까지 했다.** `Upbound`가 맞는 말일 가능성이 높지만 실응답에서 본 적이 없어 옮기지 않고 흘려보낸다.

### 남은 것

- 실기기에서 영어 화면의 줄바꿈을 안 봤다. 헤드리스로는 상세 제목·칩을 쟀다.
- 지하철역·주차장·대여소·행사 이름과 서울 API 자유 문장(`message`·`PCP_MSG`·`AIR_MSG`)은 영어 화면에서도 한국어다. **결함이 아니라 데이터가 없는 것이다.**
- `index.html`의 `<title>`은 한국어로 남는다 — 정적 파일이라 언어 스토어가 닿지 않는다.

## 재난문자를 홈에 내놓는다 (2026-08-18)

서울 인파레이더처럼 **홈 맨 위**에 재난문자를 세운다. 예전에는 「오늘의 서울」을 열어야 볼 수 있었고, 홈에는 요약 줄이 「재난문자 1건」이라는 **수만** 말했다.

### 캐시만 읽던 것이 문제였다

`useCachedCityAlerts`는 이미 받아둔 `cityInfo` 캐시에서만 경보를 긁는다. 「오늘의 서울」에는 맞는 규칙이다 — 사용자가 눌러서 들어온 화면이고 추가 호출 0이 그 화면의 존재 근거다. **홈에는 안 맞았다.** 앱을 열고 아무 명소도 안 누르면 캐시가 비어 있어서, **경보가 걸려 있어도 홈에 아무것도 안 떴다.**

`useCityAlerts`가 **한 곳**(`ALERT_SOURCE_AREA` = 광화문·덕수궁)의 `citydata`를 스스로 부르고, 상세에서 받아둔 캐시도 함께 모아 문구로 중복을 지운다.

### 총량이 안 늘어난다

| | 주기 | 하루 |
| --- | --- | --- |
| 30곳을 부르면 | 3시간 | **+240** — 한도 초과 |
| 한 곳을 부르면 | 3시간 | **+0** (최악 기준) |

그 명소가 예산의 「도시정보 최악 30곳」에 이미 들어 있어서, 30곳을 다 열어 본 날에는 같은 캐시 항목이라 한 번도 더 안 든다. 실제로 늘어나는 것은 **아무도 그 명소의 상세를 안 연 날**의 8회뿐이다.

### 한 곳으로 서울 전체를 덮는다고 단정하지 않았다

`LIVE_DST_MESSAGE`가 시 전역인지 자치구 단위인지 **명세에 없고, 실응답에서는 비어 있는 것만 봤다**(`docs/fixtures/`의 `LIVE_DST_MESSAGE: []`). 그래서 화면이 「서울의 재난문자 전부」라고 말하지 않는다 — **받은 문구를 그대로** 보여줄 뿐이다.

이게 안전한 이유는 **재난문자 본문이 스스로 지역을 밝히기 때문**이다(「[서울특별시] …」). 범위를 우리가 주장하지 않으면 틀릴 것도 없다.

### 건수가 아니라 본문이다

요약 줄의 「재난문자 2건」으로는 **우산을 챙길 일인지 대피할 일인지 알 수 없다.** 배너가 첫 건의 본문을 두 줄까지 보여주고, 나머지는 수로 말한 뒤 누르면 전체가 있는 「오늘의 서울」이 열린다.

그래서 **요약 줄에서 경보를 걷어냈다.** 같은 것을 두 줄이 말하면 스트립의 한 칸이 중복에 낭비되고, 배너가 이미 빨간데 바로 아래 줄까지 빨가면 무엇이 경보인지 흐려진다.

### 배너는 자기 여백을 갖는다

부르는 쪽에서 `<div className="px-4">`로 감쌌더니, 경보가 없는 날에도 **빈 div가 남아 세로 묶음의 `gap-3`이 12px을 먹었다** — 화면에는 아무것도 없는데 목록이 밀린다. 스스로 `null`이 되려면 바깥까지 자기 것이어야 한다(`AlertDigest`·`RankList`가 이미 `mx-4`를 갖는 이유와 같다).

**경보가 없는 날이 대부분이다.** 실응답에서도 비어 있었고, 목업도 5곳 중 1곳만 만든다.

### 남은 것

- **여러 건일 때의 실화면을 못 봤다.** 목업이 명소마다 같은 문구를 주어 중복 제거 후 언제나 1건이 된다. 「외 N건」 줄은 단위 테스트로만 확인했다.
- 경보 본문은 영어 화면에서도 한국어다 — 서울 API 자유 문장이다.

## 지도에 이름표를 단다 (2026-08-18)

서울 인파레이더처럼 **지도에서 곧바로 「거기가 어디인지」를 읽게** 했다. 예전에는 핀 색이 혼잡도만 말하고 이름은 목록에만 있어서, 지도를 보는 동안에는 어느 점이 어디인지 알 수 없었다.

### 줌 14부터, 고른 곳은 언제나

혼잡도 알약은 줌 12부터인데(`LABEL_MIN_ZOOM`) 이름표는 **두 칸 깊은 14다**(`NAME_MIN_ZOOM`). 이름표가 훨씬 넓기 때문이다 — 「약간 붐빔」은 네 글자인데 「Hongik Univ. Station」은 스무 자다. 줌 12에서 강남역·가로수길·압구정로데오거리·청담동 명품거리 넷은 약 66px 안에 들어오는데 이름표 하나가 이미 그만큼 넓다.

14는 조작과도 맞는다 — 「내 주변」이 14로 가고(`RECENTER_ZOOM`) 명소를 열면 15로 간다(`AREA_ZOOM`). **사용자가 한 곳을 보려고 한 순간부터** 이름이 붙고, 서울 전역을 훑는 기본 줌(11)에서는 지도가 조용하다.

**고른 마커는 줌과 무관하게 이름을 단다.** 목록에서 고르면 지도가 그리로 움직이는데, 그때 어느 핀이 방금 고른 것인지 말해 주는 표시가 핀 크기(`size-9` vs `size-7`)뿐이었다 — 옆 핀과 2px 차이라 사실상 안 보인다.

### 실측이 잡은 것: 핀이 18px 북쪽을 가리켰다

`AdvancedMarker`는 **내용물의 아래 끝 가운데**를 좌표에 맞춘다. 이름표를 핀 아래에 흐름으로 놓으면 그 아래 끝이 이름표 밑으로 내려가고 **핀이 그만큼 위로 밀린다.** 390×844에서 핀 하단이 186px → **168px**로 올라갔다(컨테이너 하단은 186 그대로). 줌 14를 넘나들 때마다 마커 30개가 18px씩 튀고, 고른 핀은 늘 북쪽으로 어긋난 자리를 가리킨다.

`absolute top-full`로 띄워 흐름에서 빼면 컨테이너의 아래 끝이 핀 끝 그대로다. 고친 뒤 다시 재서 186px을 확인했다.

**jsdom으로는 못 잡는다**(레이아웃이 없다). 그 18px을 만드는 클래스는 테스트가 잠근다 — `BottomSheet`의 히트 영역과 같은 처지다.

### 알약과 색을 나눠 갖지 않는다

혼잡도 4색은 값을 말하는 색이라 이름표까지 물들이면 「이 이름이 붐빈다」로 두 번 말하게 되고, 그러면 이름표는 알약을 더 크게 그린 것에 지나지 않는다. 이름표는 카드와 같은 표면색이다. 다크 모드에서도 지도 타일 위에서 읽힌다(실측 확인).

### 남은 것

- 겹침을 없애지는 못한다. 클러스터링이나 충돌 회피가 할 일이고, 30곳에서는 줌으로 표현을 바꾸는 것으로 충분하다고 봤다 — `LABEL_MIN_ZOOM`이 이미 같은 교환을 하고 있다.
- 이름표는 `max-w-32`에서 자른다. 「Haebangchon & Gyeongnidan-gil」은 지도에서 잘리고 전체 이름은 상세에 있다.

## 주소가 없어서 셋이 깨져 있었다 (2026-08-18)

「완성도를 위해 다음에 무엇을 할까」를 묻길래 코드로 막힘없이 되는 것을 훑다가 찾았다. `history.pushState`도 `URLSearchParams`도 저장소 전체에 **한 군데도 없었고**, 그 하나 때문에 셋이 함께 깨져 있었다.

| | 고치기 전 | 지금 |
| --- | --- | --- |
| 공유 | `"강남역 실시간 혼잡도 - 서울 라이브"` — **주소가 없어 받은 사람이 못 엶** | 문장 + `…?area=강남역` |
| 뒤로 가기 | 상세에서 누르면 **앱이 통째로 닫힘** | 목록으로 돌아옴 |
| 새로고침 | 보던 명소가 사라짐 | 그대로 |

**공유가 특히 나빴다.** 버튼이 있고 눌리고 공유 시트까지 뜨는데 받는 쪽에서 아무 일도 안 일어난다 — 없는 기능보다 있는 척하는 기능이 나쁘다.

설계 판단은 `AGENTS.md`의 라우팅 조항에 있다. 여기에는 **하다가 배운 것**만 적는다.

- **「목록으로」를 `history.back()`으로 만들지 않았다.** 오늘의 서울 → 강남역 → 「목록으로」에서 `back()`은 오늘의 서울로 가는데, 버튼에 적힌 글자가 「목록으로」라 거짓말이 된다. 기존의 의도된 동작(「여닫는 길이 대칭이다」)도 그대로 지킨다. 뒤로 가기의 몫은 브라우저·OS 버튼에 남긴다.
- **`vi.spyOn(window.history, 'pushState')`를 쓴다 — 구현을 보는 단언이지만 다른 길이 없다.** 처음에는 주소만 봤는데 `openArea`의 `pushSearch`를 지워도 **79개가 전부 통과했다**(「주소 맞추기」 effect가 `replace`로 같은 주소를 만든다). `history.length`로 바꿨더니 이번에는 이 파일이 통째로 돌 때만 실패했다 — 앞 테스트의 `back()`이 스택 중간에 세워 두면 다음 `pushState`가 앞쪽 칸을 잘라내 길이가 안 는다. push와 replace의 차이는 **오직 히스토리에만 남는다.**
- **jsdom에서 `history.back()`은 매크로태스크 한 번으로 안 끝난다.** `act` 안에서 `setTimeout(0)`을 기다리게 짰다가 두 테스트가 조용히 실패했다. `waitFor`로 **주소가 실제로 바뀔 때까지** 기다린다 — 화면을 기다리면 「주소는 그대로인데 화면만 어쩌다 맞았다」를 통과시킨다.
- **테스트 사이에 주소가 샜다.** 명소를 여는 테스트가 남긴 `?area=…`를 주소를 안 만지는 파일들이 물려받아 **18개가 한꺼번에 깨졌다.** 오염을 만든 파일이 아니라 당한 파일이 깨져서 원인을 찾기 어려웠다. `src/test/setup.ts`의 `beforeEach`가 전역으로 되돌린다.
- **`react-hooks`가 함수 선언 순서를 막는다.** `popstate` effect가 `applyRoute → showArea → moveMapTo → focusMapOn → sheetRatio`로 닿으면서, `const sheetRatio`가 아래에 있는 것이 오류가 됐다("Cannot access variable before it is declared"). 규칙이 맞다 — 함수 선언은 끌어올려지지만 `const`는 아니다. `mapUnavailableReason`부터 `sheetRatio`까지를 위로 옮겼다.
- **어떤 테스트로도 못 죽이는 분기를 하나 만들었다가 지웠다.** `routeFromSearch`에서 맨 앞 물음표를 `slice(1)`로 뗐는데, `URLSearchParams`가 이미 하는 일이라 그 줄을 없애도 전부 통과했다. 이 저장소가 `?.`를 두고 금지하는 것과 같은 것이다 — 돌연변이 테스트가 아니었으면 안 보였다.

**실제 크롬으로 확인했다**(390×844, 목업 모드). jsdom이 못 덮는 것이 둘 있었다: 앞으로 가기와 새로고침. 여섯 항목 전부 통과했다 — 공유 링크 진입 / 명소를 열면 주소가 따라옴 / 뒤로 가기 / 앞으로 가기 / 새로고침 / 모르는 이름은 목록으로.

**남은 것:** `VITE_PUBLIC_APP_URL`을 배포 도메인으로 채우는 일(`.env.example`에 설명이 있다). 안 채우면 공유 링크가 `location.origin`으로 떨어지는데, **토스 안에서 그 값이 남에게 열리는 주소인지 확인한 적이 없다.** Vercel 웹에서는 채우지 않아도 맞다.

## 지도를 안전영역까지 보낸다 (2026-08-18)

**안전영역 처리가 저장소에 한 줄도 없었다.** `viewport-fit=cover`도, `env(safe-area-inset-*)`도 0건이었다 — 노치 있는 기기에서 화면 전체가 레터박스였고 지도가 끝까지 안 갔다.

규칙은 `AGENTS.md`의 안전영역 조항에 있다. 여기에는 **하다가 배운 것**만 적는다.

- **크롬 145의 CDP에 `Emulation.setSafeAreaInsetsOverride`가 있다.** 노치를 흉내 내 실제로 잴 수 있다 — 이 저장소에서 안전영역을 만질 때 쓸 도구다. 393×852에 노치 59 / 인디케이터 34를 넣고 쟀다.

  | | 값 |
  | --- | --- |
  | 지도 레이어 | 0 → 852 (화면 전체) |
  | 오버레이 `padding-top` | 59px |
  | 검색창 상단 | 79px (노치 59 + 검색 바 자체 여백 20) |
  | 시트 배경 | 375 → **852** |
  | 시트 내용 | 399 → **818** (= 852 − 34, 인디케이터 위에서 정확히 멈춘다) |

- **그런데 그 도구가 `viewport-fit`을 재현하지 않는다.** `viewport-fit=cover`를 빼고 다시 쟀더니 `padding-top`이 **그대로 59px**이었다. 명세상 그 줄이 없으면 `env()`는 0인데 데스크톱 크롬은 무관하게 값을 준다. **즉 이번 실측은 「값이 0이 아닐 때 배치가 옳은가」까지만 증명한다** — 「이 줄이 없으면 0이 된다」는 실기기에서만 확인된다. 그래서 `src/app/viewport.test.ts`가 그 한 줄의 존재를 대신 지킨다.
- **`index.html`을 검사하는 테스트는 `node:fs`로 못 읽는다.** `src`가 브라우저용 tsconfig(`types: ["vite/client"]`)라 **실행은 되고 `tsc`만 깨진다** — 테스트가 초록인 채로 빌드가 막혀 원인을 찾기 어려웠다. `vitest.config.ts`의 `define`으로 주입한다(`__INDEX_CSS__`와 같은 방식).
- **시트의 아래 여백은 뿌리에 걸어야 한다.** 내용 상자에 걸면 「여백의 주인은 뷰다」 규칙(테스트가 잠근 결정)과 부딪치고, 배경이 인디케이터까지 안 내려가 아래에 색이 다른 띠가 남는다.
- **보안 훅이 정규식 `RegExp.prototype` 메서드 이름을 셸 실행으로 오탐한다.** 파일 쓰기가 막혔다. `String.prototype.match`로 바꿔 피했다 — 다음에 같은 걸 만나면 오탐이다.

**남은 것이었던 `apple-mobile-web-app-status-bar-style`은 2026-08-28에 닫았다** — 아래 절을 볼 것.

## 상태 표시줄 밑까지 (2026-08-28)

위 회차가 「남은 것」으로 남겨 둔 마지막 띠다. `apple-mobile-web-app-status-bar-style`을 `black-translucent`로 바꿔, iOS 홈 화면에 설치한 standalone에서도 지도가 상태 표시줄 자리까지 간다. 규칙은 `AGENTS.md`의 안전영역 조항에 붙였다. 여기에는 **하다가 배운 것**만 적는다.

- **`default`를 고른 근거가 그새 낡아 있었다.** `index.html`에는 「화면 맨 위 0~112px을 검색 바와 필터 칩이 차지해 시계와 겹친다」고 적혀 있었는데, 그 열에는 이미 `pt-safe`가 붙어 있어 검색 바가 상태 표시줄 **아래**에서 시작한다. 겹칠 수가 없다. 관찰은 그 시점에 맞았고 그 뒤 다른 커밋이 전제를 바꿨다 — **이 저장소에서 반복해 나오는 결함 모양이고, 컴파일러도 테스트도 못 잡는 층이다.**
- **바꾸기만 하면 고치기 전보다 나빠질 수 있었다.** 이 모드의 상태 표시줄 글자는 **항상 흰색**이고, `colorScheme='LIGHT'`인 구글 지도는 거의 흰 바탕이다. 「지도가 끝까지 간다」를 얻고 「시계가 안 보인다」를 무는 거래라, `status-bar-scrim`(그라디언트 그림자)을 짝으로 함께 넣었다. 지도 앱들이 같은 자리에서 같은 답을 쓴다.
- **그 그림자 높이에 상수를 보태면 안 된다.** `calc(env(safe-area-inset-top) + 8px)`처럼 쓰면 노치 없는 기기와 브라우저 탭에 이유 없는 검은 띠가 생긴다 — 그런 곳에서는 상태 표시줄이 우리 화면 위에 겹치지도 않는다. `env()` 그대로여야 값이 0일 때 높이 0이 되어 조용히 사라진다(`pt-safe`와 같은 성질).
- **jsdom은 클래스가 붙었다는 것까지만 잠근다.** `@utility status-bar-scrim`을 통째로 지워도 컴포넌트 테스트는 초록으로 남는다 — CSS를 적용하지 않기 때문이다. 그래서 `app/viewport.test.ts`가 `__INDEX_CSS__`로 유틸리티의 존재와 `env(safe-area-inset-top)` 사용까지 본다. `viewport-fit=cover`를 그 파일이 지키는 것과 같은 이유다.

**남은 것:** 실기기 확인. 데스크톱 크롬은 standalone 상태 표시줄을 흉내 내지 못하므로(`Emulation.setSafeAreaInsetsOverride`는 여백만 준다) **시계 대비는 iOS 실기기에서만 판정된다.** 모자라면 알파(0.32)를 올린다. 토스 웹뷰·모바일 사파리에는 해당 없다 — 거기서는 원래 지도가 끝까지 갔고, 이번 변경으로 달라지는 것도 없다.

## 문구와 문서를 실제와 맞췄다 (2026-08-28)

Task 4~7이 상세의 두 호출(`citydata_ppltn` + `citydata`)을 하나(`citydata`)로 합치면서 캐시 TTL이 3시간에서 1시간으로, 호출 방식이 「혼잡도 24회 + 도시정보 8회」에서 「상세 전체 24회」로 바뀌었다. 그런데 코드가 바뀐 뒤로도 `AGENTS.md`의 쿼터 표와 화면 문구 곳곳이 옛 숫자(3시간·8회·30곳)를 그대로 달고 있었다. 이번 회차는 **코드를 안 건드리고 문구·주석·문서만** 실제와 맞췄다.

- **「최대 3시간 전」류 문구를 「최대 1시간 전」으로 바꿨다.** `NearbyPanel`·`TrafficPanel`의 안내 문구 넷, `en.ts`의 대응 키(옛 키는 지우고 새 키를 더했다 — 이 저장소는 i18n 키가 한국어 원문 자체다), `AGENTS.md`·`freshness.ts`·`freshnessNote.ts`·`InfoSection.tsx`·`client.ts`·`areas.ts`의 서술 주석, 그리고 그 문구를 그대로 단언하던 테스트(`InfoSection.test.tsx`·`AreaDetailScreen.test.tsx`·`CctvSection.test.tsx`·`client.test.ts`·`freshness.test.ts`)까지 전부 짝을 맞췄다. **손 안 댄 것:** 인구 시간대비(1시간·3시간·한달 전)는 캐시 TTL과 무관한 별개 기능이라 그대로 뒀다 — `PopulationTrendCard`·`domain/populationTrend.ts`·`domain/pattern.ts`와 그 테스트들.
- **`freshness.ts`의 버킷(분/시간)은 TTL과 무관해서 그대로 뒀다.** `elapsed()`가 "방금/N분 전/N시간 전"을 가르는 기준(1분·1시간 문턱)은 캐시 상한이 얼마든 똑같이 쓰는 일반 시간 표기 규칙이다. TTL이 바뀌어 달라지는 것은 **모를 때 돌아가는 fallback 문자열**(`최대 N시간 전 기준이에요`)뿐이고, 그건 버킷 로직이 아니라 호출부가 넘기는 인자다.
- **`AGENTS.md`의 쿼터 표를 다시 짰다.** 「상세 혼잡도」·「도시정보」 두 줄이 「상세 전체」 한 줄(1시간·24회)로 합쳐졌고, 「명소 상세의 인파 변화」(SeoulRtd, 0회) 줄을 새로 더했다. 홈의 재난문자도 같은 TTL을 물려받아 3시간→1시간(8→24회)이 됐다.
- **「남은 위험은 상세 쪽이다」 문단의 숫자를 다시 셌다.** 30곳→41곳(24×41=984 ≤ 1000, 42곳부터 1,008로 한도를 넘긴다), 121곳이 다 열리면 3,872→2,904회. **바로 아래 「홈의 재난문자가 총량을 안 늘리는 이유」 문단이 이 30곳을 그대로 가리키고 있었다** — 위 문단만 41곳으로 고치고 이 문단을 안 고치면 문서 안에서 숫자가 서로 어긋나는 채로 남는다. 그래서 이 문단의 240→984회, 1,200→1,968회도 함께 고쳤다. 지시받은 범위 밖이었지만, 고치라고 받은 숫자를 바로 옆 문장이 되받아치고 있어서 같이 고치지 않으면 검토 지시를 절반만 따른 게 된다고 판단했다.
- **「상세도 SeoulRtd로 옮기는 것이 길이다」는 더 이상 계획이 아니다.** 인구수·예보·구성비는 공식 API만 준다 — SeoulRtd는 등급·좌표·분류뿐이라 상세를 통째로 옮길 수 없다. 남은 길은 TTL을 3시간으로 되돌려 121곳 전부를 예산 안에 넣고(8×121=968), 혼잡도 등급 한 칸만 인증키 없는 SeoulRtd로 덮어써 신선하게 유지하는 것이다. **대가는 관측 시각이 갈리는 것이다** — 등급은 방금 값, 인구수·예보·구성비는 최대 3시간 전 값이 되어 한 화면 안에 서로 다른 순간이 섞인다. 지금은 호출 하나가 한 순간을 보장하는데 그 성질이 깨진다.

### `citydata_ppltn` ↔ `citydata` 실호출 대조 (2026-08-27 14:55, 광화문·덕수궁·강남역·홍대 관광특구)

이번 병합 전체의 전제다 — 두 서비스가 진짜 같은 값을 주는지 실호출로 쟀다. 세 곳 모두 `AREA_CONGEST_LVL`·`AREA_PPLTN_MIN`/`MAX`·`PPLTN_TIME`·`REPLACE_YN`·`FCST_YN`·`FCST_PPLTN.length`(12)·`FCST_PPLTN[0]`이 전부 일치했다. 표본이 셋뿐이라 전수 검증은 아니지만, 이 값이 다르면 상세가 조용히 틀린 인구수를 보여주게 되므로 **측정한 사실로 남긴다.**

### 이번 회차에서 배운 것 — 코드가 아니라 태도

지난 몇 회차가 잡은 결함 셋을 한 줄로 꿰면 이렇다: **컴파일러도 테스트 스위트도 못 잡는 층이 있다.**

- **재난문자 캐시 사고.** `useCachedCityAlerts`가 읽는 캐시의 키와 모양이 상세 병합으로 함께 바뀌었는데(`['cityInfo', name]` + 파싱된 값 → `areaPayloadKey` + 원본 `AreaPayload`), 그 훅을 부르는 유일한 화면의 테스트가 훅 자체를 `vi.mock`으로 갈아 끼우고 있어서 실제 캐시 읽기 로직은 어디서도 실행되지 않았다. 1,736개 테스트가 전부 초록인 채로 조용히 깨져 있었다.
- **`select` 참조 재파싱.** TanStack Query 5의 `select`는 값이 아니라 **레퍼런스**가 같을 때만 메모한다(`options.select === this.#selectFn`, `node_modules`의 v5.101.4에서 직접 확인). 인라인 화살표를 넘기면 데이터가 안 바뀌어도 렌더마다 `parseCitydataResponse`가 다시 돈다 — 상세가 홈의 바텀시트 안에 있어 지도를 움직이거나 시트를 드래그할 때마다 리렌더되는 화면이라 실제로 자주 일어난다. `useCallback`으로 참조를 고정해 막았다(`useAreaSnapshot`·`useCityInfo`).
- **`fetchAreaCongestion`이 무방비였다.** 잘 테스트되던 옛 `fetchAreaSnapshots`(모의 모드·부분 실패·URL 정규화까지 여섯 테스트)가 죽은 코드로 지워지면서, 전체 혼잡도의 유일한 남은 경로(`fetchAreaCongestion`·`useAreaCongestion`·`parseHotspotsResponse`)는 어느 소비 화면에서도 직접 테스트되지 않은 채 남아 있었다 — 전부 `vi.mock('../data/queries', ...)`로 갈아 끼워졌다. 코드 리뷰가 잡았고, 18개 테스트를 새로 붙였다(`a01a930`).

**셋의 공통점.** 하나는 「같이 바꿔야 할 두 곳 중 한 곳을 못 바꿨다」, 하나는 「라이브러리의 동작을 문서가 아니라 짐작으로 알고 있었다」, 하나는 「죽은 형제 코드를 지우면서 살아남은 코드의 커버리지를 안 봤다」다. 셋 다 `tsc -b`도 `npm test`도 초록이었다 — **테스트가 통과한다는 것과 그 테스트가 실제로 그 자리를 지킨다는 것은 다른 말이다.**

**참고로 `findSeededSnapshot`은 이번에도 여전히 죽어 있다.** 목록 출처가 SeoulRtd로 간 2026-08-20부터 `['areas', ...]` 캐시를 아무도 안 채워서 항상 `undefined`를 돌려준다 — 되살리는 것은 계획서 Task 8의 몫이고 아직 실행 전이다. AGENTS.md 참고.

## 「최대 3시간 전」이라는 반쪽 거짓말 (2026-08-18)

도시정보 세 절(지하철·주차장·따릉이)이 **방금 받은 값에도** 「최대 3시간 전 기준이에요」라고 적었다. 그 문구는 프록시 캐시 TTL에서 온 **상한**이라 절반은 거짓말이었다. 이제 `Age` 헤더로 실제 경과를 적는다 — 「방금 받은 값이에요」 / 「12분 전 값이에요」 / 「2시간 전 값이에요」.

설계 규칙은 `AGENTS.md`의 `freshness` 조항에 있다. 여기에는 **하다가 배운 것**만 적는다.

- **옛 계획서(「다음 후보」 4번)가 두 군데 틀려 있었고, 그걸 먼저 확인한 것이 이 작업의 절반이었다.**
  - 「실응답에 `CUR_PRK_TIME`이 함께 온다」 → 필드는 오지만 **값이 빈 문자열**이다(`docs/fixtures/citydata-광화문덕수궁.json:154,172,190`). 채우고 있던 것은 목업(`mockCityInfo.ts:152`)뿐이었다. **픽스처를 열어 보지 않고 「필드가 있다」만 확인하면 이렇게 된다.**
  - 「`Age`를 화면까지 나르면 된다」 → 그전에 **클라이언트가 아예 못 읽는다.** 오리진이 달라 `Access-Control-Expose-Headers`가 필요했다.
- **그 CORS 가정을 실제 크롬에서 쟀다.** 노출 헤더를 켠 서버와 끈 서버를 나란히 두고 교차 출처로 `fetch` → `742` vs `null`. **알고 있는 것과 확인한 것은 다르다** — 이 기능 전체가 그 한 줄에 얹혀 있어서 재 봤다.
- **「모르면 모른다고 한다」가 이 기능의 안전장치다.** `Age`를 못 읽을 때 0으로 떨어뜨리면 3시간 묵은 값이 「방금」이 되어 **고치기 전보다 나빠진다.** 그래서 `elapsed()`가 `unknown`을 주고 화면이 예전 문구로 돌아간다 — 덕분에 프록시 재배포보다 클라이언트가 먼저 나가도 화면이 안 나빠진다.
- **`Age`만으로는 부족했다.** 도시정보 `staleTime`이 30분이라 응답이 클라이언트 캐시에 그만큼 더 앉아 있다. `receivedAt`을 함께 안 들면 42분 묵은 값에 「12분 전」이라 적는다 — 고치려던 반쪽 거짓말을 크기만 줄여 되풀이하는 셈이다.
- **`CityInfo.freshness`를 필수로 뒀더니 컴파일러가 만드는 자리 여섯을 다 잡아냈다**(`nameEn`과 같은 판단). **단 하나, `as unknown as`로 캐스트하는 픽스처는 못 잡았다** — `HomeScreen.test.tsx`가 그랬고 화면이 `undefined.receivedAt`으로 터졌다. 캐스트를 쓰는 픽스처는 손으로 맞춰야 한다.
- **주어를 뺐다.** 예전에는 「잔여 면수는」·「거치 대수는」처럼 주어를 붙였는데, 시각이 뭉뚱그려져 있어 **무엇에 걸리는 말인지** 밝힐 필요가 있었기 때문이다. 시각이 정확해진 지금은 바로 위 제목이 그 일을 한다 — 주어까지 살리면 단위 셋 × 주어 셋으로 사전 항목이 아홉 개가 된다.
- **음수 방어를 시험한 첫 테스트가 가짜였다.** `ageSeconds: 0`으로 두고 시계를 뒤로 돌렸는데, 음수를 그대로 더해도 합이 1분 미만이라 어느 쪽이든 「방금」이 나왔다. 나이가 있는 채로 시계가 뒤로 가야 차이가 드러난다 — 돌연변이가 살아남아서 알았다.

**실제 크롬으로 확인했다**(390×844, 목업 모드, 공유 링크로 상세 진입). 세 절이 전부 「방금 받은 값이에요」로 뜨고 예전 문구는 화면에 없다.

**아직 확인 안 된 것:** Vercel CDN이 캐시 적중에 `Age`를 실제로 붙여 주는가. 표준 동작이고 `setCacheHeaders`가 `s-maxage`를 심지만 **실배포에서 재 본 적이 없다.** 안 붙으면 세 절이 조용히 예전 문구에 머문다(거짓말은 안 한다). **확인법:** 배포 후 `curl -sI https://<도메인>/api/cityinfo?area=광화문·덕수궁 | grep -i age`를 시간을 두고 두 번.

## CCTV 출처를 찾았고 붙였다 (2026-08-19)

**2026-08-13의 「쓸 만한 출처가 없다」는 결론이 틀렸다.** 사용자가 「서울인파레이더에 CCTV가 있다」고 짚어 줘서 다시 봤고, **서울시가 스스로 만든 웹(SeoulRtd)의 자바스크립트를 열어 보는 것만으로** 30분 만에 나왔다. 그때 뒤진 넷(`citydata` 명세·안심이·TOPIS·ITS)은 **각각은 사실이었지만 전부 엉뚱한 곳**이었다. **경쟁 서비스가 하고 있는 것이 보이면, 그 서비스의 프런트엔드부터 열어라** — 명세를 아무리 읽어도 안 나온다.

### 엔드포인트

```
GET https://data.seoul.go.kr/SeoulRtd/api/cctv?hotspotNm=<명소명>
→ [{ src, STRMID, XCOORD(경도), YCOORD(위도), CCTVNAME }]
```

- **키가 우리 것과 같다.** `areas.ts`의 `name`을 그대로 넣으면 된다 — `광화문·덕수궁`처럼 중점이 든 이름도 통했다.
- **인증키가 없다 → 하루 1,000회 쿼터를 1원도 안 쓴다.** 지금 960/1,000인 표가 그대로 있어도 된다.
- **HTTPS다.** `openapi.seoul.go.kr:8088`과 달리 mixed content 문제가 없다.
- **다만 세션 게이트가 있다.** `?hotspotNm=`을 붙인 `/SeoulRtd/map`을 먼저 받아 `JSESSIONID`를 얻어야 하고, 아니면 **302**다(본문 없이 조용히). `hotspotNm` 없이 부트스트랩하면 쿠키는 받아도 302다 — 실제로 여기서 한 번 헤맸다. `Referer`와 `X-Requested-With: XMLHttpRequest`도 함께 보냈다. **즉 클라이언트가 직접 못 부른다** — 이유는 다르지만 결론은 프록시로, `api/`가 할 일이다.

### 영상

원본은 TOPIS가 아니라 **UTIC 도시교통정보센터**다. `http://210.179.218.5x:1935/live/NN.stream/playlist.m3u8` — **평문 HTTP만 열려 있다**(https는 연결 자체가 안 된다: `HTTP 000`). 8088과 똑같은 벽이다.

**그런데 서울시가 HTTPS 프록시를 이미 열어 뒀다.**

```
https://data.seoul.go.kr/SeoulRtd/cctv/proxy?src=<urlencoded m3u8>
→ Content-Type: application/vnd.apple.mpegurl, Access-Control-Allow-Origin: *
```

chunklist URL까지 프록시 경로로 다시 써서 준다. 실제로 받아 확인했다(`#EXTM3U` … `1280x720`).

**재생은 iframe이 아니다.** SeoulRtd의 플레이어(`/SeoulRtd/cctv`)는 **hls.js + `<video>`**이고, 자기 지도에서 그걸 iframe으로 감쌀 뿐이다. **우리는 그 iframe을 안 쓰면 된다** — 앱인토스 금지 조항(`AGENTS.md`)에 안 걸린다. 「iframe 금지라 영상은 어차피 범위 밖」이라던 옛 문장은 **플레이어와 그 포장을 혼동한 것**이다.

### 커버리지 — 30곳 중 19곳 (실측, 2026-08-19)

30곳을 전부 불러서 셌다.

| | 곳 |
| --- | --- |
| **스트림 있음 (19)** | 명동 7, 광화문·덕수궁 5, 월드컵공원 3, 강남역·북촌한옥마을·창덕궁·종묘·잠실 관광특구·영등포 타임스퀘어 각 2, 경복궁·서울숲공원·남산공원·어린이대공원·잠원한강공원·가로수길·인사동·서촌·압구정로데오거리·청담동 명품거리·해방촌·경리단길 각 1 |
| **위치만, 스트림 없음 (1)** | 반포한강공원 |
| **아예 없음 (10)** | 홍대입구역(2호선), 여의도한강공원, 뚝섬한강공원, 성수카페거리, DDP, 이태원 관광특구, 잠실한강공원, 연남동, 광장(전통)시장, 북서울꿈의숲 |

**`src`가 빈 문자열인 항목이 섞여 온다**(위치는 있고 스트림은 없다). 거르지 않으면 검은 화면이 뜬다.

**빈 결과가 「CCTV 없음」인지 「이름이 틀림」인지 구분되지 않는다.** 광장(전통)시장이 0건인데, 괄호 이름 공백 문제(위 「명소 이름의 공백」 미해결 항목)와 같은 자리라 둘이 겹친다. **붙이기 전에 이 하나는 갈라야 한다.**

### 붙였다 (2026-08-19)

사용자가 범위를 「영상까지, 서울시 프록시 사용」으로 정했다. 영상 대역폭은 서울시 프록시가 지므로 우리 Vercel 함수로는 **JSON만** 흐른다 — 지금의 중계 가정이 그대로 유지된다.

| 자리 | 파일 |
| --- | --- |
| 상류 중계(세션 부트스트랩 + `src` 재작성) | `api/_lib/seoulRtd.ts` |
| 엔드포인트 | `api/cctv.ts` · 개발 서버는 `vite.config.ts` |
| 관대한 파서 | `src/data/cctvSchema.ts` |
| 플레이어(hls.js + `<video>`) | `src/components/cityinfo/CctvPlayer.tsx` |
| 절(한 번에 한 대) | `src/components/cityinfo/CctvSection.tsx` |

**하다가 배운 것:**

- **`canPlayType`이 거짓말을 한다.** 크롬이 HLS를 못 틀면서 `"maybe"`를 준다. 처음에 그걸 믿고 짰더니 실제 크롬에서 `readyState 0`인 채 검은 화면이었다 — **jsdom 테스트 8개가 전부 초록인 채였다.** 규칙은 `AGENTS.md`의 「HLS는 `canPlayType`으로 판단하지 마라」에 옮겼고, 잡아 두는 테스트는 `MSE가 있으면 canPlayType이 된다고 해도 src를 직접 걸지 않는다`이다. **실제 브라우저로 열어 보지 않았으면 안드로이드에서만 깨진 채 나갔다.**
- **부트스트랩에 `hotspotNm`을 안 실으면 쿠키는 받아 오는데 목록이 302다.** 「쿠키가 있으니 세션이 잡혔다」고 착각하기 딱 좋다.
- **한 번에 한 대만 튼다.** 명동이 7대라 다 틀면 스트림 일곱 개가 동시에 흐른다.
- **hls.js 설정은 서울시 것을 베꼈다**(`maxBufferLength: 10`, `backBufferLength: 0`, `liveSyncDurationCount: 2`). 기본값은 녹화물 기준이라 라이브에 안 맞는다 — 같은 스트림을 몇 년째 서비스하는 쪽의 값이 짐작보다 낫다.
- **`.env`의 `VITE_API_BASE_URL`이 배포 주소라 개발 서버에서 CCTV가 안 뜬다.** 클라이언트가 로컬 미들웨어 대신 배포를 부르는데 거기엔 아직 이 함수가 없다. 로컬로 보려면 그 값을 비우고 띄워라(`VITE_API_BASE_URL= npm run dev`).

### 실제로 확인한 것 / 못 한 것

**확인했다(실제 크롬 390×844, 2026-08-19).** 명동 관광특구에서 카메라 7대가 뜨고 첫 대(명동입구)가 **실제 도로 영상으로 재생됐다**(`readyState 4`, 1280×720, `currentTime`이 6초 넘게 진행). 칩으로 청계광장에 갈아타니 영상이 바뀌고 지도가 그 자리로 가 핀이 떴다. 프록시 사슬도 따로 쟀다 — 마스터 m3u8 → chunklist → 2.5MB `.ts` 세그먼트가 전부 HTTPS·`ACAO: *`로 온다.

**못 했다 — 장시간 재생.** 이 기계(WSL2)의 크롬은 몇 초 뒤 `PIPELINE_ERROR_DECODE`로 죽는다. **우리 코드 문제가 아닌 것은 확인했다:** 맨 hls.js에 서울시 설정 그대로 넣어도 똑같이 죽고, 같은 크롬이 로컬에서 만든 1280×720 H.264 High L3.1 HLS는 MSE로 끝까지 재생하며, ffmpeg는 이 스트림을 오류 없이 디코딩한다(즉 스트림도 멀쩡하다). 스트림이 VFR이고 프레임레이트를 선언하지 않는데(`avg_frame_rate=0/0`) 그 근처가 의심되지만 **재지 않았다.** 화면은 그때 「지금은 영상을 불러올 수 없어요」로 떨어진다 — 서울시 플레이어도 같은 자리에서 「원본 시스템 점검중입니다」를 띄운다.

**그래서 실기기 회차에 이걸 꼭 볼 것:** 안드로이드 토스 웹뷰에서 **1분 이상 끊김 없이 재생되는가.** 여기서 깨지면 안드로이드 사용자에게는 이 기능이 없는 것과 같다.

### 남은 위험

1. **문서화되지 않은 내부 엔드포인트다.** 이용약관상 근거가 없고 Referer로 언제든 막힐 수 있다. 깨져도 **조용히** 깨진다(302에 본문이 없다). 그래서 프록시가 실패를 200 + `[]`로 접고 화면은 「CCTV 없음」이 된다 — 30곳 중 10곳이 원래 그 상태라 사용자에게는 같은 화면이다. **이 흡수를 502로 바꾸지 마라.**
2. **영상 대역폭은 서울시가 진다.** 우리가 싸게 얻는 대신 남의 서버에 매달려 있다. 막히면 1번과 같은 경로로 조용히 사라진다.
3. **세션 부트스트랩이 매 호출마다 필요한지 안 쟀다.** 지금은 캐시 미스마다 두 번 나간다.
4. **`광장(전통)시장`이 0건인 게 이름 탓인지 CCTV가 없는 탓인지 아직 못 갈랐다.** 괄호 이름 공백 문제(위 미해결 항목)와 같은 자리다.

## 상세 헤더를 샘플에 맞췄다 — 조작부가 제목 줄로 (2026-08-19)

사용자가 `seoul_detail.png`(서울 인파레이더)를 다시 짚었다: **「공유하기, 즐겨찾기, 인스타 등이 동일하게 반영되지 않고 있다」.**

### 무엇이 달랐나

| | 샘플 | 우리(고치기 전) |
| --- | --- | --- |
| 즐겨찾기·공유 | 제목과 **같은 줄**에 아이콘 | 히어로 아래 **폭을 반씩 나눠 갖는 48px 행** |
| 인스타그램 | 있음 | **없음** |
| 요약 칩 | 아이콘 + `CCTV 5` 포함 | 글자만, **CCTV 칩 없음** |

### 고친 것

1. **조작부가 제목 줄로 올라갔다.** `AreaHero`에 `actions` 슬롯을 냈고 `ActionButtons`가 아이콘 셋이 됐다(48px, `ThemeToggle`과 같은 크기). **48px 행 하나 + 위아래 여백 24px, 합계 72px을 아꼈다** — 그만큼 예측 차트가 폴드 안으로 올라온다. 상태는 여전히 글자로만 말한다(`aria-label`이 「저장」↔「저장됨」, `aria-pressed`는 계속 안 쓴다).

2. **인스타그램 버튼을 새로 붙였다.** `domain/socialLinks.ts`가 명소 이름을 해시태그로 줄인다. **카탈로그 이름을 그대로 못 쓴다** — 30곳 중 9곳에 해시태그가 못 받는 글자가 있다. 규칙 셋을 이 순서로: 괄호 안만 지우고 앞뒤를 잇고(`광장(전통)시장`→`광장시장`, 여는 괄호에서 자르면 `광장`이라는 다른 곳이 된다), 가운뎃점 앞만 남기고(`광화문·덕수궁`→`광화문`), 「관광특구」 꼬리를 뗀다(`#명동`과 `#명동관광특구`는 규모가 몇 자릿수 다르다). 30곳 전부를 통과시키는 테스트가 있다.

   **주소는 `/explore/tags/`다. `/explore/search/`가 아니다** — 실측(2026-08-19): 검색 주소는 302로 `/accounts/login/`에 튕기고, 태그 주소는 로그인 없이 200이다(`/popular/<태그>/`로 301된다). 미니앱 밖으로 나가는 링크라 받는 쪽 로그인 상태를 우리가 못 정한다.

3. **`CCTV n` 칩이 생겼다.** 도메인의 `cctvChip()`이 만든다 — `summarizeCityInfo`와 따로인 이유는 CCTV가 `citydata`가 아니라 별개 엔드포인트라서다. 그래도 **도메인에 둔 이유는 `i18n.test.ts`다**: 그 테스트가 칩 라벨을 도메인에서 뽑아 사전 완결성을 검사하므로, 화면에서 리터럴로 지으면 그물 밖으로 빠져 번역 없이 배포된다. **칩이 맨 앞인 것은 칩 순서 = 절 순서라는 계약** 때문이다(CCTV 절이 도시정보 패널보다 위다). 샘플은 끝에 두지만 그쪽 칩은 누를 수 없어 순서가 목차일 필요가 없다.

4. **칩에 아이콘이 붙었다.** 절 제목이 쓰는 것과 **같은** 글리프여야 한다 — 칩이 목차라, 도착한 절에 다른 그림이 붙어 있으면 제대로 왔는지 알 수 없다.

### 이 작업이 만든 고장 하나 — 눈으로만 보이는 것

**영어 이름이 말줄임표도 없이 잘렸다.** 조작부가 제목 줄로 올라오면서 제목에 남는 폭이 약 130px이 됐는데(390×844 실측), 「Gyeongbokgung」은 한 낱말이라 그 폭에서 줄을 바꿀 자리가 없다 — 넘친 부분이 `line-clamp`의 `overflow: hidden`에 걸려 **「Gyeongbokgu」에서 뚝 끊겼다.** 테스트는 전부 초록이었다(jsdom에 레이아웃이 없다). `break-words`로 고쳤고 클래스를 테스트로 잠갔다.

한국어 이름은 글자마다 줄을 바꿀 수 있어 달라지는 것이 없다. 긴 이름(`홍대입구역(2호선)`·`DDP(동대문디자인플라자)`)은 두 줄로 감기고, 그때 아이콘 셋이 두 줄의 한가운데에 선다 — 잘리는 것보다 낫다.

### 실측 (390×844, 실제 크롬)

- 제목 줄 높이 **50px**(한 줄 이름) / **82px**(두 줄 이름). 아이콘 셋은 48×48 그대로.
- CCTV 칩을 누르면 시트가 **0 → 1211px** 스크롤하고 포커스가 `cityinfo-cctv`로 간다.
- 인스타 href: `.../explore/tags/%EA%B2%BD%EB%B3%B5%EA%B6%81/`(경복궁), `.../tags/%EB%AA%85%EB%8F%99/`(명동 관광특구).
- 영어에서 라벨 셋: `Save` / `See photos on Instagram` / `Share`. 칩: `Cameras 1` / `Traffic slow` / `Subway 1`.
- 프리캐시 558.00 → **559.23 KiB**(인스타 글리프 한 줄).

### 일부러 안 바꾼 것

- **칩 줄은 여전히 가로 스크롤이다.** 샘플은 두 줄로 감는다. 우리 근거는 그대로 유효하다 — 줄바꿈이면 그 아래 카드가 명소마다 다른 높이에서 시작한다. 샘플은 4,000px짜리 한 장이고 우리는 시트 안이다.
- **둘째 줄은 카테고리다.** 샘플은 그 자리에 「지금 약 2,000~2,500명」을 둔다. 우리는 그 숫자가 `CongestionCard` 안에 있고, 옮기려면 절 순서를 다시 짜야 해서 이번 범위 밖이다.

## 큰 글씨를 등급에서 사람 수로 옮겼다 (2026-08-19)

사용자 지적: **「다소 혼잡」 텍스트가 너무 크고, 서울 인파레이더는 인원을 먼저 보여준다.**

### 무엇이 틀렸나

혼잡도 카드가 「다소 혼잡」을 **32px(`text-display-lg`)**로 이고 시작했다. 두 가지가 겹쳐 있었다.

1. **크기** — 사용자가 지적한 것.
2. **중복** — 더 중요한 쪽이다. 바로 위 제목 줄에 「약간 붐빔」 배지가 있는데 그 아래에 같은 말을 32px로 한 번 더 적고 있었다. 사실 하나를 크기만 바꿔 두 번 말한 셈이다.

정작 **사람 수는 14px(`text-label-md`)로 안내 문구 밑에 깔려 있었다.**

### 고친 것

`PopulationLead`를 새로 냈다. 히어로 다음, 칩 앞이다 — 샘플의 자리다.

```
광화문·덕수궁        ☆ ⃞ ⤴  [보통]
지금 약 40,000~42,000명          ← 24px, 「지금 약」만 흐리게
아직 비교할 기록이 부족해요.
[CCTV 6][도로 정체][지하철 3][주차 …]
```

**왜 숫자가 큰 글씨를 받는가:** 혼잡도는 다섯 등급이라 배지 한 칸에 다 들어간다. 반면 **사람 수는 등급이 뭉갠 것을 되살리는 유일한 값이다** — 같은 「약간 붐빔」이 광화문에서는 40,000명이고 서촌에서는 4,000명이다.

`CongestionCard`에서 셋이 빠졌다(큰 글씨 헤드라인·추정 인구 줄·평소 대비 줄). 남은 것은 **지금을 설명하는 것들**이다: 여유 예상 → 안내 문구 → 갱신 시각 → 인구 구성. `pattern` prop도 필요 없어져 뗐다.

`i18n`: `'추정 인구 {최소}~{최대}명'`을 지우고 `'지금 약'`을 새로 넣었다. 숫자 쪽(`'{최소}~{최대}명'`)은 **이미 있던 키를 나눠 쓴다** — 혼잡예보 막대의 소리 전용 줄이 같은 낱말로 같은 것을 센다.

### 길찾기 위치 — 앞의 판단이 틀렸다

`MapLinkButtons`에 「샘플이 다 보여준 뒤 맨 끝에 놓는다 → 읽고 나서 갈지 정한다」고 적어 뒀는데 **틀렸다.**

그 결론은 `detail_page_sample.png`(전체 페이지 4,147px)를 보고 낸 것이다. 전체 페이지 스크린샷에서는 **고정 요소가 맨 끝에 한 번 렌더된다** — 그걸 페이지 끝의 정적 요소로 읽었다.

`seoul_detail.png`(뷰포트 915px)를 보면 갈린다. 지도가 맨 위에 있는 **안 스크롤한** 화면인데 길찾기 셋과 푸터가 화면 맨 아래에 있고, 그 바로 위에 **「1시간 전 대비」 카드의 윗머리가 잘려 깔려 있다.** 정적 요소라면 뷰포트 첫 화면에 나올 수가 없다. **길찾기는 화면 하단에 고정된 바다.**

**실측(390×844, `광화문·덕수궁`):** 시트가 half에서 보이는 높이 **449px**, 내용 전체 **5,395px**, 카카오맵 버튼은 **5,369px 지점**. 즉 **주 CTA가 12화면 아래**에 있다.

**고쳤다 — 시트 안 sticky 바로 갔다**(사용자 결정, 2026-08-19). 대가를 알고 고른 것이다: 바 높이가 **73px**이고 half에서 보이는 449px의 **16%**다.

- `MapLinkButtons`에 `pinned`가 붙었다. 참이면 `sticky bottom-0` + 위쪽 선 + 불투명 배경, 거짓이면 흐름 안의 마지막 요소(고정 이전과 같은 자리).
- **`fixed`가 아니라 `sticky`인 것이 핵심이다.** sticky는 흐름에 자기 자리를 남기므로 맨 아래까지 굴리면 「근처 쾌적한 장소」 뒤 제자리로 돌아간다 — `fixed`면 그 절의 마지막 줄이 **영영** 바 밑에 깔린다.
- **`-mb-6`으로 상세 뿌리의 `pb-6`을 상쇄한다.** 없으면 sticky의 담는 상자가 24px 위에서 끝나 바가 시트 밑변에서 떠 있다.
- **peek에서는 안 붙인다.** 그 단계에서 시트가 보여주는 높이가 **111px**(실측)이라 73px 바를 붙이면 「목록으로」 한 줄만 남는다. `HomeScreen`이 `wide || sheetDetent !== 'peek'`로 판단한다 — `wide ||`가 필요한 이유는 좁은 화면에서 peek으로 내려 둔 뒤 창을 넓히면 `sheetDetent`에 그 값이 남기 때문이다(`showRecenter`와 같은 형태).

**실측(390×844, 실제 크롬):** 스크롤 0 / 2000 / 맨 아래에서 바 밑변이 전부 시트 밑변(844)과 **같고 간격 0**이다. 바 자리의 `elementFromPoint`가 `A`(링크)라 내용이 비치지 않는다. 단계별 `position`: half `sticky` / peek `static`(바가 6,056px 아래로 내려가 안 보인다) / full `sticky`.

**jsdom은 이걸 못 잰다**(레이아웃이 없다). 클래스만 `AreaDetail.test.tsx`의 두 테스트가 잠근다.

## 명소가 30곳에서 121곳이 됐다 (2026-08-20)

사용자 지적: **「121곳이 다 나와야 한다.」** 맞는 말이었고, 못 하고 있던 이유가 사실은 없었다.

### 벽이라고 믿었던 것

「하루 1,000회라 30곳이 상한」이 이 저장소의 오래된 전제였다. 산수 자체는 맞다 — `citydata_ppltn`은 `AREA_NM`이 필수라 명소당 1회이고, 121곳을 1시간마다 갱신하면 **2,904회**로 한도의 세 배다. 한도 안에 넣으려면 갱신 주기를 3~4시간으로 늘려야 하는데, 그러면 「지금 붐비나?」를 묻는 앱이 죽는다.

**틀린 것은 산수가 아니라 「그 문으로만 들어갈 수 있다」는 전제였다.**

### 찾은 것

CCTV 때 배운 것을 그대로 했다 — *경쟁 서비스가 하고 있는 것이 보이면 그 서비스의 프런트엔드부터 열어라.* 서울시 실시간 도시데이터 웹(SeoulRtd)의 `js/api/hotspot-api.js`·`population-api.js`를 열었다.

```
GET https://data.seoul.go.kr/SeoulRtd/api/hotspot?hotspotNm=
→ 121곳 전부 · 22KB · 인증키 없음 · 중앙값 50ms
   { area_nm, area_congest_lvl, area_congest_num, congestion_color, x, y, category }
```

**한 번의 호출로 121곳이 오고 하루 1,000회를 1원도 안 쓴다.** 인파레이더가 121곳·5분을 하는 것도 아마 이 문이다 — 우리가 진 것은 설계가 아니라 비싼 문으로 들어가고 있었기 때문이다.

### 스파이크로 확인한 것 (붙이기 전에 쟀다)

| 잰 것 | 결과 |
| --- | --- |
| 이름이 우리 카탈로그와 맞는가 | **121/121 일치** — 괄호 주변 공백까지 같다 |
| 값이 공식 API와 같은가 | **같은 순간 30곳 중 29** (남은 하나는 등급 경계) |
| 갱신 주기 | **5분** — 1분 간격 45회 표본에서 9번 바뀌었고 간격이 `5 5 5 5 5 4 5 5`다 |
| 안정성 | **45/45 성공**, 302·403 없음, 재부트스트랩 0회, 항상 121행 |
| 세션 수명 | 쿠키 하나로 **44분**(2,643초) 내내 버텼다 — 한 번도 안 죽었다 |
| 응답 시간 | 중앙 50ms / p90 72ms / 최대 228ms |

**`4`가 하나 낀 것은 상류가 불규칙해서가 아니라 우리가 1분 눈금으로 봤기 때문이다.** 5분 주기를 1분 간격으로 관측하면 경계가 눈금 사이에 떨어질 때 4분이나 6분으로 보인다 — 02:29~03:08의 39분을 8구간으로 나누면 4.875분이고, 공식 응답의 `PPLTN_TIME`도 5분 단위로 떨어진다. **나중에 4분 간격을 보고 「주기가 들쭉날쭉하다」고 결론 내지 마라.**

**「같은 시스템」이라는 결정적 증거:** 공식 OpenAPI 응답의 봉투 이름이 문자 그대로 `SeoulRtd.citydata_ppltn`이다. 두 문이 한 창고로 이어져 있다.

### 덤으로 닫힌 미해결 항목

**괄호 이름 공백 문제**가 끝났다. 「실제 호출로만 확정된다」고 열어 두었던 것인데 `광장(전통)시장`·`홍대입구역(2호선)` 포함 121개가 우리가 적어 둔 문자열과 정확히 같다. 따라서 **`광장(전통)시장`의 CCTV 0건은 이름 탓이 아니라 진짜로 카메라가 없는 것**이다 — 겹쳐서 못 갈랐던 두 가지가 함께 갈렸다.

### 무엇을 바꿨나

**카탈로그를 생성기가 만든다**(`scripts/generate-areas.mjs`). 손으로 적던 시절 `code`가 30곳 중 25곳 틀렸는데(2026-08-13), 121곳이면 같은 실수가 네 배가 되고 좌표까지 손으로 찍어야 한다. `code`·`lat`·`lng`·`category`는 서울시가 이미 갖고 있으니 받아 적는다. 영어 이름 91개와 목적 태그만 우리 것이다.

- **좌표가 폴리곤 중심으로 바뀌었다.** 손값과 29곳 중 3곳이 500m 이상 달랐다(최대는 넓은 구역인 잠실 관광특구 1.4km). API 값이 낫다.
- **`AREA_CD`는 한 번만 받는다.** 처음엔 돌릴 때마다 121회를 썼는데, 이미 아는 코드는 기존 파일에서 읽어 건너뛰게 고쳤다. 재생성 비용이 0회다.
- **생성기가 `ALERT_SOURCE_AREA`를 통째로 빠뜨렸다.** 문서 주석이 붙은 export였고, 그 순간 홈의 재난문자가 죽었다. 테스트가 잡았다 — **생성기를 쓸 때는 「카탈로그 말고 그 파일에 또 뭐가 사는가」를 먼저 세어라.**

**데이터 경로가 둘로 갈렸다.**

| 자리 | 출처 | 쿼터 |
| --- | --- | --- |
| 목록·지도·「오늘의 서울」 | SeoulRtd `/api/hotspot` (1회) | **0** |
| 명소 상세 | 공식 `citydata_ppltn` (연 곳만) | 씀 |

**그 분리가 가능했던 이유가 이 작업의 핵심이다.** 목록·지도·오늘 경로가 스냅샷에서 실제로 읽는 것은 **`congestion` 하나뿐**이었다(`summary.ts`의 4곳도 전부). 인구수·예보·구성비는 상세에서만 쓴다. 그래서 가벼운 타입(`AreaCongestion = { name, congestion }`)을 새로 두고 목록만 그쪽으로 옮겼다 — `AreaSnapshot`이 이 모양을 구조적으로 만족해서 두 경로가 한 타입 아래서 만난다.

**곁가지로 고친 것:** `buildNearbyList`가 `snapshots[index]`로 자리를 맞추고 있었다. 호출부가 넘긴 배열이 `entries`와 같은 순서·같은 길이라는 약속에 기대는 코드였고, 어긋나면 **명소 A의 혼잡도가 B에 붙는다**(조용히, 그럴듯하게). 이름으로 맞추게 바꿨다.

### 남은 일 — 셋 다 진짜 위험이다

1. **폴백이 없다.** SeoulRtd는 문서화되지 않은 내부 엔드포인트라 언제든 막힐 수 있고 **조용히** 깨진다(302, 본문 없음). CCTV는 깨져도 「CCTV 없음」으로 접혔지만 **혼잡도는 앱의 본체**다. 지금은 `/api/hotspots`가 502를 올리고 화면이 오류를 보여주는 데서 끝난다. 물러설 길(예: 가까운 40곳만 공식 API로)이 필요하다.
2. **상세가 쿼터를 쓴다.** 하루에 서로 다른 명소 상세가 30곳 넘게 열리면 (24+8)×30 = 960으로 한도에 닿는다. 121곳이 다 열리면 넘긴다. **막는 길은 상세도 SeoulRtd로 옮기는 것**이고, 실제로 `/api/ppltn`·`/api/ppltn_congest`가 성별·연령·상주비율에 더해 **1시간/3시간/한달 전 대비**와 **과거 12시간 실측 인구**까지 키 없이 준다 — PLAN.md 4차와 STATE.md가 「지금 저장 구조로 못 만든다」고 적어 둔 것들이다.
3. **상세가 다시 스켈레톤으로 열린다.** `findSeededSnapshot`이 `['areas', ...]` 캐시를 찾는데 목록이 더는 그걸 안 채운다. 코드는 안 깨지고 그냥 항상 `undefined`다. 가벼운 등급만이라도 헤더에 먼저 꽂아야 한다.

**목적 태그가 19곳에만 있다.** 새 91곳은 「아이와 나들이」·「데이트」 프리셋에서 빠진다 — 타입이 `purposes?`로 선택인 것이 바로 이 경우를 위한 설계였다(조용히 오분류되느니 빠진다). 121곳 기준으로 다시 태그할지는 UI 회차에서 정한다.

**「오늘의 서울」의 카드별 「HH:MM 업데이트됨」을 뺐다.** 새 출처에 명소별 관측 시각이 없다. 없는 값을 지어내지 않는다 — 게다가 원래도 한 번의 일괄 조회에서 나온 **같은 값**을 카드마다 반복하고 있었다. 제대로 된 자리는 화면 위 한 줄이고 그건 아직 없다.

### 확인한 것 / 못 한 것

**확인했다.** 개발 서버로 `/api/hotspots`를 불러 **121행**이 우리 코드 경로로 오는 것을 봤다. 테스트 1,225개·`tsc`·`eslint` 전부 통과.

**못 했다 — 화면으로 못 봤다.** 121곳이 지도에 얹혔을 때 마커가 얼마나 겹치는지, 시트 목록이 네 배로 길어져 정렬·필터가 감당되는지는 **실제로 열어 봐야 안다.** 30곳 기준으로 잡아 둔 화면이라 여기서 무너질 가능성이 크다. 다음 회차의 첫 항목.

## 길찾기 셋을 중립 상자로 통일했다 (2026-08-20)

**주석과 테스트가 먼저 바뀌어 있고 코드가 안 바뀐 채로 발견했다.** 08-19 17:42에 누가 손대다 멈춘 상태였다 — `MapLinkButtons`의 주석은 「셋 다 중립 배경, 브랜드 색은 아이콘에만」이라 선언하는데 `mapLinks()`는 여전히 `bg-brand-kakao`·`bg-brand-naver`였고, `tokens.test.ts`는 그 사이에 4.5:1 대비 검사를 **이미 뺐다.**

**세 조합 중 가장 나쁜 것이다.** 화면은 옛것 그대로인데 그것을 지키던 검사만 사라져서, 테스트는 green인데 그 green이 아무 뜻이 없다. **주석·테스트를 코드보다 먼저 바꾸지 마라** — 순서가 갈리면 그 사이에 보호막만 없는 구간이 생긴다.

### 왜 중립 상자인가

샘플(서울 인파레이더)을 보면 **셋 다 같은 중립 상자**이고 브랜드 색은 화살표 글리프에만 있다. 칠하는 쪽이 틀린 이유가 셋이다.

1. **정보를 나르는 것은 라벨이다.** 「카카오맵」이라 적혀 있으면 배경까지 노랑일 필요가 없다.
2. **다크 모드가 저절로 풀린다.** 브랜드 배경은 밤에도 못 바꾸는데 그 위의 글자는 바뀌어야 해서 `--color-brand-ink`를 따로 들여야 했다. 배경이 우리 토큰이면 `text-on-surface`가 두 모드에서 알아서 맞는다 — **그 토큰은 지웠다.**
3. **셋의 무게가 같아진다.** 지도 앱 선택은 취향이라 우리가 하나를 밀 이유가 없다.

### 옮기면서 딸려온 함정 — 배경으로 멀쩡하던 색이 전경에서 무너진다

카카오 노랑(`#fee500`)은 **배경**일 때 어두운 글자를 얹어 12.90:1이었다. 같은 값을 흰 표면 위 **아이콘**으로 놓으면 **1.28:1**이라 통째로 사라진다. 배경일 때는 「글자를 어둡게」로 풀리던 문제가, 전경이 되는 순간 **색 자체를 바꾸는 것 말고 길이 없어진다.**

그래서 `--color-brand-kakao`를 지우고 `--color-brand-kakao-ink: #998a00`을 뒀다. 색상(54°)은 그대로 두고 명도만 내린 **우리 값**이다 — 남의 로고가 아니라 우리 화살표에 칠하는 색이라 조정할 수 있다.

| 색 | 밝은 표면 | 어두운 표면 |
| --- | --- | --- |
| `brand-kakao-ink #998a00` | **3.51** | 4.98 |
| ~~진짜 카카오 노랑 `#fee500`~~ | ~~1.28~~ | ~~13.65~~ |
| `brand-naver #03c75a` | 2.25 | 7.75 |

**네이버는 손대지 않았다.** 2.25:1로 3:1에 못 미치지만 **초록이라는 것은 읽힌다** — 1.28:1의 노랑과 다르다. 실제 브랜드 값을 그대로 두는 쪽이 맞다. **티맵은 브랜드 색을 안 쓴다** — 토큰을 하나 더 들이는 값보다 얻는 것이 적어서, 아이콘이 라벨과 같은 `on-surface`를 쓴다.

**3:1은 규정이 아니라 우리가 고른 바다.** 라벨이 같은 말을 하므로 색은 정보를 나르지 않고, WCAG 1.4.1에도 1.4.11에도 걸리지 않는다. 그래도 카카오만 올린 것은 **훑을 때 단서로 쓰이려면 일단 보여야 하기 때문**이다.

### 잠근 것

`tokens.test.ts`의 「길찾기 버튼 색 대비」가 **「길찾기 버튼 아이콘 색」**이 됐다. 재는 대상이 「브랜드 배경 위의 글자 4.5:1」에서 「표면 위의 아이콘 3:1」로 바뀐다. `#fee500`으로 되돌려 **밝은 쪽이 1.2788로 죽는 것을 확인했다.** 다크 완결성 검사의 `EXEMPT`도 `brand-kakao-ink`·`brand-naver` 둘로 줄였고, 면제가 늙지 않도록 「다크에서 갈아 끼우지 않는다」를 그 둘에 직접 건다.

**확인법:** 상세를 열어 하단 바를 본다. 셋이 같은 흰 상자에 테두리, 아이콘만 각각 올리브·초록·기본색이면 맞다. 다크 토글로 한 번 더 본다.

**안 한 것:** 실기기에서 못 봤다. 올리브(`#998a00`)가 「카카오」로 읽히는지는 산수가 답할 수 없다 — 노랑의 색상은 남겼지만 명도를 내리면 사람 눈에는 겨자색에 가깝다. **토스 실기기 회차에 눈으로 확인할 것.**

## 사람 수 글씨를 제목보다 한 단 내렸다 (2026-08-20)

`PopulationLead`가 `headline-md`(24px)였는데 **그 크기가 제목(`광화문·덕수궁`, `AreaHero`)과 똑같다.** 한 화면에 헤드라인이 둘이면 무엇이 이 화면의 주인인지 흐려진다.

게다가 **우리 숫자가 샘플보다 넓다.** 같은 「40,000~42,000명」이 우리는 197px, 샘플은 줄 전체가 208px이다(390×844 / 412px 실측). `headline-sm`(20px)이면 숫자 부분이 약 164px이라 「지금 약」과 함께 한 줄에 넉넉히 들어간다.

`headline-sm` + `label-md`(앞머리)로 내렸다. **여전히 화면에서 둘째로 큰 글씨라 「인원을 먼저」는 유지된다** — 08-19에 큰 글씨를 등급에서 사람 수로 옮긴 판단(위 절)은 그대로다. 바뀐 것은 크기지 자리가 아니다.

## 시안 폴더를 지웠고, 그게 스위트를 멈춰 세웠다 (2026-08-20)

사용자가 `stitch_ui/`를 지웠다 — **디자인을 새로 넣기로 했기 때문**이다. 시안 4종이 4탭 기준이라 탭 없는 지금 구조와 통째로 달랐으니 맞는 정리다.

**그런데 테스트가 통째로 멎었다.**

```
Startup Error
Error: ENOENT: no such file or directory, open 'stitch_ui/seoul_flow/DESIGN.md'
```

`vitest.config.ts`가 디자인 토큰 정본을 `readFileSync`로 **직통으로** 읽어 `__DESIGN_MD__`에 박고 있었다. 파일이 사라지자 설정 자체가 던져서 **1,239개가 한 개도 안 돌았다.** 문서 하나가 없다고 스위트 전체가 멎는 것은 과한 결합이다.

### 고친 것

- **`readDesignSource()`가 두 자리를 본다** — 새 자리 `docs/DESIGN.md`, 옛 자리 `stitch_ui/seoul_flow/DESIGN.md`. 둘 다 없으면 **빈 문자열**이고 러너는 그대로 뜬다.
- **`tokens.test.ts`가 그 상태를 알아보고 비켜선다.** 다만 조용히는 아니다 — 매 실행마다 `정본(docs/DESIGN.md)이 아직 없어 색 대조를 건너뛴다`가 한 줄 찍힌다. **describe를 지우면 「검사가 있었다」는 사실까지 사라진다.** 이 저장소는 그 규칙이 문장으로만 있던 동안 실제로 열 곳이 갈렸던 곳이다(2026-08-12 감사).
- 비켜설 때도 **코드 쪽은 읽히는지 확인한다**(`colorsOfCss().size > 40`). 0이면 정본이 없는 게 아니라 주입이나 정규식이 깨진 것이다.

### 새 디자인을 넣을 때

**`docs/DESIGN.md`에 넣으면 양방향 대조가 저절로 되살아난다.** 프론트매터의 `colors:` 블록을 읽는다:

```
---
colors:
  primary: '#004ac6'
  ...
---
```

되살아나는 순간 `index.css`와 다른 값이 있으면 그 토큰 이름을 짚으며 죽는다. **그게 이 검사의 값어치이므로, 새 디자인을 넣고 나면 한 번 돌려서 어긋난 곳을 다 맞춰라.**

**남은 잔재:** `index.css`의 `@source not "../stitch_ui"`는 남겨 뒀다 — 없는 경로를 적어 두는 것은 무해하고 새 시안이 같은 자리에 들어올 수 있다.

## 지도와 목록을 「보이는 곳」에 묶었다 (2026-08-20)

121곳으로 늘린 뒤 헤드리스로 재 보니 **30곳 전제로 만든 화면이 무너져 있었다.**

| | 전 | 후 |
| --- | --- | --- |
| 보이는 띠(371px) 안 마커 | 110개 | **33개** |
| 겹치는 쌍 | **269쌍** | **0쌍** |
| 다른 마커와 겹치는 마커 | 103개(94%) | **0개** |
| 목록 첫 세 줄(위치 권한 없음) | 홍대 관광특구·서울 암사동 유적·김포공항 | **시의회 앞·명동 관광특구·북창동 먹자골목** |

### 무엇을 했나

**`domain/viewport.ts`** 하나가 생겼다. 순수 함수라 전부 테스트로 잠긴다(13개).

- **`thinForLegibility`** — 겹치는 마커를 솎아낸다. 우선순위대로 훑으며 이미 놓은 마커에서 34px 안이면 건너뛰는 탐욕법이다. 우선순위는 **(1) 카테고리 무게 (2) 화면 한가운데에서 가까운 순**.
- **`areasForList`** — 목록은 보이는 영역 안이되 **12곳 하한**이 있다.
- **`sortByDistanceFromView`** — 위치 권한이 없을 때 화면 한가운데를 기준점으로 삼는다.

**카테고리 무게가 이 설계의 핵심 판단이다.** 121곳 중 **48곳이 인구밀집지역**이고 그 대부분이 역 이름이다. 자리를 다투면 역이 경복궁을 가리는데, 나들이 목적지를 찾는 화면에서 그건 거꾸로다. 그래서 등급이 아니라 **무엇을 보러 가는가**로 매긴다: 관광특구 → 고궁·문화유산 → 공원 → 발달상권 → 인구밀집지역.

**안 고른 길 둘.**

- **클러스터링**(핀을 숫자 뱃지로): 「몇 개 있다」를 알려주고 답은 한 번 더 누르게 한다 — 이 앱이 줄이려는 단계를 오히려 늘린다.
- **줌 단계로 등급 거르기**(「낮은 줌에선 붐빔만」): 이 앱 질문의 절반이 「어디가 한산한가」라 여유를 감추면 그 절반이 죽는다. 거르는 기준은 등급이 아니라 **자리**여야 한다.

### 구현하다 드러난 구멍 넷 — 전부 테스트가 먼저 잡았다

1. **`full`에서 목록이 통째로 빈다.** 그 단계의 보이는 띠는 화면의 8%뿐인데, 시트를 끝까지 올린 것은 **목록을 읽으려는** 조작이다. → 단계와 무관하게 **`half` 기준의 고정된 띠**를 쓴다. 마커가 단계마다 재배치되던 것도 함께 사라졌다.
2. **검색이 뷰포트에 갇힌다.** 강남역을 열면 지도가 줌인하는데, 그 상태에서 「경복궁」을 치면 결과가 0이었다. → **검색어가 있거나 칩이 눌려 있으면 화면 밖까지 본다.** 이름을 친 것은 「어디 있든 찾아 달라」는 뜻이다.
3. **칩이 굳는다.** 개수를 뷰포트로 세면 줌인한 순간 프리셋이 전부 0이 되어 눌리지 않는다 — 다른 목적으로 갈아탈 방법이 사라진다. 2번과 같은 처방으로 풀린다.
4. **연 명소의 마커가 사라진다.** 이웃한 더 「무거운」 명소에 밀려 솎이면, 상세를 보고 있는데 지도가 그 자리를 안 가리킨다. → `thinForLegibility`에 `pinnedName`을 넣어 **지금 열어 둔 곳은 무조건 남긴다.**

### 안 풀린 것 — 정직하게

**목록 길이는 그대로다(116행 / 15.9화면).** 서울 전역 뷰에서는 121곳이 거의 다 화면 안에 있으니 뷰포트 결속이 목록을 안 줄인다. 줄어드는 것은 줌인했을 때뿐이다.

그래도 **정렬이 고쳐졌으므로 성격이 달라졌다** — 전에는 16.5화면이 무작위였고 지금은 위 몇 줄이 답이다. 길이 자체를 손보려면 별도 결정이 필요하다(상한 + 「더 보기」, 초기 줌 올리기 등). 지금은 안 한다.

### 테스트에서 배운 것

**마커를 이름으로 찍던 단언 일곱이 깨졌다.** 「경복궁 마커를 누른다」 같은 것인데, 도시 전역 줌에서 경복궁은 1km 옆 광화문·덕수궁에 밀려 안 그려진다 — **그건 결함이 아니라 그 줌에서 둘을 따로 그릴 픽셀이 없다는 사실이다.** 그려진 것 하나를 집어 **그 이름으로** 결과를 확인하도록 바꿨다(`anyMapMarker`). 재려던 것은 「마커를 누르면 그 명소가 열리는가」이지 경복궁이 그려지는가가 아니었다.

## 지도 초기 중심이 서울시청이 아니었다 — 고쳤다 (2026-08-20)

**2026-08-18에 「결함이 아닐 가능성이 높다」고 적어 뒀는데 틀렸다.** 121곳으로 늘린 뒤 헤드리스로 첫 화면을 찍었더니 **의정부·도봉구**가 보이는 띠를 채우고 있었다. 서울 도심이 통째로 시트 뒤에 있었다.

### 산수가 왜 틀렸나 — 배울 만한 실수다

08-18의 계산은 이랬다: *「peek 시트(135px)를 빼도 보이는 띠가 0~709px이라 시청(y=422)은 띠 안에 있다」.*

**진입 단계가 peek이 아니라 `half`다.** `const [detent, setDetent] = useState<Detent>('half')`. 그래서 실제로 보이는 띠는 709px이 아니라 **371px**이고, y=422의 시청은 시트 뒤로 들어간다.

**틀린 값 하나를 가정에 넣고 산수를 하면 결론이 확신을 갖고 틀린다.** 그때 「재측정은 안 했다」고 스스로 적어 뒀으면서도 「지울 항목으로 보라」고 했다 — 그 두 문장이 같은 절에 있으면 안 됐다.

### 원인

`HomeScreen.tsx`의 `center` 초기값이 **명소를 가리키는 주소로 들어올 때만** 시트 보정을 했다.

```js
if (initialRoute.kind !== 'area') return SEOUL_CENTER   // ← 보정 없음
return offsetCenter({ lat, lng }, AREA_ZOOM, SHEET_RATIO.half, wide)
```

그냥 열면 `SEOUL_CENTER`가 날것으로 들어가 뷰포트 기하학적 중심(844px에서 y=422)에 놓인다. **뒤따르는 보정 effect도 안 돈다** — `appliedRatioRef`가 `SHEET_RATIO.half`로 시작해 「이미 보정했다」고 여기기 때문이다(그 ref의 주석이 이유를 적고 있다).

### 고친 것

두 갈래를 하나로 합쳐 **어느 쪽으로 들어오든 같은 규칙**을 쓴다. 서울 전역이면 `SEOUL_CENTER`+`DEFAULT_ZOOM`, 명소면 그 좌표+`AREA_ZOOM`, 보정은 둘 다 `offsetCenter`.

**확인:** 헤드리스 390×844 첫 화면에 종로·중구·강남이 들어온다. 의정부는 사라졌다.

**잠근 것:** `첫 화면 중심이 시트 높이만큼 비켜 잡혀 있다`가 `centerBelowSheet`를 다시 불러 **정확히 같은 값인지** 잰다. 되돌려서 3개가 죽는 것을 확인했다.

> **테스트 둘이 이 버그를 고정하고 있었다.** `expect(map).toHaveAttribute('data-center', '37.5665,126.978')`가 두 곳에 있었다 — 보정 없는 값을 「서울 전역」이라 적어 두고 지키고 있었던 것이다. **화면을 한 번도 안 본 채 쓴 단언이 버그의 자물쇠가 된다.**

## 도시 정보의 확인 안 된 가정

- **`ROAD_TRAFFIC_IDX`의 값 목록을 모른다.** 공식 명세(`서울시+실시간+도시데이터.xls`)에 이 필드의 출력명(「전체도로소통평균현황」)만 있고 값의 종류가 없다 — 「원활」·「서행」·「정체」를 명세 전문에서 찾으면 **0회**다. 그래서 혼잡도·통합대기환경등급과 달리 **네 톤에 겹치지 않고 문자열 그대로 보여준다.** 짐작으로 매핑하면 처음 보는 값에서 색이 안 붙는 게 아니라 **틀린 색이 붙는다.** 목업(`mockCityInfo.ts`)이 저 셋을 쓰지만 파서도 카드도 그걸 안다고 가정하지 않는다. **확인법:** `/api/cityinfo?area=광화문·덕수궁` 응답의 `ROAD_TRAFFIC_IDX` 값을 몇 시간대에 걸쳐 모아 종류를 센다.

**이 절의 아래쪽은 인증키 발급 전에 쓴 것이라 낡았다.** 2026-08-13에 실호출을 했고 응답 한 벌이 `docs/fixtures/citydata-광화문덕수궁.json`에 있다 — 최상위 키·문화행사 키·중첩 구조는 전부 확인됐다(위 「확인된 값」). 남은 것은 **값의 종류를 묻는 항목**뿐이다(표본이 한 명소뿐이라).

아래는 그때의 설계 근거로 남긴다. 그래서 `cityInfoSchema.ts`는 혼잡도 쪽(`schema.ts`)과 **반대로 관대하게** 파싱한다 — 값이 없거나 모양이 다르면 던지지 않고 `null`·빈 배열로 떨어진다. 최악의 경우 섹션이 "제공되는 도시 정보가 없어요"로만 뜨고 아무것도 깨지지 않는다.

당시 「인증키가 나오면 확인할 것」으로 적어 둔 목록 — **첫 둘은 풀렸다:**

- ~~**최상위 키가 `CITYDATA`인가.**~~ — **맞았다.**
- ~~**문화행사 섹션의 키.**~~ — **`EVENT_STTS`였다**(명세의 `CULTURALEVENTINFO`가 아니라). 둘 다 받게 해 둔 덕에 안 깨졌다. **줄이지 마라** — 명세와 실응답이 어긋난 자리라, 다른 명소에서 명세대로 올 가능성을 배제할 근거가 없다.
- **`PAY_YN`의 값 형태.** `Y`/`N`인지 `유료`/`무료`인지 명세가 말하지 않는다. 지금은 넷 다 받는다. 주차장과 문화행사가 같은 필드명을 쓰는데 값 형태가 서로 다를 수도 있다.
- **섹션이 배열인가 객체인가.** 항목이 하나면 객체로 온다는 보고가 있어 양쪽을 받는다.
- **`WEATHER_STTS`가 배열의 첫 항목인가.** 지금은 첫 항목만 읽는다. 여러 관측소가 실려 오면 어느 것을 쓸지 정해야 한다.
- ~~**`REPLACE_YN`**~~ — 2026-08-11에 읽기 시작했다. 값의 종류는 여전히 확인 안 됐다(아래 참고).

확인 방법은 간단하다. `/api/cityinfo?area=광화문·덕수궁`을 한 번 호출해 응답 JSON을 저장소에 저장해두면 위가 전부 풀리고, 그걸 목업의 기준으로도 쓸 수 있다.

**인구 구성도 같은 처지지만 서비스가 다르다.** `citydata_ppltn` 쪽이라 `/api/citydata?area=…` 한 번으로 따로 확인해야 한다. 파싱은 `data/compositionSchema.ts`가 같은 방향(관대하게)으로 맡는다 — 자세한 확인 항목은 아래 미해결에 있다.

## 미해결 / 확인 안 된 가정

### 요일×시간 패턴 (2026-08-12)

- **실사용에서 얼마나 차는지 모른다.** 56칸을 이 기기의 조회만으로 채우므로, 한 사용자가 한 명소를 여러 요일·시간대에 열어야 패턴이 생긴다. 대부분의 사용자에게 이 표는 오래 비어 있을 것이다 — 그래서 「모으는 중」 문구와 관측 수를 함께 둔다. **확인법:** 실사용 후 `seoul-live:pattern:{명소}` 값을 꺼내 `filledCells`를 센다.
- ~~**히트맵 색 램프**~~ — **해소.** 램프를 명도 단조로 다시 잡았고(이웃 1.89/1.66/1.82), **색각 이상도 계산으로 확인했다**(Machado 2009 변환 행렬, severity 1.0):

  | 시야 | 이웃 대비 | 단조 |
  | --- | --- | --- |
  | 정상 | 1.89 / 1.66 / 1.82 | 유지 |
  | 1형(protanopia) | 2.25 / 1.92 / 1.91 | 유지 |
  | 2형(deuteranopia) | 1.71 / 1.55 / 1.78 | 유지 |
  | 3형(tritanopia) | 2.02 / 1.59 / 1.60 | 유지 |

  세 유형 전부에서 명도 단조가 유지되고 이웃 간격이 1.55 이상이다. **명도로 램프를 만든 것이 여기서 값을 했다** — 색상으로만 갈랐으면 2형에서 무너졌을 것이다. 헤드리스 크롬의 `Emulation.setEmulatedVisionDeficiency`로 눈으로도 볼 수 있지만(CDP 필요), 계산이 같은 것을 답한다.
- **`observationSlot`이 기기 시간대를 쓴다.** `new Date(연, 월-1, 일).getDay()`라 문자열을 벽시계 날짜로 읽는다 — 서울 시각을 서울에서 보면 맞다. 기기 시간대가 다른 사용자에게 요일이 어긋나는지는 확인하지 않았다.

- **실기기로만 확인되는 것 5가지** — `Device.getLocation`의 권한 흐름(첫 호출이 네이티브 팝업을 띄우는지), `Device.openURL`로 카카오맵·네이버지도가 실제로 열리는지, `Share.sendMessage` 공유 시트, **지도 제스처(핀치 줌·팬)가 토스 웹뷰 안에서 정상 동작하는지**, 그리고 **전환 애니메이션의 프레임**(2026-08-21에 더해졌다). 앞의 셋은 브리지가 없으면 웹 표준(`window.open`·클립보드)으로 떨어지게 해뒀고(`src/platform/links.ts`) 폴백이 도는 건 테스트로 확인했다. 지도 제스처는 `gestureHandling: 'greedy'`로 Google Maps에 맡겨뒀을 뿐 폴백이 없다. 애니메이션도 폴백이 없다 — 끊기면 끊기는 대로 보인다(`reducedMotion="user"`는 접근성 설정을 켠 사용자만 비켜 준다). 웹뷰 안에서 브리지 쪽·제스처·프레임이 도는지는 기기로만 알 수 있다.
- **토스 웹뷰의 mixed content 정책** — HTTPS로 번들을 서빙한다고 가정했다. 로컬 파일 스킴이면 HTTP 직접 호출이 될 수도 있다. 다만 쿼터 때문에 프록시는 어차피 필요해서 설계는 안 바뀐다.
- **명소 이름의 정확한 공백** — 위 "명소 이름 검증" 참고. 실제 호출로만 확정된다.
- **`REPLACE_YN`의 값 목록을 모른다** — 읽기는 한다(2026-08-11). 다만 공식 명세에 필드명과 「대체 데이터 여부」만 있고 값의 종류가 없어서, `PAY_YN`·`CUR_PRK_YN`처럼 **아는 값이면 읽고 아니면 모름**으로 둔다(`'Y'` → 대체값, `'N'` → 실측, 그 외·없음 → `null`). 목업이 `'Y'`/`'N'`을 쓰지만 파서가 그 둘만 안다고 읽지 마라. **확인법:** 실호출 응답에서 `REPLACE_YN` 값을 모아 종류를 센다. 2026-08-13 한 표본은 `"N"`이었고 가정이 맞았다 — 다만 한 표본이라 「대체값이 실제로 얼마나 자주 오는가」는 여전히 모른다.
- **도시 정보의 목록 상한이 5곳이라는 게 맞는지** — 주차장·따릉이를 여유 많은 순 5곳까지만 보여주고 나머지는 "외 N곳"으로 접는다. 실제 응답에 한 명소당 몇 곳이 오는지 모르는 채로 정한 수다. 3곳이면 접을 이유가 없고 50곳이면 5곳도 부족할 수 있다.
- **위치 정확도** — `Accuracy.Balanced`(수백 m)를 쓴다. 거리순 정렬과 2km 추천에는 충분하다고 봤지만, 사용자가 "가까운 순이 이상하다"고 느끼는지는 기기에서 봐야 안다.
- **폰트가 실기기에서 어떻게 보이는지 모른다** — 2026-08-12에 웹폰트를 걷고 `system-ui, -apple-system, 'Apple SD Gothic Neo', 'Noto Sans KR', sans-serif`로 갔다. iOS는 SF + Apple SD Gothic Neo, 안드로이드는 Roboto + Noto Sans KR로 떨어질 **것으로 본다.** 헤드리스 크롬(리눅스)의 `system-ui`는 대상 기기 것이 아니라 확인이 안 된다. **확인법:** 토스 웹뷰에서 상세를 열고 「추정 인구 88,000~90,000명」 줄의 숫자와 한글 높이가 맞는지, 32px 헤드라인이 어색하지 않은지 본다.
- **앱인토스가 서드파티 SDK의 동적 스크립트 로딩을 허용하는지 확정하지 못했다** — 심사 체크리스트의 "외부에서 전달받은 코드를 실행하는 기능은 사용할 수 없어요(예: `eval` 등)"가 Google Maps JS API의 스크립트 주입에도 걸리는지 문서로 확인되지 않는다. 같은 체크리스트가 지도를 제스처 확대·축소의 예외로 인정하고 있고 이미 Google Fonts를 CDN에서 받고 있어서 대상이 아니라고 읽었지만, 확정은 심사로만 된다. 걸리면 대안은 Static Maps API다(설계 문서 §3).

### 셸 개편이 남긴 미해결 (2026-08-10)

거의 다 실기기로만 확인된다. **각 항목에 「어떻게 확인하나」를 함께 적었다** — 「실기기 필요」만 적힌 항목은 다음 사람이 쓸 수 없다.

- **시트 드래그와 지도 팬 제스처가 충돌하는지** — 손잡이는 `touch-none`으로 그 위 터치를 가로채고 지도는 `gestureHandling: 'greedy'`로 모든 제스처를 먹는다. 손잡이 바로 위아래가 지도라 경계에서 손가락이 어느 쪽으로 잡히는지는 웹뷰 구현에 달렸다. **확인법:** 토스 웹뷰에서 손잡이를 위아래로 열 번 끌어 시트가 매번 붙는지 본다. 지도가 대신 팬되면 `pointercancel`이 오는 것이므로 시트가 「끌었는데 안 붙었다」로 남는다(그 경로는 이미 방어돼 있다 — 위 「바텀시트」 참고).
- **시트 내용 스크롤이 시트 드래그로 새는지** — 드래그를 손잡이에서만 받고 내용 상자에 `overscroll-contain`을 걸어 1차 방어했지만, iOS Safari/WKWebView의 러버밴딩이 이걸 존중하는지는 다르다. **확인법:** 전체(92%)에서 상세를 맨 위까지 스크롤한 뒤 계속 아래로 당겨 본다. 시트가 따라 내려오거나 뒤 지도가 움직이면 새는 것이다.
- **iOS 안전 영역과 시트 바닥** — 시트가 `bottom-0`에 붙는데 코드 어디에도 `env(safe-area-inset-bottom)` 처리가 없고 `index.html`의 viewport에 `viewport-fit=cover`도 없다. 후자 때문에 iOS가 웹뷰를 안전 영역 안에 두어 괜찮을 가능성이 높지만 확인은 안 됐다. **확인법:** 홈 인디케이터가 있는 기기에서 시트 맨 아래 항목(목록의 마지막 행, 상세의 `pb-6`)이 인디케이터에 가리는지 본다. 가리면 시트 루트가 아니라 내용 상자에 `pb-[env(safe-area-inset-bottom)]`을 준다.
- **`h-dvh`가 토스 웹뷰에서 실제로 뷰포트를 주는지** — 셸이 `<main className="h-dvh">` 하나가 되면서 지도 높이가 전부 여기 달려 있다. 이 값이 틀어지면 `size-full`인 홈 루트가 auto 부모를 만나 **지도가 0px로 접힌다.** 헤드리스 크롬(390×844)에서는 정확히 뷰포트를 받았지만 iOS 주소창·안전 영역이 낀 실기기는 미확인. **확인법:** 웹뷰에서 `document.querySelector('main').getBoundingClientRect().height`를 `window.innerHeight`와 비교한다.
- **손잡이 히트 영역이 지도 위 20px을 삼킨다** — 의도한 교환이지만 공짜는 아니다. `peek`(16%)에서는 화면 84% 지점을 가로지르는 죽은 띠가 되어 그 줄의 마커 탭도 지도 팬도 먹지 않는다. **확인법:** `peek`에서 시트 바로 위 20px에 마커를 걸치게 지도를 옮긴 뒤 그 마커를 눌러 본다. 안 열리면 이 띠 때문이다. 고친다면 늘린 20px을 위가 아니라 좌우로 넓히는 쪽이다.
- **여기 적힌 실측은 전부 뷰포트 390×844 **한 크기**에서 잰 것이다.** 아래 두 항목(「필터 해제」 노출, FAB 겹침)은 640·713px에서 다시 재라고 적혀 있는데 **그 크기로는 아직 안 쟀다.** 작은 화면일수록 나빠지는 쪽이라 숫자가 그대로일 것으로 기대하면 안 된다.

- **FAB이 손잡이 히트 영역의 오른쪽 구석과 겹친다**(`peek`·`half`) — 겹치는 양이 `20px − 0.02×컨테이너높이`라 화면이 작을수록 는다(800px에서 4px, 713px에서 8px). 폭 48px짜리 구석이고 손잡이의 보이는 띠는 가운데 36px이라 실제로 막지는 않는다고 봤다. `full`은 FAB을 아예 안 그리므로 해당 없다. **확인법:** 작은 기기에서 손잡이 오른쪽 끝을 눌러 시트 단계가 굴러가는지 본다.
- ~~**`preventScroll` 없이 포커스를 옮기면 시트가 튀는지**~~ — **확인법을 그대로 실행해 정리했다.** 헤드리스 크롬(390×844) 실측: `scrollTop=200`에서 `focus({ preventScroll: true })`도 200, `focus()`도 200이다. **옵션 유무가 스크롤을 바꾸지 않는다** — 포커스 대상이 스크롤 컨테이너의 첫 자식이라 브라우저가 끌어올 것이 애초에 없다. 「이 옵션이 없으면 시트가 튄다」는 근거는 거짓이었고 주석에서 지웠다. 옵션은 남긴다: 이제 우리가 `scrollTop`을 0으로 정하므로 브라우저가 그 위에 제 스크롤을 얹지 않게 하는 진짜 이유가 생겼다.

  **그런데 그 확인을 하다가 진짜 문제가 나왔다** — 아래 「뷰가 갈려도 시트 스크롤이 남던 것」이다. 미해결 항목의 확인법을 실행하는 것은 그 항목을 지우는 일만이 아니다.
- **포커스 상자에 포커스가 왔을 때 스크린리더가 무엇을 읽는지** — `tabindex="-1"`인 익명 div는 암묵 role이 `generic`이라 아무것도 안 읽거나 상자 안 텍스트를 통째로 읽을 수 있다(목록 뷰면 명소 30곳). 이름을 주려면 `role="group"`(또는 `region`)이 함께 와야 한다 — `generic`은 name-prohibited라 `aria-label`만 얹으면 조용히 무시된다. **확인법:** VoiceOver/TalkBack을 켜고 명소를 연다. 목록 전체가 낭독되면 `role="group"` + `aria-label`을 함께 붙인다.
- **내용과 함께 DOM에 삽입되는 live region을 일부 보조기술이 놓친다** — 빈 목록 안내는 상자 자체가 조건부 렌더라 `role="status"`가 내용과 **동시에** 나타난다. 표준 완화책은 리전을 상시 렌더하고 안쪽 텍스트만 조건부로 두는 것이다. 확인할 수단이 없어 결함이 아니라 미해결로 남긴다. **확인법:** 실기기에서 빈 「내 장소」를 눌러 안내가 낭독되는지 듣는다 — 안 들리면 이것부터 의심하라.
- **빈 목록 안내의 `role="status"`가 타이핑 중 얼마나 시끄러운지** — 검색어 한 글자마다 문구가 바뀌어 다시 낭독된다. 표준 패턴이지만 체감은 실기기 몫이다. 「내 장소일 때만」으로 좁히는 대안은 규칙이 하나 느는 쪽이라 택하지 않았다. **확인법:** 스크린리더를 켜고 결과가 0인 검색어를 한 글자씩 친다.
- **빈 「내 장소」 안내의 「필터 해제」 버튼이 `half`에서 4.2px만 노출된다** — **이 숫자는 낡았다.** 2026-08-12에 `SHEET_RATIO.half`가 0.46 → 0.56으로 올라 시트에 85px이 생겼으므로 지금은 온전히 보일 가능성이 높다. 아래 내용은 0.46 기준이고, 아직 다시 재지 않았다. (헤드리스 크롬 390×844 실측) — 문구(48px)와 선택된 칩(40px)은 온전히 보이고 칩을 다시 눌러 해제할 수 있으므로 갇히지는 않는다. 원인은 빈 목록 문구 위에 요약 스트립·위치 안내·카테고리·정렬이 얹힌 목록 배치다. **확인법:** 뷰포트 높이를 640·713·800px로 바꿔 가며 버튼의 `getBoundingClientRect().bottom`을 시트 하단과 비교한다. 작은 기기에서 완전히 가리면 문구를 목록 위가 아니라 스트립 바로 아래로 올리는 것이 수정 방향이다.
- **`releasePointerCapture`가 던지는 브라우저가 있는지** — 던지면 손잡이가 영구히 죽는 경로를 `try/catch`로 막아 뒀지만 실제로 던지는지는 미확인. 막아 둔 이상 확인 못 해도 손해는 없다.
- **즐겨찾기 별의 접근성 이름이 실기기에서 들리는지** — 목록 행의 별을 `<span role="img" aria-label="즐겨찾기한 곳">`로 고쳤으나 jsdom은 `generic`의 name-prohibited를 모형화하지 않아 테스트로는 검증 불가. **확인법:** 스크린리더로 즐겨찾기한 행을 훑어 「즐겨찾기한 곳」이 들리는지 듣는다.
- ~~**인구 구성 필드의 실제 형태**~~ — **해소(2026-08-13).** `"50.0"` 소수 한 자리 문자열이다. 위 「확인된 값」 표 참고.
- **필터 칩 이름 셋이 참고 앱 「서울 인파레이더」의 것과 같다** — 「아이와 나들이」·「데이트」·「지금 핫플」. **사용자가 이번 브랜치에서는 그대로 두기로 정했다.** 바꿀 거면 정본은 `src/domain/presets.ts`의 `PRESETS`다 — 칩과 빈 목록 문구가 모두 `filterLabel()`을 통해 읽으므로 거기 한 곳만 고치면 화면은 따라온다. 이름을 문자열로 박아둔 테스트가 `FilterChips.test.tsx`·`HomeScreen.test.tsx`에 있어 함께 고쳐야 한다. 「내 장소」는 `PRESETS`가 아니라 같은 파일의 `NON_PRESET_LABEL`에 있다.
- **프리셋 이름 셋의 정의가 사용자 기대와 맞는지** — 아래 「알고 있지만 아직 안 고친 것」 참고. 이름 문제와 별개다.

## UI 감사 (2026-08-12)

화면 전체를 훑고 넷을 고쳤다. 측정은 **390px 고정폭 iframe 안에 앱을 띄운 헤드리스 크롬**으로 했다 — `--window-size`가 이 기계에서 500px 아래로 안 내려가 그냥 찍으면 **넓게 그린 화면을 잘라낸 것**이 나온다. 처음 찍은 390·320 캡처가 그래서 전부 거짓이었고(칩·정렬이 잘린 것처럼 보였다), iframe으로 다시 재니 320px에서도 가로 넘침이 없었다. **이 함정을 모르면 없는 결함을 고치게 된다.**

### 고친 것

- **지도 실패 안내가 검색 바·칩 열 뒤에 통째로 가려 있었다.** 안내가 지도 레이어에 있고 오버레이가 `z-20`으로 화면 위 0~112px을 덮는데, `ErrorState`의 `py-10` 때문에 문구가 y≈64px에서 시작했다. 칩 사이 틈으로 글자 조각만 새어 나와 사용자에게는 **설명 없는 빈 화면**이었다. 안내를 오버레이 열의 마지막 칸으로 옮겼다 — 흐름대로 칩 아래에 놓이므로 112px을 어디에도 다시 적지 않아도 된다. 재현법은 `--host-resolver-rules="MAP maps.googleapis.com 127.0.0.1"`.
- **포커스 스타일이 앱 전체에 한 줄도 없었고 검색창은 `outline-none`으로 기본 링마저 지웠다.** `index.css`의 `@layer base`에 `:focus-visible` 링을 두고, 검색 필드는 상자가 `focus-within:ring-2`로 받는다. base 레이어인 것이 핵심이다 — 레이어 밖에 두면 유틸리티를 이겨 검색창이 이중 링을 그린다. 빌드 산출물에서 `:focus-visible`이 base(byte 4173), `.outline-none`이 utilities(byte 7953)에 들어간 것을 확인했다.
- **혼잡도 배지 네 종류가 전부 대비 미달이었다** — 여유 3.32 · 보통 2.86 · 약간 붐빔 3.11 · 붐빔 3.95. 12px/600 글자라 4.5:1이 필요하고 DESIGN.md도 그 숫자를 못박는다. 원인은 **토큰 하나가 두 일을 맡은 것**이다: `--color-calm` 등이 지도 핀(타일 위에서 튀어야 함)과 배지 글자(옅은 `-container` 위라 어두워야 함)를 겸했다. `--color-on-*-container` 넷을 따로 두어 갈랐고 지금은 6.3~6.8:1이다. 지도 핀·마커 알약은 그대로다.
- **기본 `half`에서 목록이 1.8행만 보였다** — 390×844에서 시트를 픽셀로 재니 크롬이 284px을 먹었다(손잡이 24 + 요약 스트립 36 + 위치 안내 72 + 카테고리 56 + 정렬 48, 사이 12씩). 388px 중 104px = 1.8행. 「목록 단계」라는 이름이 무색했다. `SHEET_RATIO.half`를 0.46 → **0.56**으로 올려 473px / 189px = **3.2행**이 됐다(실측. 행 간격이 58이 아니라 59px이라 예측 3.3보다 0.1 적다). 지도는 54%→44%로 줄지만 `peek`(16%)이 지도를 보는 단계로 따로 있다. 구조는 안 건드렸다 — 정렬을 카테고리 줄로 합치거나 위치 안내를 옮기는 길은 남아 있다.

  **상수 하나를 고쳤는데 가드가 넷 물었다**: `RecenterButton`의 파생 규칙 테스트(`bottom-[48%]`가 안 따라옴), 같은 파일의 리터럴 표, `nearestDetent`의 중간값 테스트(0.33이 peek 쪽으로 넘어감), `BottomSheet`의 높이 표. 마지막 것은 리터럴만 고칠 일이 아니었다 — **`0.56 * 100`이 이진 부동소수에서 `56.00000000000001`이라** 그 숫자가 DOM 스타일에 실렸다(0.16·0.46·0.92는 우연히 깨끗해서 여태 안 드러났다). 표현 경계에서 자르는 쪽으로 고쳤다.

- **붐비는 마커가 여유로운 마커 뒤에 숨었다** — 기본 줌에서 중심부 12개쯤이 겹치는데 쌓임 순서가 없어 「붐빔」이 「여유」 뒤로 갈 수 있었다. `domain/map`의 `markerZIndex`로 혼잡도 순으로 쌓는다(정보 없음이 가장 아래). **겹침 자체는 그대로다** — 클러스터링은 안 넣었다.

- **접근성 역할 넷이 실제 동작과 어긋났다.**
  - `SkeletonList`의 `aria-label`이 버려지고 있었다(generic은 name-prohibited). **이 저장소가 같은 규칙을 두 곳에 주석으로 적어 두고 여기만 놓쳤다.** `role="status"` + 내용으로 준다 — 라이브 리전은 이름이 아니라 안쪽 글자를 읽으므로 `aria-label`로는 부족하다. 다만 **리전이 내용과 동시에 삽입되는 문제는 그대로**라 아래 미해결 항목이 여전히 걸린다.
  - `AlertDigest`의 `role="alert"`(assertive)를 걷었다. 이미 받아둔 캐시를 모아 보여주는 목록이라 낭독을 끊을 이유가 없다.
  - `prefers-reduced-motion`을 존중한다. 이 규칙만 `@layer` 밖에 둔다 — `duration-200`이 유틸리티라 같은 레이어에서는 순서 싸움이 된다.
  - **`tablist` 오용 셋**(`FilterChips`·`SortSegmented`·`CategoryFilter`) → `role="group"` + `aria-pressed`. `role="tab"`이 약속하는 tabpanel·`aria-controls`·화살표 이동·roving tabindex가 넷 다 없었고, 한 화면에 tablist가 셋이었다. `radiogroup`도 화살표 이동을 요구하는 데다 칩은 다시 눌러 해제할 수 있어 「반드시 하나」와 어긋난다. 정렬·카테고리는 이름 없는 tablist이기까지 해서 `aria-label`을 함께 붙였다. **테스트 60군데를 옮겼고, 기계적 치환이라 `role="tab"`을 되살려 32개가 죽는 것을 확인했다.**

- **웹폰트를 걷고 플랫폼 기본 스택으로 통일했다.** Hanken Grotesk에 실측 229KB(woff2 4개 × 57KB)를 쓰고 있었는데 **그 폰트에 한글 글리프가 없어** 화면의 대부분은 어차피 기기 폰트였다. 한 줄 안에서 서체가 갈리는 것이 더 나빴다(「추정 인구 88,000~90,000명」). Pretendard도 재 봤고 택하지 않았다 — 동적 서브셋이 조각당 평균 35KB라 200~700KB이고, **미리 서브셋을 뜨는 길은 막혀 있다**(`snapshot.message`와 재난문자가 서울 API에서 오는 임의의 한글이라 글리프를 예측할 수 없다). `system-ui`가 먼저인 이유는 핵심 정보가 큰 숫자여서다. **실기기 확인은 안 됐다** — 헤드리스 크롬(리눅스)의 `system-ui`는 대상 기기 것이 아니다. 확인법은 `index.css` 주석에.

- **히트맵 램프를 명도 단조로 다시 잡았다.** 이웃 대비 1.14/1.02/3.20/1.36 → **1.89/1.66/1.82**. 전용 토큰(`--color-heat-*`)으로 갈랐다 — 핀은 색상으로, 히트맵은 명도로 갈려야 해서 요구가 반대다. 「관측 없음」은 램프에서 뺐다(어떤 회색도 여유와 명도가 겹친다). 채움 대신 테두리라는 **다른 채널**로 말한다 — 색만으로 다섯을 나누려던 것이 애초에 무리였다.

- **길찾기 버튼 대비와 흰 카드 경계.** 네이버가 흰 글자로 2.25:1이었다. 흰 배경에 녹색 글자로 뒤집어도 같은 2.25:1이라(그 녹색이 흰색과 2.25:1이다) 어두운 글자만이 답이었다 — 둘 다 `text-on-surface`(네이버 7.63, 카카오 13.43). 브랜드 색은 raw hex에서 토큰으로 올렸다. 「오늘의 서울」 카드 넷에는 테두리를 줬다(흰 카드 vs 캔버스 1.05:1).

- **정본을 실제와 맞추고 정합을 테스트로 잠갔다.** DESIGN.md가 열 곳에서 사실과 달랐다 — 캔버스 색 자기모순, 카드 스트로크 불일치, 3색 vs 4단계, 정본에 없는 토큰 18개, 없어진 상단바의 글래스모피즘, 예외 없는 48px 규칙(근거 있는 면제 넷이 위반으로만 남아 있었다), 포커스 링·reduced motion 층 자체 부재. 전부 반영했고 면제는 근거와 함께 표로 적었다. **`tokens.test.ts`가 프론트매터와 `index.css`의 색을 양방향으로 대조한다** — 한쪽만 고치면 어긋난 이름을 짚으며 죽는다(양쪽 다 넣어 확인했다). 규칙이 문장으로만 있던 동안 열 곳이 갈렸다는 것이 이 테스트의 근거다.

- **잔가지 넷을 쓸어담았다** (2026-08-12).
  - **지도 마커 알약**이 선명한 톤 + 흰 글자라 3.19~3.77이었다. 네 톤이 전부 중간 명도라 검은 글자로도 안 되고(붐빔 3.56), 옅은 `-container`로 뒤집으면 **밝은 타일 위에서 알약이 사라진다**(1.00~1.06). 어두운 채움 + 흰 글자만이 셋을 만족한다(7.09~8.31, 타일 대비 6.18~7.24). **색상 구분은 핀이 맡는다** — 핀은 선명한 톤 그대로다.
  - **목록 스켈레톤이 카드였다.** 그 자리에 올 것은 구분선 행이라 데이터가 오는 순간 레이아웃이 바뀌었다. `SkeletonRows`를 따로 뒀다.
  - **반경을 전부 토큰으로.** `--radius-sm`도 새로 뒀다 — Tailwind 기본이 마침 같은 값이었을 뿐이다. **정본의 `rounded` 스케일과 Tailwind 이름이 어긋난다**(정본 `lg`=1rem, Tailwind `rounded-lg`=0.5rem). 그래서 Tailwind 기본 이름을 쓰지 않는다.
  - **예측 차트에 축 라벨.** 격자선 넷이 무엇을 뜻하는지 아래 문장에만 있었다. 위치는 격자선과 같은 `yOf()`에서 뽑는다.

- **검색어 지우기 버튼이 16px 타깃이었다** — 패딩도 최소 높이도 없어 버튼이 곧 아이콘 크기였다. WCAG 2.5.8(24px) 미달인데 **하필 빈 목록 문구가 이 버튼을 탈출로로 지목한다**(`HomeScreen`의 `emptyMessage` 주석). `size-10` + `-mr-3`으로 히트 상자만 40×40으로 키웠다 — 빌드된 CSS를 얹은 정적 페이지에서 재니 상자 40×40, 아이콘 중심은 필드 오른쪽 테두리에서 21.0px로 **키우기 전과 같은 자리**, 세로 넘침 없음, 입력과 겹침 없음이다.

`tokens.test.ts`가 `index.css`를 읽어 배지 네 짝의 대비를 계산한다. **토큰 값을 테스트에 옮겨 적지 않았다** — 적으면 색을 고칠 때 테스트도 함께 고치게 되어 아무것도 못 죽인다. 값을 되돌려 3.32로 죽는 것을 확인했다. 주입 경로(`vitest.config.ts`의 `define`)를 쓴 이유는 그 파일 주석에 있다.

### 감사에서 나왔지만 안 고친 것

우선순위대로다. 위 넷과 달리 판단이 필요하거나 파급이 커서 남겼다.

- **지도 마커 알약이 흰 글자로 대비 미달** — 여유 3.77 · 보통 3.19 · 약간 붐빔 3.56(붐빔만 4.83 통과). 게다가 배경이 임의의 지도 타일이라 실제 조건은 계산보다 나쁘다. `-container` 배경 + `on-*-container` 글자로 뒤집으면 이번에 만든 토큰으로 바로 풀리지만, 알약이 옅어져 타일 위에서 안 튈 수 있다 — 뒤집을지 톤을 어둡게 할지는 실기기에서 보고 정할 일이다.
- **중심부 마커가 여전히 겹친다** — 기본 줌에서 30개 중 약 12개가 종로·중구의 60×60px 안에 몰린다. 쌓임 순서는 위에서 고쳤지만 **겹침 자체는 그대로**라 가려진 핀은 탭할 수 없다. 클러스터링을 넣거나 초기 줌을 올리는 쪽인데, 전자는 의존성이 늘고 후자는 「서울 전역이 보인다」는 초기 뷰의 근거(`DEFAULT_ZOOM` 주석)와 부딪힌다.
- **시트를 더 줄일 여지** — `half`를 0.56으로 올려 3.2행이 됐지만 크롬은 여전히 284px이다. 정렬을 카테고리 줄로 합치면 60px, 위치 안내를 요약 스트립 자리로 옮기면 84px을 더 회수한다. 지도를 더 깎지 않고 밀도를 올리는 길이다.
- **「필터 해제」 버튼의 노출량을 다시 재야 한다** — 아래 「셸 개편이 남긴 미해결」에 `half`에서 4.2px만 보인다고 적혀 있는데 그건 `half = 0.46`일 때의 실측이다. 0.56으로 오르며 85px이 생겼으니 지금은 온전히 보일 가능성이 높다. **확인법은 그 항목에 적힌 그대로다.**
- **가로 스크롤 줄에 끝 신호가 없다** — 칩 열·카테고리·추천 캐러셀. 390px에서 「지금 핫플」과 「상권·거리」가 페이드도 그림자도 없이 잘린다. **일부러 안 고쳤다:** 상시 마스크는 끝까지 스크롤한 상태에서 거짓말을 하고(더 있는 것처럼 보인다), 조건부로 하려면 `animation-timeline: scroll()`(토스 웹뷰 지원 미확인)이나 JS 스크롤 리스너가 필요하다. 사소한 어포던스에 상태를 하나 더 만드는 값이라 판단을 미룬다. 고칠 거면 **끝 도달을 감지해 마스크를 끄는** 쪽이어야 한다.
- **요일×시간 표가 「지금」을 열로만 짚고 오늘 요일 행은 표시가 없다** · **접힌 「이곳의 도시 정보」가 안에 뭐가 있는지 안 말한다** · **다크 모드 없음**(팔레트를 한 벌 더 만드는 일이라 잔가지가 아니다) · **중심부 마커 겹침**(쌓임 순서는 고쳤지만 클러스터링은 의존성이나 초기 줌 재검토가 필요하다).
- **지도가 죽어도 「내 주변」 FAB이 그대로 뜬다** — 이번에 안내를 오버레이로 옮기며 눈에 띈 것이다. 누르면 없는 지도의 카메라를 옮기고 시트를 `peek`으로 내려 빈 화면을 드러낸다. 좌표가 없으면 `disabled`라 항상 나는 건 아니다.

## 알고 있지만 아직 안 고친 것 (지도)

코드 리뷰에서 나왔고 근거를 따져 미룬 것들이다. 몰라서 남긴 게 아니다.

- ~~**마커 `position` 객체가 렌더마다 새로 만들어진다**~~ — **해소.** `toMapMarkers`가 `position`까지 만들고 `HomeScreen`이 그 결과를 `useMemo`로 붙든다. 지도를 팬하면 `onCameraChanged`가 화면을 다시 그리지만, `visible`이 같으면 `markers`도 같은 배열이라 vis.gl의 `usePropBinding`이 대입을 내지 않는다. `React.memo`까지는 안 갔다 — 남은 비용은 30개 엘리먼트의 재조정뿐이고 그건 실기기에서 끊기는 걸 본 뒤에 판단한다.
- ~~**지도 카메라가 상세를 다녀오면 초기화된다**~~ — **해소.** 화면이 하나가 되어 지도가 언마운트될 자리가 없다.
- ~~**별 아이콘의 채움 상태가 테스트로 안 덮인다**~~ — **해소.** 상세의 별 토글이 「저장/저장됨」 버튼이 됐고, 목록 행의 별은 즐겨찾기일 때만 그려지는 표시라 채움 상태라는 개념이 없다.
- **`HomeScreen` 함수가 492줄이고 그중 156줄이 주석이다**(파일 전체는 548줄) — 「함수 50줄 미만」 규약 밖이다. `listPane`을 빼면 prop이 10개인 컴포넌트가 생겨 결합이 늘기만 해서 안 쪼갰다. 쪼갤 거면 먼저 prop 수를 세어 보라.
- **`emptyMessage`의 4단 삼항을 `domain/`으로 빼지 않았다** — 네 분기 중 마지막 「조건에 맞는 명소가 없어요」는 **지금 카탈로그로는 도달할 수 없다**(필터도 검색어도 없으면 목록이 그대로 내려오고, 모든 카테고리에 명소가 있다). 빼서 단위 테스트를 붙이면 도달 불가능한 분기를 테스트하게 된다. 나머지 셋은 `HomeScreen.test`가 각각 잠근다. 카테고리가 비는 날(카탈로그 축소·API 화이트리스트 변경)이 오면 그때가 뺄 자리다.
- **`MapUnavailableNotice`가 사용자에게 환경변수 이름을 보여준다** — 키 미설정 문구에 `VITE_GOOGLE_MAPS_API_KEY`가 그대로 들어간다. 개발 중 원인을 즉시 알려주는 값어치가 커서 의도적으로 남겼다. 출시 빌드에서 키가 어긋나면 사용자가 이 문자열을 보게 되므로, 출시 전 점검에서 키 설정을 확인하는 것으로 갈음한다.
- **별을 한 번 누르면 상세 시트가 통째로 다시 그려진다** (2026-08-19 실측) — `HomeScreen`이 `useFavorites()`를 구독하고(칩 개수·걸러진 목록·목록 줄의 별 — 거기서는 화면에 실제로 쓰는 값이라 옳다) 상세가 그 자식이라, 즐겨찾기가 바뀌면 `ForecastChart` SVG·`CityInfoPanel`·주차/따릉이 목록이 다 다시 그려진다. **별 4번에 각각 8번, 클릭 한 번이 약 40ms**(390×844, dev 빌드. 프로덕션은 더 빠르고 실기기는 더 느리다). 시트 안 DOM 노드는 882개.

  `ActionButtons`로 구독을 내려 봤지만 **범위는 그대로였다**(이동 전후 똑같이 8번) — 조상이 여전히 구독하기 때문이다. 그 이동은 남겨 뒀다: 연쇄를 끊는 유일한 길인 「상세 부분트리를 `memo`로 막기」를 할 때, 상세 안에 구독이 남아 있으면 그 `memo`가 조용히 무력해진다.

  **안 고친 이유:** `memo`가 물려면 `HomeScreen`이 넘기는 콜백 넷(`showList`·`openArea`·`showFacilityOnMap`·`toggleCctv`)을 먼저 `useCallback`으로 안정화해야 한다 — 지금은 전부 렌더마다 새로 만들어지는 `function` 선언이다. 화면의 중심을 건드리는 변경이라 **실기기에서 별 탭이 끊기는 것을 본 뒤에** 하는 편이 맞다(마커 `React.memo`를 미룬 것과 같은 판단). 잴 때는 위 숫자를 기준선으로 쓰라.
- **프리셋 정의가 사용자 기대와 맞는지 확인되지 않았다** — 「데이트」 태그를 19곳에 붙인 것은 가정이다(청담동 명품거리를 기대할 수도 있다). 「지금 핫플」도 `붐빔`만이라 좁을 수 있는데, 실데이터에서 `붐빔`이 하루 중 몇 시간이나 나오는지 봐야 `약간 붐빔`까지 넓힐지 정할 수 있다. 목업으로는 판단할 수 없다.

## 진행하며 실제로 잡은 문제

같은 실수를 반복하지 않으려고 남긴다. 대부분 계획서에 있던 결함이고, 몇 개는 우리가 쓴 테스트 자체의 결함이다.

| 문제 | 영향 | 처리 |
| --- | --- | --- |
| Tailwind가 `stitch_ui/` 시안까지 스캔 | CSS 39.5KB → 쓰지 않는 유틸리티 포함 | `@source not`으로 제외 |
| `formatDistance` 경계 버그 | 997m가 "1000m", 1000m가 "1.0km" | 반올림을 분기 앞으로 |
| `isCalm` 이름 모순 | `isCalm('보통')`은 true인데 `congestionTone('보통')`은 'normal' | `isUncrowded`로 개명 |
| 응답 `AREA_NM` 미검증 | sample 키가 뭘 요청하든 광화문을 주는데 앱이 모름. 카탈로그 오타도 영구 은폐 | `expectedName` 대조 + 전용 에러 |
| `Number('')`이 0 | 서울 API의 빈 값이 "추정 인구 0~0명"으로 표시 | 정규식 검증으로 교체 |
| 시각 정규식이 범위 미검사 | `"99:99"` 통과 → "99시엔 여유 예상" | 시(hour) 범위 제한 |
| 서울 API 에러 봉투 폐기 | `INFO-200 해당하는 데이터가 없습니다`를 형태 불평으로 대체 | 전용 에러로 보존 |
| 목업 예측이 톱니파 | 모든 명소가 4시간 내 여유 시간대를 가짐 → 빈 상태를 목업으로 볼 수 없음 | 시간별 해시로 상관 제거 |
| 계획서의 `getCurrentLocation` | web-framework 3.0.1에서 `@deprecated` | `Device.getLocation`으로 교체 |
| 차트 라벨 인덱스 `[0, mid, last]` | 예측이 2개면 `[0,1,1]` → 같은 React key가 두 번 | `Set`으로 중복 제거 |
| 길찾기·공유가 웹 표준만 사용 | 토스 웹뷰에서 막히면 버튼이 조용히 죽음 | `src/platform/links.ts` — 브리지 우선, 실패 시 웹 표준 폴백 |
| 항상 참인 테스트 2건 | "입력 배열 불변", "없는 명소는 조회 안 함" 둘 다 무엇을 해도 통과 | 변이 테스트로 발견 → falsifiable한 단언으로 교체 |
| `App.test.tsx`가 실제 토스 브리지 호출 | jsdom엔 브리지가 없어 SDK 동작에 따라 정렬 결과가 달라짐 | SDK 목업으로 고정 |
| ESLint가 `coverage/` 스캔 | 커버리지를 한 번 돌리면 lint가 계속 경고 | `globalIgnores`에 추가 |
| `SeoulApiError`를 전부 재시도 불가로 묶음 | `ERROR-500`·`ERROR-600`은 잠시 뒤 성공할 수 있는데, 서울 API가 1초 삐끗한 것만으로 "정보 없음"이 뜸 | 두 코드만 재시도. `ERROR-601`(SQL 오류)은 결정적 버그라 제외 |
| 위치 훅이 화면 안에 있었음 | 상세에서 뒤로 갈 때마다 GPS를 새로 켬. 권한 미정 사용자에겐 팝업이 반복 | `LocationProvider`로 앱 수준에 한 번만 |
| 시안을 참고하지 않고 계획서만 따름 | DESIGN.md 색상 47개 중 17개만 반영, 타이포 스케일 0개. 화면이 시안이 아니라 평범한 목록처럼 보임 | 토큰 전량 이관 + 컴포넌트의 raw Tailwind 크기 전부 교체 |
| 항상 참인 테스트 3번째 | 원소 2개짜리 정렬 테스트가 "null을 뒤로"의 반대 가지를 태우지 않아, 정렬 방향을 뒤집어도 통과 | 원소를 5개로 늘려 비교 함수가 양방향으로 호출되게 함 |
| 더보기 쿼터 계산 오류 | "호출이 안 는다"는 전제가 교체 방식일 때만 맞는데, 실제로는 추가 엔드포인트라 최악 1,440회/일 | `CITYINFO_CACHE_TTL_SECONDS`로 분리하고 STATE에 명시 |

### 지도 홈 개편에서 잡은 것 (2026-08-07)

계획서의 코드 블록을 그대로 옮겼다면 전부 통과했을 것들이다. AGENTS.md의 "계획 문서를 다룰 때"가 왜 있는지를 다시 보여준다.

| 문제 | 영향 | 처리 |
| --- | --- | --- |
| 계획서 Task 6의 "붐비는 순 = 여유로운 순의 역순" 테스트 | 픽스처에 동점(여유 2곳)이 있어 **구현이 옳아도 실패**한다. 안정 정렬이 동점의 원래 순서를 지키므로 목록을 통째로 뒤집은 것과는 절대 같아지지 않는다 | 이름 순서 대신 혼잡도 수열로 비교 |
| 계획서 Task 6의 "스냅샷 없는 항목이 뒤로" 테스트 | 픽스처에 스냅샷 없는 항목이 아예 없어 `at(-1)`이 항상 non-null | 전용 픽스처를 따로 만들고 동점·null을 일부러 넣음 |
| 계획서 Task 2의 변이 지시 | "빈 문자열 분기를 지우면 실패한다"고 했지만 통과했다. `''.includes('')`가 항상 참이라 `toEqual`로는 못 가른다 | `toBe`로 참조 동등성을 고정 — 분기의 실제 값어치가 그것이다 |
| `snapRatio` 안의 `clampRatio`가 미검증 | 범위 밖 값은 스냅 계산이 이미 처리해서 clamp를 지워도 통과. 실제로 갈리는 건 `NaN` 하나뿐 | NaN이 최소가 아니라 기본값으로 떨어지는 테스트 추가 |
| 계획서가 즐겨찾기 저장소를 `localStorage`로 한정 | SDK에 `Storage.getItem/setItem`이 실제로 있다(앱 종료 후에도 유지). 계획서는 "문서에서 확인 안 되면"이라는 조건부였는데 확인이 됐다 | `links.ts`와 같은 브리지 우선 폴백으로 구현 |
| `useFavorites`의 경쟁 상태 | 초기 읽기가 끝나기 전에 별을 누르면 늦게 온 저장 목록이 그 토글을 덮는다. 사용자에겐 별이 눌렸다 저절로 풀리는 것으로 보인다 | `touchedRef` 가드. 「더보기」의 늦은 좌표 문제와 같은 종류다 |
| `TopAppBar`와 화면 제목이 둘 다 "오늘의 서울" | 같은 제목이 두 줄로 겹쳐 보인다 | 상단바는 앱 이름만, 제목은 각 화면이 든다 |
| 프리셋 개수 규칙이 홈에서 미검증 | 옛 `MapScreen.test.tsx`가 지켜주던 규칙인데 그 파일이 삭제된다. 걸러진 목록으로 세면 칩 하나를 고르는 순간 나머지가 0으로 굳는다 | 테스트를 `HomeScreen.test.tsx`로 옮김 |
| 붐빔 TOP과 여유 TOP을 바꿔치기해도 통과 | 제목만 확인하는 테스트라 두 목록의 내용이 서로 바뀌어도 못 잡는다 | 첫 줄의 혼잡도 배지를 고정 |

### 셸 개편에서 잡은 것 (2026-08-10)

**대부분 리뷰가 잡았다.** 계획서 코드를 그대로 받아썼으면 전부 남았을 결함들이다.

| 문제 | 영향 | 처리 |
| --- | --- | --- |
| `residentLabel`이 못 읽은 0으로 단정 | 상주비율 필드가 비어 오면 0이 되는데, 그 0을 근거로 「동네 생활권」이라고 써 버렸다. 못 읽은 것과 실제 0%가 구분되지 않는다 | 0을 단정의 근거로 쓰지 않는다 |
| `Number('0x1f') === 31` | 관대한 파서가 「없는 값」이 아니라 **그럴듯한 틀린 값**을 만든다(`'1e1'` → 10, `'+50'` → 50). 화면에 비율 31%가 뜬다 | 정규식(`^-?\d+(?:\.\d+)?$`)으로 먼저 거른다. `cityInfoSchema`·`schema`도 같은 이유로 정규식을 쓴다 |
| `pointercancel`을 확정으로 다룸 | 취소된 제스처가 시트를 `full`로 밀어 올린다. cancel의 `clientY`가 0으로 오면 비율이 1.0이 된다 | 취소는 「없던 일」로 되돌린다. 단계를 바꾸지 않는다 |
| 목록 개수를 `favorites.length`로 셈 | 카테고리로 좁혔거나 카탈로그에서 이름이 바뀐 곳까지 세어, 칩에 2가 뜨는데 목록은 0인 화면이 된다 | `filterCounts`가 `filterAreas`를 그대로 부른다 — 개수와 목록이 같은 술어를 쓰는 것이 구조로 보장된다 |
| 연령 막대를 실제 합으로 정규화 | 합이 100이 아닌 응답에서 **없는 분포를 지어낸다.** 8칸 중 3칸만 읽히면 그 셋이 100%를 나눠 갖는다 | 분모를 `Math.max(합, 100)`으로. 합이 모자라면 모자란 대로 여백이 남고, 100을 넘으면 실제 합으로 되돌아가 압축된다 |
| 한쪽만 읽힌 성별을 사실로 적음 | `rate()`가 칸마다 따로 0을 떨어뜨려 한쪽만 읽히는 일이 둘 다 실패하는 것보다 흔한데, 그 0을 그대로 그리면 「남 100% · 여 0%」가 된다 | `hasGenderSplit`이 둘 다 0 초과일 때만 참. 「그릴 게 있나」의 판정을 `domain/composition.ts` 한 곳이 소유한다 |
| 파일을 지우며 잃을 뻔한 보호 | `FavoritesScreen.test`의 「혼잡도 조회가 실패해도 담은 목록은 보여준다」를 저쪽이 **잡고 있지 않았다.** 「같은 이름의 테스트가 저쪽에 있는가」로는 안 걸린다 | 옮겨 붙이고 변이로 확인. 아래 AGENTS.md 「테스트를 지울 때」 |
| Task 9의 「검색 바 + 칩 열 = 0~88px」 | 검색 바의 세로 패딩을 빠뜨린 값이었다. 실제로는 0~112px(64 + 4 + 44) | Task 10에서 헤드리스 크롬으로 실측해 정정. 결론(「full에서는 안 그린다」)은 오히려 강해졌다 |
| Task 9의 「`RecenterButton`은 실기기에서 언제나 잘린다」 | 상단바·탭바가 빠져 컨테이너가 `100dvh − 7.5rem`에서 `100dvh`가 되면서 필요 뷰포트가 920px → 800px로 내려가 **거짓이 됐다** | 결정(`full`에서 안 그린다)은 유지하되 근거를 다시 썼다 — 이제 근거는 「작은 기기에서는 여전히 잘리고, 화면 크기로 갈라 그리면 규칙이 둘이 된다」이다 |
| 포커스 요청을 `ref`로 들었음 | 세 setter가 전부 같은 값이라 React가 렌더를 건너뛰면 신호만 굳어 있다가 **나중에 아무 렌더에서 터진다**(지도 팬·창 복귀 refetch). 사용자에겐 포커스가 제멋대로 튀는 것으로 보인다 | 상태 카운터로 옮겼다. 증가값은 언제나 새 값이라 「요청했으면 반드시 한 번 돈다」가 보장된다 |
| 「뷰가 달라졌는가」로 포커스를 판단 | 검색어를 치면 선택이 풀려 상세→목록 전환이 일어나는데 그건 타이핑의 부수 효과다. 거기서 포커스를 가져가면 **첫 글자만 입력되고 둘째부터 사라진다**(실제로 그랬다) | 상태를 비교하지 않고 「옮겨 달라고 말한 조작」만 옮긴다 |

## 1차에서 의도적으로 뺀 것

**지도 화면은 완료(2026-08-05)됐다** — 카카오·네이버가 아니라 Google Maps JavaScript API로 붙였다. 설계와 근거는 `docs/superpowers/specs/2026-08-04-map-tab-design.md`.

| 항목 | 이유 |
| --- | --- |
| ~~도시정보(더보기) 화면~~ | **완료(2026-08-07).** `citydata` 한 서비스로 붙였고, 지금은 명소 상세 안에 펼쳐진 절들이다 |
| ~~인구 구성~~ | **완료(2026-08-10).** `citydata_ppltn`의 같은 응답에 있어 추가 호출이 0이다 |
| 요일×시간 히트맵 | 서울 API는 현재와 예측만 준다. 과거 데이터 누적용 저장소 필요 |
| 어제 대비 비교 | 위와 같은 이유 |
| CCTV 영상 | 별도 API, 웹뷰 재생 제약 검증 필요 |

**탭은 없다.** 화면이 하나라 갈 곳이 남지 않았다 — 2026-08-10 개편.
