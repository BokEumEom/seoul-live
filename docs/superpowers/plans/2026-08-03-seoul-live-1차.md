# 서울 라이브 1차 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 서울시 실시간 도시데이터로 명소 혼잡도를 보여주는 토스 미니앱의 「내 주변」·「혼잡예보」 두 화면을 만든다.

**Architecture:** React 19 + Vite 정적 번들이 토스 웹뷰에서 돌고, 서울 OpenAPI가 HTTP 전용이라 Vercel Function이 HTTPS 중계와 CDN 캐시를 담당한다. 순수 도메인 로직(`src/domain`)은 React를 모르게 격리해 단위 테스트로 덮고, 네트워크는 `src/data`에 가둔다.

**Tech Stack:** React 19, TypeScript, Vite 8, TanStack Query v5, zod, Tailwind CSS v4, React Router v7, Vitest + Testing Library, Vercel Functions

**설계 문서:** `docs/superpowers/specs/2026-08-03-seoul-live-design.md`

---

## 시작 전 알아야 할 것

**이 계획서의 코드는 출발점이지 받아쓰기 대상이 아니다.** 실제로 Task 6의 `formatDistance`에 경계 버그가 있었고(997m가 "1000m"으로 표시됨), 그대로 옮겨져 코드 리뷰에서야 잡혔다. 코드 블록에서 결함이 보이면 옮기지 말고 지적할 것.


**서울 OpenAPI 기본형**
```
http://openapi.seoul.go.kr:8088/{인증키}/json/citydata_ppltn/1/5/{AREA_NM}
```
- `AREA_NM`은 필수다. 빼면 500 에러가 난다.
- 인증키 자리에 `sample`을 넣으면 지역명과 무관하게 항상 광화문·덕수궁이 돌아온다. 실데이터 검증에 못 쓴다.
- HTTPS가 없다. 80·443 포트가 닫혀 있다. 브라우저에서 직접 호출하면 mixed content로 차단된다.
- 일반인증키는 하루 1,000회 제한이다. 활용갤러리 등록 시 해제된다.

**응답 형태 (실제 확인함)**
```json
{
  "SeoulRtd.citydata_ppltn": [{
    "AREA_NM": "광화문·덕수궁",
    "AREA_CD": "POI009",
    "AREA_CONGEST_LVL": "보통",
    "AREA_CONGEST_MSG": "사람이 몰려있을 수 있지만 크게 붐비지는 않아요...",
    "AREA_PPLTN_MIN": "42000",
    "AREA_PPLTN_MAX": "44000",
    "PPLTN_TIME": "2026-08-03 14:35",
    "FCST_YN": "Y",
    "FCST_PPLTN": [
      { "FCST_TIME": "2026-08-03 16:00", "FCST_CONGEST_LVL": "약간 붐빔",
        "FCST_PPLTN_MIN": "42000", "FCST_PPLTN_MAX": "44000" }
    ]
  }]
}
```
숫자가 전부 **문자열**로 온다. 스키마 단계에서 변환한다.

**토스 번들과 프록시는 오리진이 다르다.** 번들은 토스가 서빙하고 프록시는 Vercel에 있다. 프록시 응답에 CORS 헤더가 없으면 전부 실패한다. Task 11에서 반드시 넣는다.

**인증키가 아직 없다.** Task 1~18은 전부 목업 데이터로 완주할 수 있게 설계했다. 실데이터 전환은 `VITE_USE_MOCK` 환경변수 하나만 바꾸면 된다.

---

## File Structure

| 경로 | 책임 |
|---|---|
| `src/domain/congestion.ts` | 혼잡도 4단계 정의·파싱·정렬순위·색상 톤. React 모름 |
| `src/domain/distance.ts` | 하버사인 거리, 거리 포맷, 도보 시간. React 모름 |
| `src/domain/types.ts` | `Coords`, `AreaSnapshot`, `Forecast` 등 공용 타입 |
| `src/data/areas.ts` | 명소 30곳 정적 카탈로그 (좌표·카테고리) |
| `src/data/schema.ts` | zod 스키마. 원본 응답 검증 + 숫자 변환 + `AreaSnapshot` 변환 |
| `src/data/mock.ts` | 목업 응답 생성기 |
| `src/data/client.ts` | 프록시 fetch. 목업 분기 |
| `src/data/queries.ts` | TanStack Query 훅 |
| `src/hooks/useCurrentLocation.ts` | 토스 위치 권한·좌표 |
| `src/hooks/useNearbyAreas.ts` | 거리 계산·정렬·필터·추천 선정 |
| `src/components/common/*` | `CongestionBadge`, `ErrorState`, `SkeletonCard` |
| `src/components/layout/*` | `TopAppBar`, `BottomTabBar` |
| `src/components/nearby/*` | `CategoryFilter`, `RecommendationCard`, `AreaListItem` |
| `src/components/forecast/*` | `ForecastChart`, `ActionButtons` |
| `src/domain/forecast.ts` | 예측 배열에서 한산해지는 시각 찾기 |
| `src/screens/NearbyScreen.tsx` | 내 주변 화면 조립 |
| `src/screens/ForecastScreen.tsx` | 혼잡예보 화면 조립 |
| `api/citydata.ts` | 단일 명소 프록시 |
| `api/citydata-bulk.ts` | 복수 명소 프록시 |
| `api/_lib/seoul.ts` | 서울 API 호출 공통 로직 |

---

## Task 1: 의존성 설치와 테스트 환경

**Files:**
- Modify: `package.json`
- Create: `vitest.config.ts`
- Create: `src/test/setup.ts`

- [ ] **Step 1: 런타임 의존성 설치**

```bash
npm install @tanstack/react-query zod
```

라우터는 설치하지 않는다. 화면이 둘뿐이라 Task 19에서 상태 전환으로 처리한다.

- [ ] **Step 2: 개발 의존성 설치**

```bash
npm install -D vitest @vitest/coverage-v8 jsdom @testing-library/react @testing-library/jest-dom @testing-library/user-event tailwindcss @tailwindcss/vite
```

- [ ] **Step 3: `vitest.config.ts` 생성**

```typescript
import { defineConfig } from 'vitest/config'
import react from '@vitejs/plugin-react'

export default defineConfig({
  plugins: [react()],
  test: {
    environment: 'jsdom',
    globals: true,
    setupFiles: ['./src/test/setup.ts'],
    coverage: {
      provider: 'v8',
      include: ['src/**/*.{ts,tsx}'],
      exclude: ['src/test/**', 'src/**/*.test.{ts,tsx}', 'src/main.tsx'],
      thresholds: { lines: 80, functions: 80, branches: 75, statements: 80 },
    },
  },
})
```

- [ ] **Step 4: `src/test/setup.ts` 생성**

```typescript
import '@testing-library/jest-dom/vitest'
```

- [ ] **Step 5: `package.json` scripts에 테스트 명령 추가**

`"scripts"` 안에 다음 두 줄을 추가한다.

```json
"test": "vitest run",
"test:watch": "vitest",
"test:coverage": "vitest run --coverage"
```

- [ ] **Step 6: 설치 확인**

Run: `npm test`
Expected: `No test files found` 메시지와 함께 종료. 에러 없이 vitest가 실행되면 성공이다.

- [ ] **Step 7: 커밋**

```bash
git add package.json package-lock.json vitest.config.ts src/test/setup.ts
git commit -m "chore: TanStack Query·zod·Tailwind·Vitest 의존성 추가"
```

---

## Task 2: Tailwind v4와 디자인 토큰

**Files:**
- Modify: `vite.config.ts`
- Modify: `src/index.css`
- Delete: `src/App.css`

토큰 값은 `stitch_ui/seoul_flow/DESIGN.md`에서 가져왔다.

- [ ] **Step 1: `vite.config.ts`에 Tailwind 플러그인 추가**

```typescript
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'
import tailwindcss from '@tailwindcss/vite'

export default defineConfig({
  plugins: [react(), tailwindcss()],
})
```

- [ ] **Step 2: `src/index.css` 전체를 교체**

```css
@import "tailwindcss";

@theme {
  --color-primary: #004ac6;
  --color-on-primary: #ffffff;
  --color-primary-container: #2563eb;
  --color-primary-fixed: #dbe1ff;
  --color-secondary-container: #d5e0f8;

  --color-surface: #faf8ff;
  --color-surface-container-lowest: #ffffff;
  --color-surface-container-low: #f3f3fe;
  --color-surface-container: #ededf9;
  --color-surface-container-high: #e7e7f3;

  --color-on-surface: #191b23;
  --color-on-surface-variant: #434655;
  --color-outline: #737686;
  --color-outline-variant: #c3c6d7;

  --color-error: #ba1a1a;
  --color-error-container: #ffdad6;
  --color-on-error-container: #93000a;

  /* 혼잡도 4단계 */
  --color-calm: #059669;
  --color-calm-container: #d1fae5;
  --color-normal: #d97706;
  --color-normal-container: #fef3c7;
  --color-busy: #ea580c;
  --color-busy-container: #ffedd5;
  --color-crowded: #dc2626;
  --color-crowded-container: #fee2e2;

  --font-sans: "Hanken Grotesk", "Pretendard", system-ui, sans-serif;

  --radius-card: 0.5rem;
  --radius-action: 1rem;
}

html, body, #root {
  height: 100%;
}

body {
  margin: 0;
  background-color: var(--color-surface);
  color: var(--color-on-surface);
  font-family: var(--font-sans);
  -webkit-font-smoothing: antialiased;
}
```

- [ ] **Step 3: 스캐폴드 잔재 제거**

```bash
rm src/App.css
```

- [ ] **Step 4: 빌드 확인**

Run: `npm run build:vite`
Expected: 성공. `src/App.tsx`가 아직 `./App.css`를 import하고 있으면 여기서 에러가 난다. 그 경우 `src/App.tsx`의 `import './App.css'` 줄을 지운다.

- [ ] **Step 5: 커밋**

```bash
git add -A vite.config.ts src/index.css src/App.tsx src/App.css
git commit -m "feat: Tailwind v4 도입 및 디자인 토큰 정의"
```

---

## Task 3: 앱인토스 설정 갱신

**Files:**
- Modify: `apps-in-toss.config.ts`

권한 형태는 `node_modules/@apps-in-toss/web-framework/dist/config.d.ts`에서 확인한 것이다.

- [ ] **Step 1: 설정 파일 전체 교체**

```typescript
import { defineConfig } from "@apps-in-toss/web-framework/config";

export default defineConfig({
  appName: "seoul-live",
  brand: {
    primaryColor: "#004ac6",
  },
  permissions: [{ name: "geolocation", access: "access" }],
  webBundleDir: "dist",
});
```

- [ ] **Step 2: 타입 검사**

Run: `npx tsc -b`
Expected: 에러 없음.

- [ ] **Step 3: 커밋**

```bash
git add apps-in-toss.config.ts
git commit -m "feat: 위치 권한 선언 및 브랜드 컬러를 시안 기준으로 변경"
```

---

## Task 4: 공용 타입 정의

**Files:**
- Create: `src/domain/types.ts`

이후 모든 태스크가 이 타입을 참조한다.

- [ ] **Step 1: 타입 파일 작성**

```typescript
export interface Coords {
  readonly lat: number
  readonly lng: number
}

export const CONGESTION_LEVELS = ['여유', '보통', '약간 붐빔', '붐빔'] as const

export type CongestionLevel = (typeof CONGESTION_LEVELS)[number]

export type AreaCategory = '공원' | '쇼핑몰' | '카페' | '문화재' | '기타'

export interface AreaCatalogEntry extends Coords {
  readonly code: string
  readonly name: string
  readonly category: AreaCategory
}

export interface Forecast {
  /** 서울 API 원본 형식. `"2026-08-03 16:00"` — ISO 아님, 타임존 없음 */
  readonly time: string
  /** `time`에서 뽑은 0~23. 화면이 원본 형식을 파싱하지 않게 하려는 것 */
  readonly hour: number
  readonly congestion: CongestionLevel
  readonly populationMin: number
  readonly populationMax: number
}

export interface AreaSnapshot {
  readonly code: string
  readonly name: string
  readonly congestion: CongestionLevel
  readonly message: string
  readonly populationMin: number
  readonly populationMax: number
  readonly observedAt: string
  readonly forecasts: readonly Forecast[]
}

export interface NearbyArea {
  readonly entry: AreaCatalogEntry
  readonly snapshot: AreaSnapshot | null
  readonly distanceMeters: number | null
}
```

- [ ] **Step 2: 타입 검사**

Run: `npx tsc -b`
Expected: 에러 없음.

- [ ] **Step 3: 커밋**

```bash
git add src/domain/types.ts
git commit -m "feat: 도메인 공용 타입 정의"
```

---

## Task 5: 혼잡도 도메인 로직

**Files:**
- Create: `src/domain/congestion.ts`
- Test: `src/domain/congestion.test.ts`

- [ ] **Step 1: 실패하는 테스트 작성**

```typescript
import { describe, expect, it } from 'vitest'
import {
  congestionRank,
  congestionTone,
  isUncrowded,
  parseCongestionLevel,
} from './congestion'

describe('parseCongestionLevel', () => {
  it('서울 API가 주는 4단계를 그대로 인식한다', () => {
    expect(parseCongestionLevel('여유')).toBe('여유')
    expect(parseCongestionLevel('보통')).toBe('보통')
    expect(parseCongestionLevel('약간 붐빔')).toBe('약간 붐빔')
    expect(parseCongestionLevel('붐빔')).toBe('붐빔')
  })

  it('앞뒤 공백을 무시한다', () => {
    expect(parseCongestionLevel('  붐빔 ')).toBe('붐빔')
  })

  it('모르는 값은 null을 준다', () => {
    expect(parseCongestionLevel('매우 붐빔')).toBeNull()
    expect(parseCongestionLevel('')).toBeNull()
  })
})

describe('congestionRank', () => {
  it('여유가 가장 낮고 붐빔이 가장 높다', () => {
    expect(congestionRank('여유')).toBe(0)
    expect(congestionRank('보통')).toBe(1)
    expect(congestionRank('약간 붐빔')).toBe(2)
    expect(congestionRank('붐빔')).toBe(3)
  })

  it('정렬에 쓸 수 있다', () => {
    const sorted = ['붐빔' as const, '여유' as const, '보통' as const].sort(
      (a, b) => congestionRank(a) - congestionRank(b),
    )
    expect(sorted).toEqual(['여유', '보통', '붐빔'])
  })
})

describe('congestionTone', () => {
  it('단계마다 다른 톤을 준다', () => {
    expect(congestionTone('여유')).toBe('calm')
    expect(congestionTone('보통')).toBe('normal')
    expect(congestionTone('약간 붐빔')).toBe('busy')
    expect(congestionTone('붐빔')).toBe('crowded')
  })
})

describe('isUncrowded', () => {
  it('여유와 보통만 한산한 것으로 본다', () => {
    expect(isUncrowded('여유')).toBe(true)
    expect(isUncrowded('보통')).toBe(true)
    expect(isUncrowded('약간 붐빔')).toBe(false)
    expect(isUncrowded('붐빔')).toBe(false)
  })
})
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `npx vitest run src/domain/congestion.test.ts`
Expected: FAIL — `Failed to resolve import "./congestion"`

- [ ] **Step 3: 구현 작성**

```typescript
import { CONGESTION_LEVELS, type CongestionLevel } from './types'

export type CongestionTone = 'calm' | 'normal' | 'busy' | 'crowded'

const TONE_BY_LEVEL: Readonly<Record<CongestionLevel, CongestionTone>> = {
  여유: 'calm',
  보통: 'normal',
  '약간 붐빔': 'busy',
  붐빔: 'crowded',
}

export function parseCongestionLevel(raw: string): CongestionLevel | null {
  const trimmed = raw.trim()
  return (
    CONGESTION_LEVELS.find((level) => level === trimmed) ?? null
  )
}

export function congestionRank(level: CongestionLevel): number {
  return CONGESTION_LEVELS.indexOf(level)
}

export function congestionTone(level: CongestionLevel): CongestionTone {
  return TONE_BY_LEVEL[level]
}

/** 여유·보통을 한산한 것으로 본다. `congestionTone`의 'calm'보다 넓은 범위다. */
export function isUncrowded(level: CongestionLevel): boolean {
  return congestionRank(level) <= congestionRank('보통')
}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `npx vitest run src/domain/congestion.test.ts`
Expected: PASS — 4개 describe, 8개 테스트 통과.

- [ ] **Step 5: 커밋**

```bash
git add src/domain/congestion.ts src/domain/congestion.test.ts
git commit -m "feat: 혼잡도 4단계 파싱·순위·톤 매핑"
```

---

## Task 6: 거리 계산 도메인 로직

**Files:**
- Create: `src/domain/distance.ts`
- Test: `src/domain/distance.test.ts`

- [ ] **Step 1: 실패하는 테스트 작성**

기준값은 서울시청(37.5665, 126.9780)과 강남역(37.4980, 127.0276) 사이 직선거리로, 약 8.7km다.

```typescript
import { describe, expect, it } from 'vitest'
import { formatDistance, haversineMeters, walkingMinutes } from './distance'

const CITY_HALL = { lat: 37.5665, lng: 126.978 }
const GANGNAM = { lat: 37.498, lng: 127.0276 }

describe('haversineMeters', () => {
  it('같은 지점은 0이다', () => {
    expect(haversineMeters(CITY_HALL, CITY_HALL)).toBe(0)
  })

  it('서울시청-강남역은 약 8.7km다', () => {
    const meters = haversineMeters(CITY_HALL, GANGNAM)
    expect(meters).toBeGreaterThan(8300)
    expect(meters).toBeLessThan(9100)
  })

  it('방향이 바뀌어도 거리는 같다', () => {
    expect(haversineMeters(CITY_HALL, GANGNAM)).toBeCloseTo(
      haversineMeters(GANGNAM, CITY_HALL),
      6,
    )
  })
})

describe('formatDistance', () => {
  it('1km 미만은 미터로 표시한다', () => {
    expect(formatDistance(800)).toBe('800m')
    expect(formatDistance(45)).toBe('50m')
  })

  it('1km 이상은 소수점 한 자리 km로 표시한다', () => {
    expect(formatDistance(1200)).toBe('1.2km')
    expect(formatDistance(8700)).toBe('8.7km')
  })
})

describe('walkingMinutes', () => {
  it('시속 4km 기준으로 계산한다', () => {
    expect(walkingMinutes(800)).toBe(12)
    expect(walkingMinutes(2000)).toBe(30)
  })

  it('최소 1분은 보장한다', () => {
    expect(walkingMinutes(10)).toBe(1)
  })
})
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `npx vitest run src/domain/distance.test.ts`
Expected: FAIL — `Failed to resolve import "./distance"`

- [ ] **Step 3: 구현 작성**

```typescript
import type { Coords } from './types'

const EARTH_RADIUS_METERS = 6_371_000
const WALKING_SPEED_KM_PER_HOUR = 4

function toRadians(degrees: number): number {
  return (degrees * Math.PI) / 180
}

export function haversineMeters(from: Coords, to: Coords): number {
  const dLat = toRadians(to.lat - from.lat)
  const dLng = toRadians(to.lng - from.lng)

  const a =
    Math.sin(dLat / 2) ** 2 +
    Math.cos(toRadians(from.lat)) *
      Math.cos(toRadians(to.lat)) *
      Math.sin(dLng / 2) ** 2

  return 2 * EARTH_RADIUS_METERS * Math.asin(Math.min(1, Math.sqrt(a)))
}

export function formatDistance(meters: number): string {
  // 10m 단위로 먼저 반올림한 뒤 분기한다.
  // 분기를 먼저 하면 997m가 "1000m"으로 나온다.
  const rounded = Math.round(meters / 10) * 10
  if (rounded < 1000) {
    return `${rounded}m`
  }
  return `${(rounded / 1000).toFixed(1)}km`
}

export function walkingMinutes(meters: number): number {
  const minutes = Math.round(
    (meters / 1000 / WALKING_SPEED_KM_PER_HOUR) * 60,
  )
  return Math.max(1, minutes)
}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `npx vitest run src/domain/distance.test.ts`
Expected: PASS — 8개 테스트 통과.

- [ ] **Step 5: 커밋**

```bash
git add src/domain/distance.ts src/domain/distance.test.ts
git commit -m "feat: 하버사인 거리 계산과 도보 시간 산출"
```

---

## Task 7: 명소 카탈로그

**Files:**
- Create: `src/data/areas.ts`
- Test: `src/data/areas.test.ts`

서울 API는 좌표를 주지 않는다. 거리 정렬과 카테고리 필터를 위해 직접 관리한다.

> **주의:** `name` 값이 곧 API 호출 키(`AREA_NM`)다. 오타가 나면 해당 명소만 조용히 실패한다. 인증키 발급 후 서울 열린데이터광장의 공식 장소 목록과 대조해 검증할 것.

- [ ] **Step 1: 실패하는 테스트 작성**

```typescript
import { describe, expect, it } from 'vitest'
import { AREA_CATALOG, findAreaByName } from './areas'

describe('AREA_CATALOG', () => {
  it('1차 목표인 30곳을 담는다', () => {
    expect(AREA_CATALOG).toHaveLength(30)
  })

  it('명소 이름이 중복되지 않는다', () => {
    const names = AREA_CATALOG.map((area) => area.name)
    expect(new Set(names).size).toBe(names.length)
  })

  it('모든 좌표가 서울 범위 안에 있다', () => {
    for (const area of AREA_CATALOG) {
      expect(area.lat).toBeGreaterThan(37.4)
      expect(area.lat).toBeLessThan(37.75)
      expect(area.lng).toBeGreaterThan(126.75)
      expect(area.lng).toBeLessThan(127.25)
    }
  })

  it('모든 명소에 카테고리가 있다', () => {
    for (const area of AREA_CATALOG) {
      expect(area.category).toBeTruthy()
    }
  })
})

describe('findAreaByName', () => {
  it('이름으로 명소를 찾는다', () => {
    expect(findAreaByName('강남역')?.category).toBe('기타')
  })

  it('없는 이름은 undefined를 준다', () => {
    expect(findAreaByName('부산역')).toBeUndefined()
  })
})
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `npx vitest run src/data/areas.test.ts`
Expected: FAIL — `Failed to resolve import "./areas"`

- [ ] **Step 3: 카탈로그 작성**

```typescript
import type { AreaCatalogEntry } from '../domain/types'

export const AREA_CATALOG: readonly AreaCatalogEntry[] = [
  { code: 'POI014', name: '강남역', lat: 37.498, lng: 127.0276, category: '기타' },
  { code: 'POI054', name: '홍대입구역(2호선)', lat: 37.5571, lng: 126.9245, category: '기타' },
  { code: 'POI003', name: '명동 관광특구', lat: 37.5636, lng: 126.9827, category: '기타' },
  { code: 'POI009', name: '광화문·덕수궁', lat: 37.5709, lng: 126.9769, category: '문화재' },
  { code: 'POI101', name: '여의도한강공원', lat: 37.5285, lng: 126.9327, category: '공원' },
  { code: 'POI096', name: '반포한강공원', lat: 37.51, lng: 126.9955, category: '공원' },
  { code: 'POI094', name: '뚝섬한강공원', lat: 37.53, lng: 127.07, category: '공원' },
  { code: 'POI066', name: '성수카페거리', lat: 37.5445, lng: 127.0557, category: '카페' },
  { code: 'POI063', name: '북촌한옥마을', lat: 37.5826, lng: 126.983, category: '문화재' },
  { code: 'POI007', name: '경복궁', lat: 37.5796, lng: 126.977, category: '문화재' },
  { code: 'POI010', name: '창덕궁·종묘', lat: 37.5794, lng: 126.991, category: '문화재' },
  { code: 'POI080', name: 'DDP(동대문디자인플라자)', lat: 37.5665, lng: 127.009, category: '기타' },
  { code: 'POI004', name: '이태원 관광특구', lat: 37.5345, lng: 126.9946, category: '기타' },
  { code: 'POI005', name: '잠실 관광특구', lat: 37.5133, lng: 127.1, category: '기타' },
  { code: 'POI090', name: '서울숲공원', lat: 37.5444, lng: 127.0374, category: '공원' },
  { code: 'POI088', name: '남산공원', lat: 37.5512, lng: 126.9882, category: '공원' },
  { code: 'POI102', name: '월드컵공원', lat: 37.5716, lng: 126.8783, category: '공원' },
  { code: 'POI100', name: '어린이대공원', lat: 37.5497, lng: 127.0817, category: '공원' },
  { code: 'POI104', name: '잠실한강공원', lat: 37.518, lng: 127.082, category: '공원' },
  { code: 'POI105', name: '잠원한강공원', lat: 37.5185, lng: 127.0107, category: '공원' },
  { code: 'POI057', name: '가로수길', lat: 37.5203, lng: 127.023, category: '쇼핑몰' },
  { code: 'POI070', name: '연남동', lat: 37.5606, lng: 126.925, category: '카페' },
  { code: 'POI075', name: '인사동', lat: 37.574, lng: 126.9856, category: '문화재' },
  { code: 'POI065', name: '서촌', lat: 37.5788, lng: 126.97, category: '문화재' },
  { code: 'POI069', name: '압구정로데오거리', lat: 37.5273, lng: 127.04, category: '쇼핑몰' },
  { code: 'POI078', name: '청담동 명품거리', lat: 37.525, lng: 127.049, category: '쇼핑몰' },
  { code: 'POI073', name: '영등포 타임스퀘어', lat: 37.517, lng: 126.903, category: '쇼핑몰' },
  { code: 'POI079', name: '해방촌·경리단길', lat: 37.54, lng: 126.988, category: '카페' },
  { code: 'POI058', name: '광장(전통)시장', lat: 37.5701, lng: 126.9997, category: '기타' },
  { code: 'POI093', name: '북서울꿈의숲', lat: 37.6208, lng: 127.0417, category: '공원' },
]

export function findAreaByName(name: string): AreaCatalogEntry | undefined {
  return AREA_CATALOG.find((area) => area.name === name)
}

export const AREA_NAMES: readonly string[] = AREA_CATALOG.map(
  (area) => area.name,
)
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `npx vitest run src/data/areas.test.ts`
Expected: PASS — 6개 테스트 통과.

- [ ] **Step 5: 커밋**

```bash
git add src/data/areas.ts src/data/areas.test.ts
git commit -m "feat: 명소 30곳 좌표·카테고리 카탈로그"
```

---

## Task 8: 응답 스키마와 정규화

**Files:**
- Create: `src/data/schema.ts`
- Test: `src/data/schema.test.ts`
- Modify: `src/domain/types.ts` — `Forecast`에 `readonly hour: number` 추가

서울 API는 숫자를 문자열로 주고 시각도 `"2026-08-03 16:00"`이라는 비표준 형식으로 준다. 둘 다 여기서 흡수한다.

**`hour`를 여기서 뽑는 이유:** 이 값을 안 넘기면 화면 쪽에서 `time.slice(11, 13)` 같은 위치 기반 파싱을 하게 된다. 형식이 바뀌면 조용히 깨지고, 형식에 대한 지식이 여러 파일로 흩어진다.

**`AreaSnapshot`의 `code`·`name` 권위:** 카탈로그(`areas.ts`)의 `name`이 곧 API 호출 키이므로 응답의 `AREA_NM`과 같아야 한다. 화면 표시는 **카탈로그 값을 쓴다**. 응답 값은 검증용이다. 둘이 다르면 카탈로그에 오타가 있다는 뜻이다.

- [ ] **Step 1: 실패하는 테스트 작성**

```typescript
import { describe, expect, it } from 'vitest'
import { parseCitydataResponse } from './schema'

const VALID = {
  'SeoulRtd.citydata_ppltn': [
    {
      AREA_NM: '광화문·덕수궁',
      AREA_CD: 'POI009',
      AREA_CONGEST_LVL: '보통',
      AREA_CONGEST_MSG: '크게 붐비지는 않아요.',
      AREA_PPLTN_MIN: '42000',
      AREA_PPLTN_MAX: '44000',
      PPLTN_TIME: '2026-08-03 14:35',
      FCST_YN: 'Y',
      FCST_PPLTN: [
        {
          FCST_TIME: '2026-08-03 16:00',
          FCST_CONGEST_LVL: '약간 붐빔',
          FCST_PPLTN_MIN: '42000',
          FCST_PPLTN_MAX: '44000',
        },
      ],
    },
  ],
}

describe('parseCitydataResponse', () => {
  it('정상 응답을 도메인 모델로 바꾼다', () => {
    const snapshot = parseCitydataResponse(VALID)
    expect(snapshot.name).toBe('광화문·덕수궁')
    expect(snapshot.code).toBe('POI009')
    expect(snapshot.congestion).toBe('보통')
    expect(snapshot.populationMin).toBe(42000)
    expect(snapshot.populationMax).toBe(44000)
    expect(snapshot.forecasts).toHaveLength(1)
    expect(snapshot.forecasts[0].congestion).toBe('약간 붐빔')
    expect(snapshot.forecasts[0].populationMin).toBe(42000)
  })

  it('예측이 없어도 빈 배열로 처리한다', () => {
    const withoutForecast = {
      'SeoulRtd.citydata_ppltn': [
        { ...VALID['SeoulRtd.citydata_ppltn'][0], FCST_YN: 'N', FCST_PPLTN: null },
      ],
    }
    expect(parseCitydataResponse(withoutForecast).forecasts).toEqual([])
  })

  it('모르는 혼잡도 값이 오면 던진다', () => {
    const badLevel = {
      'SeoulRtd.citydata_ppltn': [
        { ...VALID['SeoulRtd.citydata_ppltn'][0], AREA_CONGEST_LVL: '초혼잡' },
      ],
    }
    expect(() => parseCitydataResponse(badLevel)).toThrow()
  })

  it('빈 배열이면 던진다', () => {
    expect(() => parseCitydataResponse({ 'SeoulRtd.citydata_ppltn': [] })).toThrow()
  })

  it('형태가 아예 다르면 던진다', () => {
    expect(() => parseCitydataResponse({ RESULT: { CODE: 'ERROR-500' } })).toThrow()
  })
})
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `npx vitest run src/data/schema.test.ts`
Expected: FAIL — `Failed to resolve import "./schema"`

- [ ] **Step 3: 구현 작성**

```typescript
import { z } from 'zod'
import { parseCongestionLevel } from '../domain/congestion'
import type { AreaSnapshot, CongestionLevel, Forecast } from '../domain/types'

// `code: 'custom'`은 zod v3·v4 양쪽에서 동작한다.
// v3의 `z.ZodIssueCode.custom`은 v4에서 제거됐으므로 문자열 리터럴을 쓴다.

const congestionSchema = z.string().transform((value, ctx): CongestionLevel => {
  const level = parseCongestionLevel(value)
  if (level === null) {
    ctx.addIssue({
      code: 'custom',
      message: `알 수 없는 혼잡도 값: ${value}`,
    })
    return z.NEVER
  }
  return level
})

const numericSchema = z.string().transform((value, ctx): number => {
  const parsed = Number(value)
  if (!Number.isFinite(parsed)) {
    ctx.addIssue({
      code: 'custom',
      message: `숫자가 아닌 값: ${value}`,
    })
    return z.NEVER
  }
  return parsed
})

const forecastSchema = z.object({
  FCST_TIME: z.string(),
  FCST_CONGEST_LVL: congestionSchema,
  FCST_PPLTN_MIN: numericSchema,
  FCST_PPLTN_MAX: numericSchema,
})

const areaSchema = z.object({
  AREA_NM: z.string(),
  AREA_CD: z.string(),
  AREA_CONGEST_LVL: congestionSchema,
  AREA_CONGEST_MSG: z.string(),
  AREA_PPLTN_MIN: numericSchema,
  AREA_PPLTN_MAX: numericSchema,
  PPLTN_TIME: z.string(),
  FCST_PPLTN: z.array(forecastSchema).nullish(),
})

const responseSchema = z.object({
  'SeoulRtd.citydata_ppltn': z.array(areaSchema).min(1),
})

// 서울 API는 "2026-08-03 16:00" 형식을 준다. ISO가 아니고 타임존도 없다.
// 이 형식에 대한 의존을 여기 한 곳에 가두고, 바깥에는 파싱된 hour를 넘긴다.
const TIME_PATTERN = /^\d{4}-\d{2}-\d{2} (\d{2}):\d{2}$/

function parseHour(raw: string): number {
  const matched = raw.match(TIME_PATTERN)
  if (matched === null) {
    throw new Error(`예상치 못한 시각 형식: ${raw}`)
  }
  return Number(matched[1])
}

function toForecast(raw: z.infer<typeof forecastSchema>): Forecast {
  return {
    time: raw.FCST_TIME,
    hour: parseHour(raw.FCST_TIME),
    congestion: raw.FCST_CONGEST_LVL,
    populationMin: raw.FCST_PPLTN_MIN,
    populationMax: raw.FCST_PPLTN_MAX,
  }
}

export function parseCitydataResponse(payload: unknown): AreaSnapshot {
  const parsed = responseSchema.parse(payload)
  const area = parsed['SeoulRtd.citydata_ppltn'][0]

  return {
    code: area.AREA_CD,
    name: area.AREA_NM,
    congestion: area.AREA_CONGEST_LVL,
    message: area.AREA_CONGEST_MSG,
    populationMin: area.AREA_PPLTN_MIN,
    populationMax: area.AREA_PPLTN_MAX,
    observedAt: area.PPLTN_TIME,
    forecasts: (area.FCST_PPLTN ?? []).map(toForecast),
  }
}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `npx vitest run src/data/schema.test.ts`
Expected: PASS — 5개 테스트 통과.

- [ ] **Step 5: 커밋**

```bash
git add src/data/schema.ts src/data/schema.test.ts
git commit -m "feat: zod 응답 검증 및 도메인 모델 정규화"
```

---

## Task 9: 목업 데이터

**Files:**
- Create: `src/data/mock.ts`
- Test: `src/data/mock.test.ts`

인증키 발급 전까지 개발을 진행하기 위한 것이다. 실제 응답과 동일한 스키마를 통과해야 한다.

- [ ] **Step 1: 실패하는 테스트 작성**

```typescript
import { describe, expect, it } from 'vitest'
import { AREA_CATALOG } from './areas'
import { buildMockSnapshot } from './mock'
import { parseCitydataResponse } from './schema'

describe('buildMockSnapshot', () => {
  it('실제 응답 스키마를 통과하는 형태를 만든다', () => {
    const raw = buildMockSnapshot('강남역')
    expect(() => parseCitydataResponse(raw)).not.toThrow()
  })

  it('요청한 명소 이름을 그대로 반영한다', () => {
    const snapshot = parseCitydataResponse(buildMockSnapshot('성수카페거리'))
    expect(snapshot.name).toBe('성수카페거리')
  })

  it('같은 명소는 항상 같은 혼잡도를 준다', () => {
    const first = parseCitydataResponse(buildMockSnapshot('강남역'))
    const second = parseCitydataResponse(buildMockSnapshot('강남역'))
    expect(first.congestion).toBe(second.congestion)
  })

  it('12시간치 예측을 만든다', () => {
    const snapshot = parseCitydataResponse(buildMockSnapshot('경복궁'))
    expect(snapshot.forecasts).toHaveLength(12)
  })

  it('카탈로그의 모든 명소에 대해 동작한다', () => {
    for (const area of AREA_CATALOG) {
      expect(() => parseCitydataResponse(buildMockSnapshot(area.name))).not.toThrow()
    }
  })
})
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `npx vitest run src/data/mock.test.ts`
Expected: FAIL — `Failed to resolve import "./mock"`

- [ ] **Step 3: 구현 작성**

명소 이름을 해시해 결정적으로 혼잡도를 고른다. 새로고침마다 값이 튀면 UI 확인이 어렵기 때문이다.

```typescript
import { CONGESTION_LEVELS } from '../domain/types'

const MESSAGES: Readonly<Record<string, string>> = {
  여유: '사람이 몰려있을 가능성이 낮고 크게 붐비지 않아요.',
  보통: '사람이 몰려있을 수 있지만 크게 붐비지는 않아요. 도보 이동에 큰 제약이 없어요.',
  '약간 붐빔': '사람들이 몰려있을 가능성이 크고 붐빌 수 있어요. 이동시 주의하세요.',
  붐빔: '사람들이 마주칠 정도로 매우 붐벼요. 안전사고에 주의하세요.',
}

function hash(value: string): number {
  let result = 0
  for (let i = 0; i < value.length; i += 1) {
    result = (result * 31 + value.charCodeAt(i)) % 100_000
  }
  return result
}

function pad(value: number): string {
  return String(value).padStart(2, '0')
}

function formatTime(date: Date): string {
  return (
    `${date.getFullYear()}-${pad(date.getMonth() + 1)}-${pad(date.getDate())}` +
    ` ${pad(date.getHours())}:${pad(date.getMinutes())}`
  )
}

export function buildMockSnapshot(areaName: string): unknown {
  const seed = hash(areaName)
  const level = CONGESTION_LEVELS[seed % CONGESTION_LEVELS.length]
  const base = 8_000 + (seed % 40) * 1_000
  const now = new Date()

  const forecasts = Array.from({ length: 12 }, (_, index) => {
    const at = new Date(now)
    at.setHours(now.getHours() + index + 1, 0, 0, 0)
    const shifted = CONGESTION_LEVELS[(seed + index) % CONGESTION_LEVELS.length]
    return {
      FCST_TIME: formatTime(at),
      FCST_CONGEST_LVL: shifted,
      FCST_PPLTN_MIN: String(base + index * 500),
      FCST_PPLTN_MAX: String(base + index * 500 + 2_000),
    }
  })

  return {
    'SeoulRtd.citydata_ppltn': [
      {
        AREA_NM: areaName,
        AREA_CD: `MOCK${pad(seed % 100)}`,
        AREA_CONGEST_LVL: level,
        AREA_CONGEST_MSG: MESSAGES[level],
        AREA_PPLTN_MIN: String(base),
        AREA_PPLTN_MAX: String(base + 2_000),
        PPLTN_TIME: formatTime(now),
        FCST_YN: 'Y',
        FCST_PPLTN: forecasts,
      },
    ],
  }
}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `npx vitest run src/data/mock.test.ts`
Expected: PASS — 5개 테스트 통과.

- [ ] **Step 5: 커밋**

```bash
git add src/data/mock.ts src/data/mock.test.ts
git commit -m "feat: 인증키 발급 전 개발용 목업 데이터"
```

---

## Task 10: 데이터 클라이언트

**Files:**
- Create: `src/data/client.ts`
- Create: `.env.example`
- Test: `src/data/client.test.ts`

목업과 실데이터의 분기를 여기 한 곳에 가둔다.

- [ ] **Step 1: 실패하는 테스트 작성**

```typescript
import { afterEach, describe, expect, it, vi } from 'vitest'
import { fetchAreaSnapshot, fetchAreaSnapshots } from './client'

afterEach(() => {
  vi.unstubAllGlobals()
  vi.unstubAllEnvs()
})

describe('fetchAreaSnapshot', () => {
  it('목업 모드에서는 네트워크를 타지 않는다', async () => {
    vi.stubEnv('VITE_USE_MOCK', 'true')
    const fetchSpy = vi.fn()
    vi.stubGlobal('fetch', fetchSpy)

    const snapshot = await fetchAreaSnapshot('강남역')

    expect(snapshot.name).toBe('강남역')
    expect(fetchSpy).not.toHaveBeenCalled()
  })

  it('실데이터 모드에서는 프록시를 호출한다', async () => {
    vi.stubEnv('VITE_USE_MOCK', 'false')
    vi.stubEnv('VITE_API_BASE_URL', 'https://proxy.example.com')
    const payload = {
      'SeoulRtd.citydata_ppltn': [
        {
          AREA_NM: '강남역',
          AREA_CD: 'POI014',
          AREA_CONGEST_LVL: '붐빔',
          AREA_CONGEST_MSG: '매우 붐벼요.',
          AREA_PPLTN_MIN: '50000',
          AREA_PPLTN_MAX: '52000',
          PPLTN_TIME: '2026-08-03 14:00',
          FCST_PPLTN: [],
        },
      ],
    }
    vi.stubGlobal(
      'fetch',
      vi.fn().mockResolvedValue({ ok: true, json: async () => payload }),
    )

    const snapshot = await fetchAreaSnapshot('강남역')

    expect(snapshot.congestion).toBe('붐빔')
    expect(fetch).toHaveBeenCalledWith(
      'https://proxy.example.com/api/citydata?area=%EA%B0%95%EB%82%A8%EC%97%AD',
      expect.anything(),
    )
  })

  it('HTTP 실패는 에러를 던진다', async () => {
    vi.stubEnv('VITE_USE_MOCK', 'false')
    vi.stubEnv('VITE_API_BASE_URL', 'https://proxy.example.com')
    vi.stubGlobal('fetch', vi.fn().mockResolvedValue({ ok: false, status: 502 }))

    await expect(fetchAreaSnapshot('강남역')).rejects.toThrow('혼잡도 정보를 가져오지 못했어요')
  })
})

describe('fetchAreaSnapshots', () => {
  it('목업 모드에서 요청한 수만큼 돌려준다', async () => {
    vi.stubEnv('VITE_USE_MOCK', 'true')
    const results = await fetchAreaSnapshots(['강남역', '경복궁'])
    expect(results).toHaveLength(2)
    expect(results[0]?.name).toBe('강남역')
  })
})
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `npx vitest run src/data/client.test.ts`
Expected: FAIL — `Failed to resolve import "./client"`

- [ ] **Step 3: 구현 작성**

```typescript
import type { AreaSnapshot } from '../domain/types'
import { buildMockSnapshot } from './mock'
import { parseCitydataResponse } from './schema'

const REQUEST_TIMEOUT_MS = 10_000

function useMock(): boolean {
  return import.meta.env.VITE_USE_MOCK === 'true'
}

function baseUrl(): string {
  return import.meta.env.VITE_API_BASE_URL ?? ''
}

async function requestJson(url: string): Promise<unknown> {
  const controller = new AbortController()
  const timer = setTimeout(() => controller.abort(), REQUEST_TIMEOUT_MS)

  try {
    const response = await fetch(url, { signal: controller.signal })
    if (!response.ok) {
      throw new Error('혼잡도 정보를 가져오지 못했어요. 잠시 후 다시 시도해주세요.')
    }
    return await response.json()
  } catch (error) {
    if (error instanceof Error && error.message.startsWith('혼잡도 정보를')) {
      throw error
    }
    throw new Error('혼잡도 정보를 가져오지 못했어요. 네트워크 상태를 확인해주세요.')
  } finally {
    clearTimeout(timer)
  }
}

export async function fetchAreaSnapshot(areaName: string): Promise<AreaSnapshot> {
  if (useMock()) {
    return parseCitydataResponse(buildMockSnapshot(areaName))
  }

  const url = `${baseUrl()}/api/citydata?area=${encodeURIComponent(areaName)}`
  return parseCitydataResponse(await requestJson(url))
}

export async function fetchAreaSnapshots(
  areaNames: readonly string[],
): Promise<readonly (AreaSnapshot | null)[]> {
  if (useMock()) {
    return areaNames.map((name) => parseCitydataResponse(buildMockSnapshot(name)))
  }

  const url = `${baseUrl()}/api/citydata-bulk?areas=${encodeURIComponent(areaNames.join(','))}`
  const payload = (await requestJson(url)) as { results?: unknown[] }

  return (payload.results ?? []).map((item) => {
    try {
      return parseCitydataResponse(item)
    } catch {
      return null
    }
  })
}
```

- [ ] **Step 4: `.env.example` 생성**

```bash
# 프록시 주소. Vercel 배포 후 실제 도메인으로 채운다.
VITE_API_BASE_URL=https://seoul-live-api.vercel.app

# true면 목업 데이터를 쓴다. 인증키 발급 전까지 true로 둔다.
VITE_USE_MOCK=true
```

- [ ] **Step 5: 로컬 `.env` 생성 (커밋 안 됨)**

```bash
printf 'VITE_USE_MOCK=true\nVITE_API_BASE_URL=\n' > .env
```

- [ ] **Step 6: 테스트 통과 확인**

Run: `npx vitest run src/data/client.test.ts`
Expected: PASS — 4개 테스트 통과.

- [ ] **Step 7: 커밋**

```bash
git add src/data/client.ts src/data/client.test.ts .env.example
git commit -m "feat: 프록시 클라이언트와 목업 분기"
```

---

## Task 11: Vercel 프록시 함수

**Files:**
- Create: `api/_lib/seoul.ts`
- Create: `api/citydata.ts`
- Create: `api/citydata-bulk.ts`
- Create: `vercel.json`

CORS 헤더가 없으면 토스 웹뷰에서 전부 실패한다. 번들과 프록시의 오리진이 다르기 때문이다.

- [ ] **Step 1: `@vercel/node` 타입 설치**

```bash
npm install -D @vercel/node
```

- [ ] **Step 2: `api/_lib/seoul.ts` 작성**

```typescript
const SEOUL_API_BASE = 'http://openapi.seoul.go.kr:8088'
const SERVICE = 'citydata_ppltn'
const FETCH_TIMEOUT_MS = 8_000

export function cacheTtlSeconds(): number {
  const raw = Number(process.env.CACHE_TTL_SECONDS)
  return Number.isFinite(raw) && raw > 0 ? raw : 3_600
}

export function apiKey(): string {
  const key = process.env.SEOUL_API_KEY
  if (!key) {
    throw new Error('SEOUL_API_KEY not configured')
  }
  return key
}

export async function fetchArea(areaName: string): Promise<unknown> {
  const url =
    `${SEOUL_API_BASE}/${apiKey()}/json/${SERVICE}/1/5/` +
    encodeURIComponent(areaName)

  const response = await fetch(url, {
    signal: AbortSignal.timeout(FETCH_TIMEOUT_MS),
  })

  if (!response.ok) {
    throw new Error(`Seoul API responded ${response.status}`)
  }

  return await response.json()
}
```

- [ ] **Step 3: `api/citydata.ts` 작성**

```typescript
import type { VercelRequest, VercelResponse } from '@vercel/node'
import { cacheTtlSeconds, fetchArea } from './_lib/seoul'

export default async function handler(
  req: VercelRequest,
  res: VercelResponse,
): Promise<void> {
  res.setHeader('Access-Control-Allow-Origin', '*')
  res.setHeader('Access-Control-Allow-Methods', 'GET, OPTIONS')

  if (req.method === 'OPTIONS') {
    res.status(204).end()
    return
  }

  const area = typeof req.query.area === 'string' ? req.query.area : ''
  if (!area) {
    res.status(400).json({ error: 'area 파라미터가 필요합니다.' })
    return
  }

  try {
    const payload = await fetchArea(area)
    const ttl = cacheTtlSeconds()
    res.setHeader(
      'Cache-Control',
      `public, s-maxage=${ttl}, stale-while-revalidate=${ttl * 2}`,
    )
    res.status(200).json(payload)
  } catch {
    res.status(502).json({ error: '혼잡도 정보를 가져오지 못했습니다.' })
  }
}
```

에러 메시지에 원본 예외를 담지 않는다. 인증키가 새어나갈 수 있다.

- [ ] **Step 4: `api/citydata-bulk.ts` 작성**

```typescript
import type { VercelRequest, VercelResponse } from '@vercel/node'
import { cacheTtlSeconds, fetchArea } from './_lib/seoul'

const MAX_AREAS = 40

export default async function handler(
  req: VercelRequest,
  res: VercelResponse,
): Promise<void> {
  res.setHeader('Access-Control-Allow-Origin', '*')
  res.setHeader('Access-Control-Allow-Methods', 'GET, OPTIONS')

  if (req.method === 'OPTIONS') {
    res.status(204).end()
    return
  }

  const raw = typeof req.query.areas === 'string' ? req.query.areas : ''
  const areas = raw.split(',').map((v) => v.trim()).filter(Boolean)

  if (areas.length === 0) {
    res.status(400).json({ error: 'areas 파라미터가 필요합니다.' })
    return
  }
  if (areas.length > MAX_AREAS) {
    res.status(400).json({ error: `한 번에 최대 ${MAX_AREAS}곳까지 조회할 수 있습니다.` })
    return
  }

  const settled = await Promise.allSettled(areas.map(fetchArea))
  const results = settled.map((r) => (r.status === 'fulfilled' ? r.value : null))

  const ttl = cacheTtlSeconds()
  res.setHeader(
    'Cache-Control',
    `public, s-maxage=${ttl}, stale-while-revalidate=${ttl * 2}`,
  )
  res.status(200).json({ results })
}
```

개별 명소 실패가 전체를 무너뜨리지 않게 `allSettled`를 쓴다.

- [ ] **Step 5: `vercel.json` 작성**

```json
{
  "functions": {
    "api/**/*.ts": {
      "maxDuration": 15
    }
  }
}
```

- [ ] **Step 6: 타입 검사**

Run: `npx tsc --noEmit -p tsconfig.node.json`
Expected: 에러 없음. `api/` 경로가 포함되지 않아 에러가 나면 `tsconfig.node.json`의 `include`에 `"api/**/*.ts"`를 추가한다.

- [ ] **Step 7: 커밋**

```bash
git add api vercel.json package.json package-lock.json
git commit -m "feat: 서울 API HTTPS 중계 프록시와 CDN 캐시 헤더"
```

---

## Task 12: TanStack Query 훅과 프로바이더

**Files:**
- Create: `src/data/queries.ts`
- Create: `src/app/QueryProvider.tsx`

- [ ] **Step 1: `src/data/queries.ts` 작성**

```typescript
import { useQuery, type UseQueryResult } from '@tanstack/react-query'
import type { AreaSnapshot } from '../domain/types'
import { fetchAreaSnapshot, fetchAreaSnapshots } from './client'

const FIVE_MINUTES = 5 * 60 * 1_000

export function useAreaSnapshot(
  areaName: string | undefined,
): UseQueryResult<AreaSnapshot> {
  return useQuery({
    queryKey: ['area', areaName],
    queryFn: () => fetchAreaSnapshot(areaName as string),
    enabled: Boolean(areaName),
    staleTime: FIVE_MINUTES,
    retry: 2,
  })
}

export function useAreaSnapshots(
  areaNames: readonly string[],
): UseQueryResult<readonly (AreaSnapshot | null)[]> {
  return useQuery({
    queryKey: ['areas', areaNames],
    queryFn: () => fetchAreaSnapshots(areaNames),
    staleTime: FIVE_MINUTES,
    retry: 2,
  })
}
```

- [ ] **Step 2: `src/app/QueryProvider.tsx` 작성**

```typescript
import { QueryClient, QueryClientProvider } from '@tanstack/react-query'
import { useState, type ReactNode } from 'react'

export function QueryProvider({ children }: { children: ReactNode }) {
  const [client] = useState(
    () =>
      new QueryClient({
        defaultOptions: {
          queries: {
            refetchOnWindowFocus: true,
            gcTime: 30 * 60 * 1_000,
          },
        },
      }),
  )

  return <QueryClientProvider client={client}>{children}</QueryClientProvider>
}
```

- [ ] **Step 3: 타입 검사**

Run: `npx tsc -b`
Expected: 에러 없음.

- [ ] **Step 4: 커밋**

```bash
git add src/data/queries.ts src/app/QueryProvider.tsx
git commit -m "feat: TanStack Query 훅과 프로바이더"
```

---

## Task 13: 위치 훅

**Files:**
- Create: `src/hooks/useCurrentLocation.ts`
- Test: `src/hooks/useCurrentLocation.test.ts`

앱인토스 SDK의 `getCurrentLocation`을 쓴다. 권한 거부와 실패를 구분해 화면이 무너지지 않게 한다.

- [ ] **Step 1: 실패하는 테스트 작성**

```typescript
import { renderHook, waitFor } from '@testing-library/react'
import { describe, expect, it, vi } from 'vitest'
import { useCurrentLocation } from './useCurrentLocation'

vi.mock('@apps-in-toss/web-framework', () => ({
  Accuracy: { Balanced: 'Balanced' },
  getCurrentLocation: vi.fn(),
  GetCurrentLocationPermissionError: class extends Error {},
}))

const framework = await import('@apps-in-toss/web-framework')
const mocked = vi.mocked(framework.getCurrentLocation)

describe('useCurrentLocation', () => {
  it('성공하면 좌표를 준다', async () => {
    mocked.mockResolvedValue({
      coords: { latitude: 37.5665, longitude: 126.978 },
    } as never)

    const { result } = renderHook(() => useCurrentLocation())

    await waitFor(() => expect(result.current.status).toBe('granted'))
    expect(result.current.coords).toEqual({ lat: 37.5665, lng: 126.978 })
  })

  it('권한 거부면 denied 상태가 되고 좌표는 없다', async () => {
    mocked.mockRejectedValue(
      new framework.GetCurrentLocationPermissionError('denied'),
    )

    const { result } = renderHook(() => useCurrentLocation())

    await waitFor(() => expect(result.current.status).toBe('denied'))
    expect(result.current.coords).toBeNull()
  })

  it('그 외 실패는 unavailable 상태가 된다', async () => {
    mocked.mockRejectedValue(new Error('timeout'))

    const { result } = renderHook(() => useCurrentLocation())

    await waitFor(() => expect(result.current.status).toBe('unavailable'))
  })
})
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `npx vitest run src/hooks/useCurrentLocation.test.ts`
Expected: FAIL — `Failed to resolve import "./useCurrentLocation"`

- [ ] **Step 3: 구현 작성**

```typescript
import {
  Accuracy,
  getCurrentLocation,
  GetCurrentLocationPermissionError,
} from '@apps-in-toss/web-framework'
import { useCallback, useEffect, useState } from 'react'
import type { Coords } from '../domain/types'

export type LocationStatus = 'loading' | 'granted' | 'denied' | 'unavailable'

export interface CurrentLocation {
  readonly coords: Coords | null
  readonly status: LocationStatus
  readonly retry: () => void
}

export function useCurrentLocation(): CurrentLocation {
  const [coords, setCoords] = useState<Coords | null>(null)
  const [status, setStatus] = useState<LocationStatus>('loading')
  const [attempt, setAttempt] = useState(0)

  const retry = useCallback(() => {
    setStatus('loading')
    setAttempt((value) => value + 1)
  }, [])

  useEffect(() => {
    let active = true

    async function load(): Promise<void> {
      try {
        const location = await getCurrentLocation({ accuracy: Accuracy.Balanced })
        if (!active) return
        setCoords({
          lat: location.coords.latitude,
          lng: location.coords.longitude,
        })
        setStatus('granted')
      } catch (error) {
        if (!active) return
        setCoords(null)
        setStatus(
          error instanceof GetCurrentLocationPermissionError
            ? 'denied'
            : 'unavailable',
        )
      }
    }

    void load()
    return () => {
      active = false
    }
  }, [attempt])

  return { coords, status, retry }
}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `npx vitest run src/hooks/useCurrentLocation.test.ts`
Expected: PASS — 3개 테스트 통과.

- [ ] **Step 5: 커밋**

```bash
git add src/hooks/useCurrentLocation.ts src/hooks/useCurrentLocation.test.ts
git commit -m "feat: 토스 위치 권한 훅"
```

---

## Task 14: 내 주변 목록 로직

**Files:**
- Create: `src/hooks/useNearbyAreas.ts`
- Test: `src/hooks/useNearbyAreas.test.ts`

정렬·필터·추천 선정은 순수 함수로 빼서 훅 없이 테스트한다.

- [ ] **Step 1: 실패하는 테스트 작성**

```typescript
import { describe, expect, it } from 'vitest'
import type { AreaCatalogEntry, AreaSnapshot } from '../domain/types'
import { buildNearbyList, pickRecommendations } from './useNearbyAreas'

const ENTRIES: AreaCatalogEntry[] = [
  { code: 'A', name: '가까운여유', lat: 37.5665, lng: 126.978, category: '공원' },
  { code: 'B', name: '가까운붐빔', lat: 37.5675, lng: 126.979, category: '카페' },
  { code: 'C', name: '먼여유', lat: 37.65, lng: 127.05, category: '공원' },
]

function snapshot(name: string, congestion: AreaSnapshot['congestion']): AreaSnapshot {
  return {
    code: name, name, congestion, message: '',
    populationMin: 0, populationMax: 0, observedAt: '2026-08-03 14:00', forecasts: [],
  }
}

const SNAPSHOTS = [
  snapshot('가까운여유', '여유'),
  snapshot('가까운붐빔', '붐빔'),
  snapshot('먼여유', '여유'),
]

const HERE = { lat: 37.5665, lng: 126.978 }

describe('buildNearbyList', () => {
  it('좌표가 있으면 거리 오름차순으로 정렬한다', () => {
    const list = buildNearbyList({ entries: ENTRIES, snapshots: SNAPSHOTS, coords: HERE, category: '전체' })
    expect(list.map((item) => item.entry.name)).toEqual(['가까운여유', '가까운붐빔', '먼여유'])
    expect(list[0].distanceMeters).toBe(0)
  })

  it('좌표가 없으면 혼잡도 낮은 순으로 정렬하고 거리는 null이다', () => {
    const list = buildNearbyList({ entries: ENTRIES, snapshots: SNAPSHOTS, coords: null, category: '전체' })
    expect(list[0].snapshot?.congestion).toBe('여유')
    expect(list[0].distanceMeters).toBeNull()
    expect(list.at(-1)?.snapshot?.congestion).toBe('붐빔')
  })

  it('카테고리로 거른다', () => {
    const list = buildNearbyList({ entries: ENTRIES, snapshots: SNAPSHOTS, coords: HERE, category: '공원' })
    expect(list).toHaveLength(2)
    expect(list.every((item) => item.entry.category === '공원')).toBe(true)
  })

  it('스냅샷이 없는 명소도 목록에 남기되 snapshot은 null이다', () => {
    const list = buildNearbyList({ entries: ENTRIES, snapshots: [null, null, null], coords: HERE, category: '전체' })
    expect(list).toHaveLength(3)
    expect(list[0].snapshot).toBeNull()
  })
})

describe('pickRecommendations', () => {
  it('2km 이내이면서 한산한 곳만 고른다', () => {
    const list = buildNearbyList({ entries: ENTRIES, snapshots: SNAPSHOTS, coords: HERE, category: '전체' })
    const picked = pickRecommendations(list)
    expect(picked.map((item) => item.entry.name)).toEqual(['가까운여유'])
  })

  it('좌표가 없으면 빈 배열을 준다', () => {
    const list = buildNearbyList({ entries: ENTRIES, snapshots: SNAPSHOTS, coords: null, category: '전체' })
    expect(pickRecommendations(list)).toEqual([])
  })

  it('최대 5개까지만 준다', () => {
    const many = Array.from({ length: 9 }, (_, i) => ({
      code: `X${i}`, name: `여유${i}`, lat: 37.5665 + i * 0.0001, lng: 126.978, category: '공원' as const,
    }))
    const list = buildNearbyList({
      entries: many,
      snapshots: many.map((e) => snapshot(e.name, '여유')),
      coords: HERE,
      category: '전체',
    })
    expect(pickRecommendations(list)).toHaveLength(5)
  })
})
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `npx vitest run src/hooks/useNearbyAreas.test.ts`
Expected: FAIL — `Failed to resolve import "./useNearbyAreas"`

- [ ] **Step 3: 구현 작성**

```typescript
import { useMemo } from 'react'
import { congestionRank, isUncrowded } from '../domain/congestion'
import { haversineMeters } from '../domain/distance'
import type {
  AreaCatalogEntry,
  AreaCategory,
  AreaSnapshot,
  Coords,
  NearbyArea,
} from '../domain/types'
import { AREA_CATALOG } from '../data/areas'

export type CategoryFilterValue = AreaCategory | '전체'

const RECOMMENDATION_RADIUS_METERS = 2_000
const MAX_RECOMMENDATIONS = 5

interface BuildInput {
  readonly entries: readonly AreaCatalogEntry[]
  readonly snapshots: readonly (AreaSnapshot | null)[]
  readonly coords: Coords | null
  readonly category: CategoryFilterValue
}

function compareByCongestion(a: NearbyArea, b: NearbyArea): number {
  if (a.snapshot === null) return 1
  if (b.snapshot === null) return -1
  return congestionRank(a.snapshot.congestion) - congestionRank(b.snapshot.congestion)
}

function compareByDistance(a: NearbyArea, b: NearbyArea): number {
  if (a.distanceMeters === null) return 1
  if (b.distanceMeters === null) return -1
  return a.distanceMeters - b.distanceMeters
}

export function buildNearbyList(input: BuildInput): readonly NearbyArea[] {
  const { entries, snapshots, coords, category } = input

  const combined = entries
    .map((entry, index): NearbyArea => ({
      entry,
      snapshot: snapshots[index] ?? null,
      distanceMeters: coords === null ? null : haversineMeters(coords, entry),
    }))
    .filter((item) => category === '전체' || item.entry.category === category)

  return [...combined].sort(
    coords === null ? compareByCongestion : compareByDistance,
  )
}

export function pickRecommendations(
  list: readonly NearbyArea[],
): readonly NearbyArea[] {
  return list
    .filter(
      (item) =>
        item.distanceMeters !== null &&
        item.distanceMeters <= RECOMMENDATION_RADIUS_METERS &&
        item.snapshot !== null &&
        isUncrowded(item.snapshot.congestion),
    )
    .slice(0, MAX_RECOMMENDATIONS)
}

export function useNearbyAreas(
  snapshots: readonly (AreaSnapshot | null)[],
  coords: Coords | null,
  category: CategoryFilterValue,
): { readonly list: readonly NearbyArea[]; readonly recommended: readonly NearbyArea[] } {
  return useMemo(() => {
    const list = buildNearbyList({
      entries: AREA_CATALOG,
      snapshots,
      coords,
      category,
    })
    return { list, recommended: pickRecommendations(list) }
  }, [snapshots, coords, category])
}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `npx vitest run src/hooks/useNearbyAreas.test.ts`
Expected: PASS — 7개 테스트 통과.

- [ ] **Step 5: 커밋**

```bash
git add src/hooks/useNearbyAreas.ts src/hooks/useNearbyAreas.test.ts
git commit -m "feat: 내 주변 정렬·필터·추천 선정 로직"
```

---

## Task 15: 공통 컴포넌트

**Files:**
- Create: `src/components/common/CongestionBadge.tsx`
- Create: `src/components/common/ErrorState.tsx`
- Create: `src/components/common/SkeletonCard.tsx`
- Test: `src/components/common/CongestionBadge.test.tsx`

- [ ] **Step 1: 실패하는 테스트 작성**

```typescript
import { render, screen } from '@testing-library/react'
import { describe, expect, it } from 'vitest'
import { CongestionBadge } from './CongestionBadge'

describe('CongestionBadge', () => {
  it('혼잡도 라벨을 보여준다', () => {
    render(<CongestionBadge level="붐빔" />)
    expect(screen.getByText('붐빔')).toBeInTheDocument()
  })

  it('단계마다 다른 스타일 클래스를 쓴다', () => {
    const { rerender } = render(<CongestionBadge level="여유" />)
    const calm = screen.getByText('여유').className
    rerender(<CongestionBadge level="붐빔" />)
    expect(screen.getByText('붐빔').className).not.toBe(calm)
  })

  it('정보가 없으면 안내 문구를 보여준다', () => {
    render(<CongestionBadge level={null} />)
    expect(screen.getByText('정보 없음')).toBeInTheDocument()
  })
})
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `npx vitest run src/components/common/CongestionBadge.test.tsx`
Expected: FAIL — `Failed to resolve import "./CongestionBadge"`

- [ ] **Step 3: `CongestionBadge.tsx` 작성**

```typescript
import { congestionTone, type CongestionTone } from '../../domain/congestion'
import type { CongestionLevel } from '../../domain/types'

const TONE_CLASS: Readonly<Record<CongestionTone, string>> = {
  calm: 'bg-calm-container text-calm',
  normal: 'bg-normal-container text-normal',
  busy: 'bg-busy-container text-busy',
  crowded: 'bg-crowded-container text-crowded',
}

interface Props {
  readonly level: CongestionLevel | null
}

export function CongestionBadge({ level }: Props) {
  if (level === null) {
    return (
      <span className="inline-flex items-center gap-1 rounded-full bg-surface-container px-2.5 py-1 text-xs font-semibold text-on-surface-variant">
        정보 없음
      </span>
    )
  }

  return (
    <span
      className={`inline-flex items-center gap-1 rounded-full px-2.5 py-1 text-xs font-semibold ${TONE_CLASS[congestionTone(level)]}`}
    >
      <span className="size-1.5 rounded-full bg-current" aria-hidden />
      {level}
    </span>
  )
}
```

- [ ] **Step 4: `ErrorState.tsx` 작성**

```typescript
interface Props {
  readonly message: string
  readonly onRetry?: () => void
}

export function ErrorState({ message, onRetry }: Props) {
  return (
    <div className="flex flex-col items-center gap-3 rounded-card bg-surface-container-lowest px-4 py-10 text-center">
      <p className="text-sm text-on-surface-variant">{message}</p>
      {onRetry !== undefined && (
        <button
          type="button"
          onClick={onRetry}
          className="min-h-12 rounded-action bg-primary px-5 text-sm font-semibold text-on-primary"
        >
          다시 시도
        </button>
      )}
    </div>
  )
}
```

- [ ] **Step 5: `SkeletonCard.tsx` 작성**

```typescript
export function SkeletonCard() {
  return (
    <div className="animate-pulse rounded-card border border-outline-variant bg-surface-container-lowest p-4">
      <div className="h-4 w-1/3 rounded bg-surface-container-high" />
      <div className="mt-3 h-3 w-1/2 rounded bg-surface-container" />
    </div>
  )
}

export function SkeletonList({ count = 5 }: { count?: number }) {
  return (
    <div className="flex flex-col gap-3" aria-label="불러오는 중">
      {Array.from({ length: count }, (_, index) => (
        <SkeletonCard key={index} />
      ))}
    </div>
  )
}
```

- [ ] **Step 6: 테스트 통과 확인**

Run: `npx vitest run src/components/common`
Expected: PASS — 3개 테스트 통과.

- [ ] **Step 7: 커밋**

```bash
git add src/components/common
git commit -m "feat: 혼잡도 배지·에러 상태·스켈레톤 공통 컴포넌트"
```

---

## Task 16: 레이아웃 컴포넌트

**Files:**
- Create: `src/components/layout/TopAppBar.tsx`
- Create: `src/components/layout/BottomTabBar.tsx`

시안의 하단 탭 4개를 유지하되 지도·더보기는 비활성으로 둔다.

- [ ] **Step 1: `TopAppBar.tsx` 작성**

```typescript
import type { ReactNode } from 'react'

interface Props {
  readonly title: string
  readonly onBack?: () => void
  readonly trailing?: ReactNode
}

export function TopAppBar({ title, onBack, trailing }: Props) {
  return (
    <header className="sticky top-0 z-10 flex min-h-14 items-center gap-2 border-b border-outline-variant bg-surface/80 px-4 backdrop-blur-md">
      {onBack !== undefined && (
        <button
          type="button"
          onClick={onBack}
          aria-label="뒤로 가기"
          className="grid size-10 place-items-center rounded-full text-on-surface"
        >
          ←
        </button>
      )}
      <h1 className="flex-1 truncate text-lg font-bold text-primary">{title}</h1>
      {trailing}
    </header>
  )
}
```

- [ ] **Step 2: `BottomTabBar.tsx` 작성**

```typescript
export type TabKey = 'map' | 'nearby' | 'forecast' | 'more'

interface Tab {
  readonly key: TabKey
  readonly label: string
  readonly icon: string
  readonly enabled: boolean
}

const TABS: readonly Tab[] = [
  { key: 'map', label: '지도', icon: '🗺️', enabled: false },
  { key: 'nearby', label: '내 주변', icon: '📍', enabled: true },
  { key: 'forecast', label: '혼잡예보', icon: '📈', enabled: true },
  { key: 'more', label: '더보기', icon: '⋯', enabled: false },
]

interface Props {
  readonly active: TabKey
  readonly onSelect: (key: TabKey) => void
}

export function BottomTabBar({ active, onSelect }: Props) {
  return (
    <nav className="sticky bottom-0 flex border-t border-outline-variant bg-surface-container-lowest">
      {TABS.map((tab) => (
        <button
          key={tab.key}
          type="button"
          disabled={!tab.enabled}
          aria-current={active === tab.key ? 'page' : undefined}
          onClick={() => onSelect(tab.key)}
          className={`flex min-h-14 flex-1 flex-col items-center justify-center gap-0.5 text-xs font-medium ${
            active === tab.key
              ? 'text-primary'
              : tab.enabled
                ? 'text-on-surface-variant'
                : 'text-outline-variant'
          }`}
        >
          <span aria-hidden>{tab.icon}</span>
          {tab.label}
        </button>
      ))}
    </nav>
  )
}
```

- [ ] **Step 3: 타입 검사**

Run: `npx tsc -b`
Expected: 에러 없음.

- [ ] **Step 4: 커밋**

```bash
git add src/components/layout
git commit -m "feat: 상단 앱바와 하단 탭바"
```

---

## Task 17: 내 주변 화면

**Files:**
- Create: `src/components/nearby/CategoryFilter.tsx`
- Create: `src/components/nearby/AreaListItem.tsx`
- Create: `src/screens/NearbyScreen.tsx`
- Test: `src/screens/NearbyScreen.test.tsx`

- [ ] **Step 1: `CategoryFilter.tsx` 작성**

```typescript
import type { CategoryFilterValue } from '../../hooks/useNearbyAreas'

const OPTIONS: readonly CategoryFilterValue[] = [
  '전체', '공원', '쇼핑몰', '카페', '문화재',
]

interface Props {
  readonly value: CategoryFilterValue
  readonly onChange: (next: CategoryFilterValue) => void
}

export function CategoryFilter({ value, onChange }: Props) {
  return (
    <div className="flex gap-2 overflow-x-auto px-4 py-1" role="tablist">
      {OPTIONS.map((option) => (
        <button
          key={option}
          type="button"
          role="tab"
          aria-selected={value === option}
          onClick={() => onChange(option)}
          className={`min-h-12 shrink-0 rounded-full px-4 text-sm font-semibold ${
            value === option
              ? 'bg-primary text-on-primary'
              : 'bg-surface-container text-on-surface-variant'
          }`}
        >
          {option}
        </button>
      ))}
    </div>
  )
}
```

- [ ] **Step 2: `AreaListItem.tsx` 작성**

```typescript
import { formatDistance } from '../../domain/distance'
import type { NearbyArea } from '../../domain/types'
import { CongestionBadge } from '../common/CongestionBadge'

interface Props {
  readonly area: NearbyArea
  readonly onSelect: (name: string) => void
}

export function AreaListItem({ area, onSelect }: Props) {
  const { entry, snapshot, distanceMeters } = area

  return (
    <button
      type="button"
      onClick={() => onSelect(entry.name)}
      className="flex min-h-16 w-full items-center gap-3 rounded-card border border-outline-variant bg-surface-container-lowest px-4 py-3 text-left"
    >
      <div className="min-w-0 flex-1">
        <p className="truncate font-semibold text-on-surface">{entry.name}</p>
        <p className="mt-0.5 text-sm text-on-surface-variant">
          {distanceMeters !== null && (
            <span className="font-semibold text-primary">
              {formatDistance(distanceMeters)}
            </span>
          )}
          {distanceMeters !== null && ' · '}
          {entry.category}
        </p>
      </div>
      <CongestionBadge level={snapshot?.congestion ?? null} />
    </button>
  )
}
```

- [ ] **Step 3: 실패하는 테스트 작성**

```typescript
import { QueryClient, QueryClientProvider } from '@tanstack/react-query'
import { render, screen, waitFor } from '@testing-library/react'
import userEvent from '@testing-library/user-event'
import { beforeEach, describe, expect, it, vi } from 'vitest'
import { NearbyScreen } from './NearbyScreen'

vi.mock('../hooks/useCurrentLocation', () => ({
  useCurrentLocation: vi.fn(),
}))

const { useCurrentLocation } = await import('../hooks/useCurrentLocation')
const mockedLocation = vi.mocked(useCurrentLocation)

function renderScreen(onSelect = vi.fn()) {
  const client = new QueryClient({
    defaultOptions: { queries: { retry: false } },
  })
  render(
    <QueryClientProvider client={client}>
      <NearbyScreen onSelectArea={onSelect} />
    </QueryClientProvider>,
  )
  return onSelect
}

beforeEach(() => {
  vi.stubEnv('VITE_USE_MOCK', 'true')
  mockedLocation.mockReturnValue({
    coords: { lat: 37.5665, lng: 126.978 },
    status: 'granted',
    retry: vi.fn(),
  })
})

describe('NearbyScreen', () => {
  it('명소 목록을 보여준다', async () => {
    renderScreen()
    await waitFor(() => expect(screen.getByText('광화문·덕수궁')).toBeInTheDocument())
  })

  it('카테고리를 고르면 목록이 걸러진다', async () => {
    renderScreen()
    await waitFor(() => expect(screen.getByText('강남역')).toBeInTheDocument())

    await userEvent.click(screen.getByRole('tab', { name: '공원' }))

    expect(screen.queryByText('강남역')).not.toBeInTheDocument()
    expect(screen.getByText('남산공원')).toBeInTheDocument()
  })

  it('명소를 누르면 콜백이 이름과 함께 불린다', async () => {
    const onSelect = renderScreen()
    await waitFor(() => expect(screen.getByText('경복궁')).toBeInTheDocument())

    await userEvent.click(screen.getByText('경복궁'))

    expect(onSelect).toHaveBeenCalledWith('경복궁')
  })

  it('위치 권한이 없으면 안내를 보여주고 목록은 유지한다', async () => {
    mockedLocation.mockReturnValue({ coords: null, status: 'denied', retry: vi.fn() })
    renderScreen()

    await waitFor(() => expect(screen.getByText('경복궁')).toBeInTheDocument())
    expect(screen.getByText(/위치 정보를 사용할 수 없어/)).toBeInTheDocument()
  })
})
```

- [ ] **Step 4: 테스트 실패 확인**

Run: `npx vitest run src/screens/NearbyScreen.test.tsx`
Expected: FAIL — `Failed to resolve import "./NearbyScreen"`

- [ ] **Step 5: `NearbyScreen.tsx` 작성**

```typescript
import { useState } from 'react'
import { CongestionBadge } from '../components/common/CongestionBadge'
import { ErrorState } from '../components/common/ErrorState'
import { SkeletonList } from '../components/common/SkeletonCard'
import { AreaListItem } from '../components/nearby/AreaListItem'
import { CategoryFilter } from '../components/nearby/CategoryFilter'
import { AREA_NAMES } from '../data/areas'
import { useAreaSnapshots } from '../data/queries'
import { formatDistance, walkingMinutes } from '../domain/distance'
import { useCurrentLocation } from '../hooks/useCurrentLocation'
import {
  useNearbyAreas,
  type CategoryFilterValue,
} from '../hooks/useNearbyAreas'

interface Props {
  readonly onSelectArea: (name: string) => void
}

export function NearbyScreen({ onSelectArea }: Props) {
  const [category, setCategory] = useState<CategoryFilterValue>('전체')
  const { coords, status, retry } = useCurrentLocation()
  const snapshots = useAreaSnapshots(AREA_NAMES)
  const { list, recommended } = useNearbyAreas(
    snapshots.data ?? [],
    coords,
    category,
  )

  return (
    <div className="flex flex-col gap-4 pb-6">
      <section className="px-4 pt-4">
        <h2 className="text-2xl font-bold text-on-surface">내 주변 명소</h2>
        <p className="mt-1 text-sm text-on-surface-variant">
          현재 위치 기반 실시간 혼잡도 정보
        </p>
      </section>

      {(status === 'denied' || status === 'unavailable') && (
        <p className="mx-4 rounded-card bg-surface-container px-4 py-3 text-sm text-on-surface-variant">
          위치 정보를 사용할 수 없어 혼잡도가 낮은 순으로 보여드려요.{' '}
          <button type="button" onClick={retry} className="font-semibold text-primary">
            다시 시도
          </button>
        </p>
      )}

      <CategoryFilter value={category} onChange={setCategory} />

      {snapshots.isError && (
        <div className="px-4">
          <ErrorState
            message="혼잡도 정보를 가져오지 못했어요."
            onRetry={() => void snapshots.refetch()}
          />
        </div>
      )}

      {snapshots.isPending && (
        <div className="px-4">
          <SkeletonList count={5} />
        </div>
      )}

      {recommended.length > 0 && (
        <section>
          <h3 className="px-4 text-lg font-bold text-on-surface">
            가까우면서 여유로운 곳 추천
          </h3>
          <div className="mt-3 flex gap-3 overflow-x-auto px-4 pb-1">
            {recommended.map((area) => (
              <button
                key={area.entry.code}
                type="button"
                onClick={() => onSelectArea(area.entry.name)}
                className="w-56 shrink-0 rounded-card border border-outline-variant bg-surface-container-lowest p-4 text-left"
              >
                <CongestionBadge level={area.snapshot?.congestion ?? null} />
                <p className="mt-2 truncate text-lg font-bold text-on-surface">
                  {area.entry.name}
                </p>
                {area.distanceMeters !== null && (
                  <p className="mt-1 text-sm text-on-surface-variant">
                    {formatDistance(area.distanceMeters)} · 도보{' '}
                    {walkingMinutes(area.distanceMeters)}분
                  </p>
                )}
              </button>
            ))}
          </div>
        </section>
      )}

      {list.length > 0 && (
        <section className="px-4">
          <h3 className="text-lg font-bold text-on-surface">
            {coords === null ? '혼잡도 낮은 순' : '거리순 주변 장소'}
          </h3>
          <div className="mt-3 flex flex-col gap-3">
            {list.map((area) => (
              <AreaListItem
                key={area.entry.code}
                area={area}
                onSelect={onSelectArea}
              />
            ))}
          </div>
        </section>
      )}
    </div>
  )
}
```

- [ ] **Step 6: 테스트 통과 확인**

Run: `npx vitest run src/screens/NearbyScreen.test.tsx`
Expected: PASS — 4개 테스트 통과.

- [ ] **Step 7: 커밋**

```bash
git add src/components/nearby src/screens/NearbyScreen.tsx src/screens/NearbyScreen.test.tsx
git commit -m "feat: 내 주변 화면"
```

---

## Task 18: 혼잡예보 화면

**Files:**
- Create: `src/domain/forecast.ts`
- Create: `src/components/forecast/ForecastChart.tsx`
- Create: `src/components/forecast/ActionButtons.tsx`
- Create: `src/screens/ForecastScreen.tsx`
- Test: `src/domain/forecast.test.ts`
- Test: `src/screens/ForecastScreen.test.tsx`

- [ ] **Step 1: 한산 시간 찾기 테스트 작성**

```typescript
import { describe, expect, it } from 'vitest'
import type { Forecast } from './types'
import { findQuietTime } from './forecast'

function forecast(time: string, congestion: Forecast['congestion']): Forecast {
  return {
    time,
    hour: Number(time.slice(11, 13)),
    congestion,
    populationMin: 0,
    populationMax: 0,
  }
}

describe('findQuietTime', () => {
  it('여유로 떨어지는 가장 이른 시각을 준다', () => {
    const result = findQuietTime('붐빔', [
      forecast('2026-08-03 16:00', '붐빔'),
      forecast('2026-08-03 21:00', '여유'),
      forecast('2026-08-03 22:00', '여유'),
    ])
    expect(result).toBe(21)
  })

  it('이미 한산하면 null을 준다', () => {
    const result = findQuietTime('여유', [forecast('2026-08-03 21:00', '여유')])
    expect(result).toBeNull()
  })

  it('예측에 여유가 없으면 null을 준다', () => {
    const result = findQuietTime('붐빔', [forecast('2026-08-03 16:00', '붐빔')])
    expect(result).toBeNull()
  })

  it('예측이 비어도 안전하다', () => {
    expect(findQuietTime('붐빔', [])).toBeNull()
  })
})
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `npx vitest run src/domain/forecast.test.ts`
Expected: FAIL — `Failed to resolve import "./forecast"`

- [ ] **Step 3: `src/domain/forecast.ts` 작성**

```typescript
import type { CongestionLevel, Forecast } from './types'

export function findQuietTime(
  current: CongestionLevel,
  forecasts: readonly Forecast[],
): number | null {
  if (current === '여유') {
    return null
  }

  const quiet = forecasts.find((item) => item.congestion === '여유')
  return quiet?.hour ?? null
}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `npx vitest run src/domain/forecast.test.ts`
Expected: PASS — 4개 테스트 통과.

- [ ] **Step 5: `ForecastChart.tsx` 작성**

혼잡도 순위(0~3)를 y축으로 쓰는 라인 차트다. 시안 `_3`의 곡선을 실데이터로 대체한다.

```typescript
import { congestionRank } from '../../domain/congestion'
import type { Forecast } from '../../domain/types'

const WIDTH = 400
const HEIGHT = 120
const MAX_RANK = 3

interface Props {
  readonly forecasts: readonly Forecast[]
}

export function ForecastChart({ forecasts }: Props) {
  if (forecasts.length < 2) {
    return (
      <p className="py-8 text-center text-sm text-on-surface-variant">
        예측 정보가 아직 없어요.
      </p>
    )
  }

  const points = forecasts.map((item, index) => {
    const x = (index / (forecasts.length - 1)) * WIDTH
    const y = HEIGHT - (congestionRank(item.congestion) / MAX_RANK) * (HEIGHT - 16) - 8
    return `${x.toFixed(1)},${y.toFixed(1)}`
  })

  const labelIndexes = [0, Math.floor(forecasts.length / 2), forecasts.length - 1]

  return (
    <div>
      <svg
        viewBox={`0 0 ${WIDTH} ${HEIGHT}`}
        preserveAspectRatio="none"
        className="h-32 w-full"
        role="img"
        aria-label="시간별 혼잡도 예측"
      >
        <polyline
          points={points.join(' ')}
          fill="none"
          stroke="var(--color-primary)"
          strokeWidth={3}
          strokeDasharray="6 4"
          strokeLinecap="round"
          strokeLinejoin="round"
        />
      </svg>
      <div className="mt-1 flex justify-between text-xs text-on-surface-variant">
        {labelIndexes.map((index) => (
          <span key={index}>{forecasts[index].hour}시</span>
        ))}
      </div>
    </div>
  )
}
```

- [ ] **Step 6: `ActionButtons.tsx` 작성**

```typescript
import type { AreaCatalogEntry } from '../../domain/types'

interface Props {
  readonly entry: AreaCatalogEntry
}

export function ActionButtons({ entry }: Props) {
  const query = encodeURIComponent(entry.name)

  return (
    <div className="flex flex-col gap-2 px-4">
      <div className="flex gap-2">
        <a
          href={`https://map.kakao.com/link/search/${query}`}
          target="_blank"
          rel="noreferrer"
          className="flex min-h-12 flex-1 items-center justify-center rounded-action bg-[#FEE500] text-sm font-semibold text-[#191600]"
        >
          카카오맵 길찾기
        </a>
        <a
          href={`https://map.naver.com/p/search/${query}`}
          target="_blank"
          rel="noreferrer"
          className="flex min-h-12 flex-1 items-center justify-center rounded-action bg-[#03C75A] text-sm font-semibold text-white"
        >
          네이버 길찾기
        </a>
      </div>
      <button
        type="button"
        onClick={() => {
          void navigator.clipboard?.writeText(
            `${entry.name} 실시간 혼잡도 - 서울 라이브`,
          )
        }}
        className="min-h-12 rounded-action border border-outline-variant bg-surface-container-lowest text-sm font-semibold text-on-surface"
      >
        친구에게 공유하기
      </button>
    </div>
  )
}
```

- [ ] **Step 7: 화면 테스트 작성**

```typescript
import { QueryClient, QueryClientProvider } from '@tanstack/react-query'
import { render, screen, waitFor } from '@testing-library/react'
import { beforeEach, describe, expect, it, vi } from 'vitest'
import { ForecastScreen } from './ForecastScreen'

function renderScreen(areaName: string, onBack = vi.fn()) {
  const client = new QueryClient({ defaultOptions: { queries: { retry: false } } })
  render(
    <QueryClientProvider client={client}>
      <ForecastScreen areaName={areaName} onBack={onBack} />
    </QueryClientProvider>,
  )
}

beforeEach(() => {
  vi.stubEnv('VITE_USE_MOCK', 'true')
})

describe('ForecastScreen', () => {
  it('명소 이름과 혼잡도를 보여준다', async () => {
    renderScreen('성수카페거리')
    await waitFor(() =>
      expect(screen.getByRole('heading', { name: '성수카페거리' })).toBeInTheDocument(),
    )
  })

  it('예측 차트를 그린다', async () => {
    renderScreen('경복궁')
    await waitFor(() =>
      expect(screen.getByRole('img', { name: '시간별 혼잡도 예측' })).toBeInTheDocument(),
    )
  })

  it('길찾기 링크를 보여준다', async () => {
    renderScreen('강남역')
    await waitFor(() =>
      expect(screen.getByRole('link', { name: '카카오맵 길찾기' })).toBeInTheDocument(),
    )
  })

  it('카탈로그에 없는 명소는 안내를 보여준다', () => {
    renderScreen('부산역')
    expect(screen.getByText(/명소를 찾을 수 없어요/)).toBeInTheDocument()
  })
})
```

- [ ] **Step 8: 테스트 실패 확인**

Run: `npx vitest run src/screens/ForecastScreen.test.tsx`
Expected: FAIL — `Failed to resolve import "./ForecastScreen"`

- [ ] **Step 9: `ForecastScreen.tsx` 작성**

```typescript
import { CongestionBadge } from '../components/common/CongestionBadge'
import { ErrorState } from '../components/common/ErrorState'
import { SkeletonList } from '../components/common/SkeletonCard'
import { ActionButtons } from '../components/forecast/ActionButtons'
import { ForecastChart } from '../components/forecast/ForecastChart'
import { TopAppBar } from '../components/layout/TopAppBar'
import { findAreaByName } from '../data/areas'
import { useAreaSnapshot } from '../data/queries'
import { findQuietTime } from '../domain/forecast'

interface Props {
  readonly areaName: string
  readonly onBack: () => void
}

export function ForecastScreen({ areaName, onBack }: Props) {
  const entry = findAreaByName(areaName)
  const query = useAreaSnapshot(entry === undefined ? undefined : areaName)

  if (entry === undefined) {
    return (
      <div>
        <TopAppBar title="혼잡예보" onBack={onBack} />
        <p className="px-4 py-10 text-center text-sm text-on-surface-variant">
          명소를 찾을 수 없어요.
        </p>
      </div>
    )
  }

  const snapshot = query.data
  const quietHour =
    snapshot === undefined
      ? null
      : findQuietTime(snapshot.congestion, snapshot.forecasts)

  return (
    <div className="flex flex-col gap-4 pb-6">
      <TopAppBar title={entry.name} onBack={onBack} />

      {query.isPending && (
        <div className="px-4">
          <SkeletonList count={3} />
        </div>
      )}

      {query.isError && (
        <div className="px-4">
          <ErrorState
            message="혼잡도 정보를 가져오지 못했어요."
            onRetry={() => void query.refetch()}
          />
        </div>
      )}

      {snapshot !== undefined && (
        <>
          <section className="mx-4 rounded-card border border-outline-variant bg-surface-container-lowest p-4">
            <CongestionBadge level={snapshot.congestion} />
            <p className="mt-2 text-sm text-on-surface-variant">
              마지막 업데이트: {snapshot.observedAt}
            </p>
            <p className="mt-3 text-sm leading-6 text-on-surface">
              {snapshot.message}
            </p>
            <p className="mt-3 text-sm text-on-surface-variant">
              추정 인구 {snapshot.populationMin.toLocaleString()}~
              {snapshot.populationMax.toLocaleString()}명
            </p>
          </section>

          {quietHour !== null && (
            <section className="mx-4 rounded-card bg-secondary-container px-4 py-3">
              <p className="text-sm leading-6 text-on-surface">
                <span className="font-bold text-primary">{quietHour}시엔 여유 예상</span>
                {' '}한산한 시간을 원하시면 조금만 기다려주세요.
              </p>
            </section>
          )}

          <section className="mx-4 rounded-card border border-outline-variant bg-surface-container-lowest p-4">
            <h3 className="text-base font-bold text-on-surface">시간별 예측</h3>
            <div className="mt-3">
              <ForecastChart forecasts={snapshot.forecasts} />
            </div>
          </section>

          <ActionButtons entry={entry} />
        </>
      )}
    </div>
  )
}
```

- [ ] **Step 10: 테스트 통과 확인**

Run: `npx vitest run src/screens/ForecastScreen.test.tsx`
Expected: PASS — 4개 테스트 통과.

- [ ] **Step 11: 커밋**

```bash
git add src/domain/forecast.ts src/domain/forecast.test.ts src/components/forecast src/screens/ForecastScreen.tsx src/screens/ForecastScreen.test.tsx
git commit -m "feat: 혼잡예보 화면과 시간별 예측 차트"
```

---

## Task 19: 앱 조립

**Files:**
- Modify: `src/App.tsx`
- Modify: `src/main.tsx`
- Modify: `index.html`
- Delete: `src/assets/hero.png`, `src/assets/react.svg`, `src/assets/vite.svg`

화면이 둘뿐이라 라우터 대신 상태로 전환한다. 라우터를 넣으면 토스 웹뷰의 딥링크 처리까지 확인해야 하는데 1차 범위 밖이다.

- [ ] **Step 1: `src/App.tsx` 전체 교체**

```typescript
import { useState } from 'react'
import { QueryProvider } from './app/QueryProvider'
import { BottomTabBar, type TabKey } from './components/layout/BottomTabBar'
import { TopAppBar } from './components/layout/TopAppBar'
import { ForecastScreen } from './screens/ForecastScreen'
import { NearbyScreen } from './screens/NearbyScreen'

function AppShell() {
  const [selectedArea, setSelectedArea] = useState<string | null>(null)
  const activeTab: TabKey = selectedArea === null ? 'nearby' : 'forecast'

  function handleTab(key: TabKey): void {
    if (key === 'nearby') {
      setSelectedArea(null)
    }
  }

  return (
    <div className="flex min-h-full flex-col bg-surface">
      <main className="flex-1">
        {selectedArea === null ? (
          <>
            <TopAppBar title="Seoul Live" />
            <NearbyScreen onSelectArea={setSelectedArea} />
          </>
        ) : (
          <ForecastScreen
            areaName={selectedArea}
            onBack={() => setSelectedArea(null)}
          />
        )}
      </main>
      <BottomTabBar active={activeTab} onSelect={handleTab} />
    </div>
  )
}

export default function App() {
  return (
    <QueryProvider>
      <AppShell />
    </QueryProvider>
  )
}
```

- [ ] **Step 2: `index.html`의 title과 lang 수정**

`<html lang="en">`을 `<html lang="ko">`로, `<title>seoul-live</title>`을 `<title>서울 라이브</title>`로 바꾼다. `<head>` 안에 폰트를 추가한다.

```html
<link rel="preconnect" href="https://fonts.googleapis.com" />
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
<link
  href="https://fonts.googleapis.com/css2?family=Hanken+Grotesk:wght@400;500;600;700&display=swap"
  rel="stylesheet"
/>
```

- [ ] **Step 3: 스캐폴드 잔재 삭제**

```bash
rm -f src/assets/hero.png src/assets/react.svg src/assets/vite.svg
```

- [ ] **Step 4: 전체 테스트 통과 확인**

Run: `npm test`
Expected: PASS — 모든 테스트 파일 통과.

- [ ] **Step 5: 빌드 확인**

Run: `npm run build:vite`
Expected: 성공. `dist/`가 생성된다.

- [ ] **Step 6: 개발 서버로 눈으로 확인**

Run: `npm run dev`
목업 모드이므로 명소 30곳이 뜨고, 명소를 누르면 예보 화면으로 넘어가고, 뒤로가기가 동작해야 한다.

- [ ] **Step 7: 커밋**

```bash
git add -A
git commit -m "feat: 앱 조립 및 스캐폴드 잔재 제거"
```

---

## Task 20: 커버리지 확인과 문서 갱신

**Files:**
- Modify: `README.md`

- [ ] **Step 1: 커버리지 측정**

Run: `npm run test:coverage`
Expected: lines 80% 이상. 미달이면 부족한 파일을 확인해 테스트를 보강한다.

- [ ] **Step 2: `README.md`에 실행 방법 추가**

파일 끝에 다음을 덧붙인다.

```markdown
## 개발

```bash
npm install
cp .env.example .env   # VITE_USE_MOCK=true 로 두면 목업으로 실행된다
npm run dev
```

## 실데이터 전환

1. data.seoul.go.kr에서 일반 인증키를 발급받는다.
2. Vercel 프로젝트 환경변수에 `SEOUL_API_KEY`를 넣는다.
3. `CACHE_TTL_SECONDS`를 설정한다. 활용갤러리 등록 전에는 `3600`, 후에는 `300`.
4. `.env`의 `VITE_USE_MOCK`을 `false`로, `VITE_API_BASE_URL`을 배포된 프록시 주소로 바꾼다.

## 테스트

```bash
npm test
npm run test:coverage
```
```

- [ ] **Step 3: 린트 확인**

Run: `npm run lint`
Expected: 에러 없음. `console.log`가 남아 있으면 제거한다.

- [ ] **Step 4: 커밋**

```bash
git add README.md
git commit -m "docs: 개발·실데이터 전환·테스트 실행 방법 추가"
```

---

## 설계 문서 대비 의도적으로 줄인 것

조용히 빠뜨린 게 아니라 판단해서 뺀 항목이다.

| 설계 문서 항목 | 이 계획에서의 처리 | 근거 |
|---|---|---|
| 위치 조회 10초 타임아웃 | SDK의 `getCurrentLocation` 실패를 `unavailable`로 처리하는 것으로 대체 | SDK 자체 타임아웃 동작을 실기기에서 확인하기 전에 임의 타임아웃을 겹치면 원인 파악이 어려워진다 |
| `Environment.getNetworkStatus` 오프라인 감지 | `client.ts`의 fetch 실패 메시지로 대체 | 별도 브릿지 호출 없이도 사용자에게 같은 정보를 준다. 실기기 테스트 후 필요하면 추가 |
| 현재 위치 재조회 FAB | 위치 안내 문구 안의 "다시 시도" 버튼으로 대체 | 위치를 못 쓸 때만 필요한 동작이라 상시 노출할 이유가 없다 |
| `react-router-dom` | 상태 기반 화면 전환 | 화면이 둘이고, 라우터를 넣으면 토스 웹뷰 딥링크 처리까지 검증해야 한다 |
| `api/_lib/normalize.ts` | 프록시는 원본을 그대로 넘기고 클라이언트가 정규화 | 정규화 로직이 두 곳에 생기는 것을 막는다. 프록시는 중계와 캐시만 한다 |

## 인증키 발급 후 할 일 (별도 진행)

이 계획의 범위 밖이지만, 인증키가 나오면 바로 확인해야 하는 항목이다.

1. **일괄 조회 가능 여부 확인**
   ```bash
   curl "http://openapi.seoul.go.kr:8088/{KEY}/json/citydata_ppltn/1/100/%20"
   ```
   전체 명소가 한 번에 돌아오면 호출량이 명소 수와 무관해져 쿼터 문제가 사라진다. 그 경우 `api/citydata-bulk.ts`를 단일 호출로 바꾼다.

2. **`AREA_NM` 값 검증** — `src/data/areas.ts`의 30개 이름을 실제 호출로 하나씩 확인한다. 오타가 있으면 해당 명소만 조용히 실패한다.

3. **활용갤러리 등록** — 하루 1,000회 제한 해제. 출시 전 필수.

4. **토스 실기기 테스트** — mixed content 정책과 `getCurrentLocation` 실제 동작 확인.
