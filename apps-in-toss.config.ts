import { defineConfig } from "@apps-in-toss/web-framework/config";

export default defineConfig({
  appName: "seoul-live",
  brand: {
    primaryColor: "#3FD599",
  },
  permissions: [],
  webBundleDir: "dist",
});
